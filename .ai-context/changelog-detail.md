# Detailed change history

Every change, newest first, with the numbers that decided it. **This is the regression
suite.** This project has repeatedly been re-broken by reasoning about the code instead of
measuring it, so each entry records what was measured before and after — those figures are
how you tell whether a later change has quietly undone an earlier one.

Read this when you are about to change something and want to know what it cost to get
right. For *what shipped when*, see [`../CHANGELOG.md`](../CHANGELOG.md).

Entries predate the release-note split of 2026-08-22 and are not grouped by version: many
landed on the same day, and the useful axis here is the measurement, not the tag.

| Date | Change |
|---|---|
| 2026-09-05 | **The release is the plugin's three files; the exporter's zip is gone.** `release.ps1` no longer builds or attaches `dist/vault-graph-<version>.zip`, and `scripts/make-package.mjs` is deleted with it: the release attaches `main.js`, `manifest.json` and `styles.css`, which is all Obsidian downloads (the directory's scanner called the zip "an extra unsupported file"). The exporter, the standalone page and every fixture stay in the repo unchanged — the invariant suite drives the standalone, and that is what it is for now; someone wanting to run the exporter clones. `releasing.md` and the README's "Get it" step follow. |
| 2026-09-05 | **The settings tab adopts the declarative API (github#59), and the two other directory-linter warnings go with it.** The community directory's review board carried three warnings against 1.9.0: `settings-tab/prefer-setting-definitions` on `VaultGraphSettingTab`, `prefer-create-el` on two `document.createElement` sites in the engine, and a CSS-lint note that `clip-path` is only partially supported by Obsidian 1.6.5. **Settings.** `getSettingDefinitions()` returns the four build toggles, a "View" group with the four view toggles, and a "Folder colours" group holding one `render` item for the swatch picker; `getControlValue`/`setControlValue` read and write `plugin.settings`, with the build/view side effects (rebuild the view, or push through the live api) in `setControlValue`. `display()` stays as the pre-1.13 path (minAppVersion 1.7.2) and is built from the same two tables (`BUILD_SETTINGS`, new `VIEW_SETTINGS`) and the same `renderColourSection()`, so the two cannot drift; the three `this.display()` re-renders after a pick became `redrawColours()`, which redraws only the swatch rows — on 1.13 a `display()` call would have drawn the imperative tab on top of the declarative one. **Measured in Obsidian 1.13.7 on the demo vault through a CDP harness: the declarative path is what rendered (6 definitions, 29 rows, both headings), flipping a toggle went through `setControlValue` once and persisted, a swatch pick redrew 18 colour rows with the pick checked and saved, 0 page errors; the tab is the same layout as develop's imperative one.** Two things that harness found on the way: Obsidian 1.13's group renderer **replaces the list's children after the render callbacks run**, so a wrapper appended beside the row was created, filled and detached (18 orphaned rows) — the swatch scope now lives INSIDE the row, which is allowed to wrap (`.vg-colour-row` in styles.css); and Settings is **its own window** in 1.13 (a second CDP target), which is where a screenshot has to be taken. Also: `obsidian` pinned as a devDependency at 1.13.1 (the typings arrived transitively at 1.12.3 and had none of these types), which surfaced 74 `no-unsupported-api` errors on `plugin.settings` — 1.13 declares `Plugin.settings?: unknown` as the conventional slot and asks subclasses to "declare a concrete type"; the field is now declared on `VaultGraphPlugin` and the errors are gone. `SettingGroup.listEl` (1.11 API) is not used for the same reason. **Engine.** Colour parsing goes through a 1×1 `OffscreenCanvas` (no element at all; `floatColor` lost its `doc` argument), and the layer canvases come from the container's `createEl` when the host has one and the namespaced DOM call otherwise; the `prefer-create-el` exemption for `src/engine` in `eslint.config.mjs` is gone. **CSS.** The automatic-slot marker under a swatch is two linear gradients instead of `clip-path: polygon(...)`. **Written with the gradient axes swapped once, and the screenshot showed a notch instead of a triangle** — caught by shooting the marker at 12× in both builds, fixed, re-shot: the same upward triangle as develop's. Not verifiable here: the pre-1.13 `display()` path on a real pre-1.13 Obsidian (this machine was upgraded 1.12.7 → 1.13.7 for the test; the develop build's tab was shot on 1.13.7 as the "before"). |
| 2026-09-05 | **The hover checks aim only once the camera is still; the re-arm miss says which kind of miss it is (github#63).** `"hover re-arms after the pointer leaves the stage"` failed 7 of 7 on the dominant-folder fixture when run right after `"layout matches its golden snapshot"`, and passed 8 of 8 alone, always as `on 710 (t 1), off null (t 0), back on null (t 0)` — the github#7 shape. Diagnostics added to the check answered the issue's question: `target now 20,4px from the aim (radius 3.1px), element at aim CANVAS, camera ratio 1.0004 at the aim -> 1.08 now`. The hover was never lost; the aim was stale. The page boots with `fit()`'s 380 ms flight (ratio 1.0 → 1.08) running, and `settle()` covers only the page's own animations, never the camera. Alone the check took 2.2 s because `settle()` waited ~600 ms behind the boot tween, which outlasts the flight; after the golden check's `relayout()` — which cancels that tween and nothing about the camera — it took 1.6 s, `settle()` returning at once with the flight still in the air. A scratch replica that settled before the relayout re-armed every time (camera 1.08 throughout); `hardRelayout()` calls no `fit()`. Fix in the checks, not the page: both pointer-aiming hover checks call the existing `camSettle()` after `settle()` before computing the aim; `demo.busy()` was left alone (it describes the page's animations, and changing it would re-time every check and the demo driver for two checks' aiming). The re-arm check keeps the diagnostics on a miss. Adjacent finding, separate commit: `runOne`'s frames-arriving guard went through `page.j`, whose `JSON.stringify` turns a pending promise into `{}`, so `fps.frames` was undefined and the guard never fired from the day it was written; through `page.eval` it awaits the 600 ms sample and asserts, which also means the first check in each browser now starts after the boot flight. Live for the first time, it refused two demo shards in the first full run — **3 frames in 1421 ms**, twelve browsers booting at once — so the sample is taken after `settle()` and retaken up to five times before a job is refused; a stopped renderer never yields a frame, a starved boot does. The same full runs caught `"the pan toggle locks the camera and flies home"` once in six on the dominant-folder fixture (`flew home to (0.6465, 0.4186)`): `camSettle()` returned inside the first frames of fit()'s ease, the trap the auto-fit helper already records, so the check now waits for `camAtRest` — fit()'s own landing signal — before reading home. Third full run after both: **72/72 on all three fixtures**. After: the pair passes on all three store fixtures and three more times in a row on the dominant-folder one (`on 710 (t 1), off null (t 0), back on 710 (t 1)`), and `"hovering a note ramps in and releases at zero"` passes on all three. |
| 2026-09-05 | **`mountVaultGraph` returns a `destroy()`, and the plugin's teardown calls it (github#62).** Tearing a mount down was `api.renderer.kill()` plus an emptied container, which released the three WebGL contexts and nothing else: the mount stayed alive through the `mousemove` and `visibilitychange` listeners it had put on the document and the `ResizeObserver`s on its root and heatmap band, so every view close, popout/re-dock and Refresh in Obsidian retained a whole graph. Measured on the 10k fixture, six kill+remount cycles of the plugin's own sequence on the standalone build (`HeapProfiler.collectGarbage` twice before every read): heap **14.1 → 55.9 MB**, DOM nodes **632 → 4107**, JS listeners **140 → 926**, document `mousemove` listeners **2 → 8**, `visibilitychange` **1 → 7** — **+579 nodes, +131 listeners, +1 of each document listener, ~+7 MB per cycle**. Torn down mid-intro (`--quick`, the "three quick cycles" of the report), the killed mount was still `busy` at +400 ms and +1.2 s with its cascade at **159 → 182 frames**, converging at 207 ~2.4 s later — a cascade over the whole vault drawn to a dead renderer. Fixed with an `onDestroy` list in page.js: every site that registers something outside the root pushes its undo there (root RO, document mousemove — removed with the same capture flag it was added with — and visibilitychange, heatwrap RO, heat-slot RO, the context menu's three listeners), plus `dead` guards on the three resize callbacks, `cascade()` and the boot timer; `destroy()` cancels every frame and timer in flight (play, cascade, tween, hover, highlight, colour), runs the list in reverse, kills the renderer last (its `kill()` is idempotent, so the order is free), clears `window.__vg` if it is this mount's, and nulls the handle's `api`. The plugin's `teardown()` calls `handle.destroy()` and still empties its own container. A list rather than named fields because the sites are scattered across 15k lines and a forgotten field is the leak. After, same measurement through the new harness: heap **14.1 → 14.4 MB**, nodes **632 → 633**, listeners **140 → 140**, document listeners **2/1 → 2/1** over six cycles — the load baseline, held; mid-intro the old cascade stops at **155 frames, `busy` false at +400 ms**. New manual harness `scripts/teardown-check.mjs` runs the cycles and fails on growth (`--quick` tears down mid-intro, `--kill` runs the old sequence and fails on every build, which is what proves the assertions bite); manual because a cycle replaces the page's `__vg` under the rest of the suite. The debug api also carries `destroy` (stripped from the plugin build with the rest of that region) so the harness can tear the first mount down. Bundle: 421 KB, unchanged. |
| 2026-09-05 | **The exporter's default output is the vault root, not a numbered folder (github#64).** `src/build-graph.mjs` defaulted `--out` to `<vault>/03 - Resources/Vault Graph/vault-graph.html` — the PARA convention of the vault this was written for, and the last folder name baked into a tool whose one rule is that nothing about a vault is (decisions/0005 names the `05 - Templates` skip that broke the same way). `writeFileSync` creates no directories, so on the vault it was written for (the folder pair left with the source on 2026-08-22) and on any other vault without it, `scripts/refresh-graph.ps1` with no arguments died with `ENOENT`. Reproduced on a generated shape vault with HEAD's exporter: `ENOENT …  - Resources\Vault Graphault-graph.html`, exit 1. Default is now `<vault>/vault-graph.html` — the root is the one folder every vault has, and it syncs with the vault, which is the whole reason the output lives in the vault at all. A dot-folder (`.obsidian/plugins/…`) was rejected by the same ADR: dot-folders do not sync, which is why the tool left `.vault-graph/` in the first place. Creating the old directory was rejected because it would plant a numbered folder in whatever vault the exporter is pointed at. `--out` is unchanged and still creates no directories. After: the same command writes `<vault>/vault-graph.html` (954 notes, 1345 KB), exit 0. The usage line at the top of the file still said `node "03 - Resources/Vault Graph/build-graph.mjs"` from when the source lived in the vault; it now names `src/build-graph.mjs` and the vault flags. README and ADR 0005 updated to match. |
| 2026-09-05 | **"Filtered to the bone" stops reading the calendar, and stops calling an unmeasured step a collapse (github#65).** The pre-push gate failed on `develop@82b46bf` on the dominant-folder fixture only, one day after that fixture was generated, on a change that touched nothing near it: `range last 0.5%: dots collapsed, diameter/step 0`. Two causes, both in the check. (1) The three squeeze windows were fractions of `#vg-from`'s `min..max`, and the strip's right edge is today whenever the vault has reached it (github#57) — so "the last 0.5%" was measured back from a moving end, and the ageing fixtures date their newest notes to their generation day. Measured population of that window on the shape fixture by calendar day: **8 notes on generation day, 6 the next, then 4, 2, 0**. The windows now come off the oldest and newest *dated note*, `to` left open, the same rule the gap-reservation check states for its cut; the shape vault generated for `--end 2026-08-20`, for `2026-09-05` and the store's copy all print **86 / 24 / 8** notes for the three windows, the demo **1104 / 478 / 196**, the 10k **1306 / 591 / 195** (the regime the thresholds were tuned in: previously 194 and 184 at 0.5%). (2) The probe's `ds` divides the median dot by the median step of rows holding ≥4 notes, and 6 notes over two rows have no such row, so `medStep` was 0 and `ds` reported 0 — read as collapse. Measured in that state: smallest dot **5.3 px** against a resting median of **3.1 px**, outer-ring notes 42–74 px under `DOT_ROOM_MAX`; nothing had collapsed. `ds` is now asserted only when a step was measured (`stepped > 0`); otherwise the check asserts the github#53 class directly — the smallest visible dot's radius may not fall under the resting disc's median — and the detail line prints `d/s n/a` and the median dot in px. Pinning `--end` on the shape and demo fixtures was rejected: it would drop the live half of the heatmap-window check from all three fixtures, and the golden snapshots already prove those two layouts day-invariant. The layout is unchanged. After: the check passes on all three store fixtures (`8n 2r d/s 0.23`, `196n 5r d/s 0.27`, `195n 5r d/s 0.32` at 0.5%). |
| 2026-09-04 | **Second verification of the engine, pixel by pixel against develop's own build, and a screenshot mode for `render-diff.mjs` (github#58).** The first pass compared against the branch's own Sigma build. This one built the three fixtures from a checkout of `develop@79d829a` itself -- untouched page.js, graphology and Sigma, no engine anywhere -- and compared the current tree against them at **five** ratios (1.08, 0.6, 0.35, 2, 4.2), at rest and in a search, in four ways: camera (every node's `graphToViewport` and `scaleSize`), the renderer's layers composited, and two `Page.captureScreenshot` clips -- the stage (`#vg-stage`) and the whole page (`#vg-app`) -- which also see the logo, heatmap band, ribbon, legend and wedge labels the page paints itself from `graphToViewport`. Compared as PNG bytes first (the same Chrome encodes identical pixels to identical bytes), decoded by a 40-line PNG reader in the harness only when the bytes differ, to count and locate. **Result, 3 fixtures × 5 ratios × 2 states: camera max \|Δ\| 0 px; layers 0 pixels differing; stage screenshots PNG-byte-identical in all 30 cases; whole-page screenshots differ in 34–42 pixels inside one 7×8 px box per fixture, at the same x on every ratio** -- probed with `elementFromPoint`: the last digit of `#vg-stats`'s "Generated 2026-09-04 20:0x" line, the build minute, which differs because the two pages were built three minutes apart. Nothing else on the page differs by one pixel. `--mode screenshot` stays in the harness for the next renderer change. |
| 2026-09-04 | **What the adversarial review of github#58 found, fixed.** A fresh-context reviewer over the whole diff, with the Sigma 3.0.2 source beside it, verified every gesture, camera, event-ordering and store behaviour the page and the suite depend on as identical, and found seven things around them. (1) **The exporter's output depended on the working directory**: esbuild labels each bundled module with a comment holding its path relative to the process cwd, so `build-graph.mjs` run from the vault (`refresh-graph.ps1`) and from the repo produced files differing from line 1248 on -- while its own comment claimed byte-for-byte determinism. `absWorkingDir: ROOT` fixes it; **measured: the same fixture built from two directories is now byte-identical**. esbuild's errors are also printed as messages now rather than lost behind `logLevel: "silent"`. (2) **The README still promised a zero-install generator** ("No `npm install`") that the release zip could no longer deliver, and esbuild sat in `devDependencies`, so the natural `npm ci --omit=dev` installed nothing. esbuild is a runtime dependency now (the lockfile moves it), and README, `releasing.md` and `make-package.mjs` say `npm ci --omit=dev` once. (3) **The store's Sigma-compat facet outlived the switch by one commit**; deleted, with the page.js header paragraph that still called the renderer Sigma's. (4) **`kill()` left the camera wired**: a tween cut short by closing the view -- `fit()`'s 380 ms flight, drag inertia -- would keep scheduling full renders on lost contexts, silently. The camera has a `kill()` now, the renderer calls it, and a `killed` flag short-circuits `refresh`, `render` and both schedulers. (5) The label rule follows the reducer's type (a string is a label, anything else is none) instead of widening to `unknown` for numbers no reducer can return. (6) Listeners are a Set, so a function registered twice on one event fires once where Node's EventEmitter fired twice -- nothing does that; recorded in 0012. (7) Stale text: the pre-push hook's network-failure message pointed at `src/vendor.mjs`; `install-spike.ps1` still copied the deleted bundles; three page.js comments described Sigma's quadtree and label grid as current. `getCanvases()` keeps `Record<string, …>` on purpose: page.js indexes it with a string list that includes a layer the engine no longer has. Lint 0/0 after all of it. |
| 2026-09-04 | **The engine's refresh was 6 ms a frame slower than Sigma's, and is now at parity (github#58 step 5).** The issue's step 5 says any of `main.js` size, frame time or the board's count getting worse is a finding, and the frame time had: `probe-frame.mjs` on the 10k fixture, run back to back on the same machine from a pre-switch worktree (Sigma) and the current tree, read **refreshFull 9.4 ms vs 15.4–17 ms, script per cascade frame 27.1 vs 34.5–36.3 ms, frames over 50 ms 4/83 vs 13/61**. The `--profile` self-time table named it rather than a theory: the renderer's own `addNode` 6.7 ms/frame (Sigma's 3.1 in `perf-cascade-frame-cost.md`), `process` 3.7 (2.5), the garbage collector 3.0. Three causes, all things the port did that Sigma did not: `applyNodeDefaults` built a second object per node through `Object.assign(styled, {…literal})` where Sigma wrote only the missing fields in place; `process()` looked every node up in the data map twice (and again inside the z-sort comparator); `floatColor` lower-cased every colour string before its cache lookup, 10,000 short-lived strings a frame during a cascade, and its cache was unbounded (Sigma's was too -- a cascade's per-frame alphas never recur, so it grew for the life of the page; capped at 200,000 entries now). **After:** refreshFull **8.8 ms**, refreshSkip 10.4, refreshPart 8.0, script per cascade frame **26.3 ms**, frames over 50 ms **3/80** -- at or slightly under Sigma's numbers on the same box, with plan (7.4) and layout (4.7) unchanged as the page's own terms. Pixel identity against the Sigma reference holds (0 differing, edgeInk equal); lint 0/0. |
| 2026-09-04 | **Sigma is gone: the engine draws the page in both hosts, and the vendor layer with it (github#58 steps 3.6 + 4, one commit).** `src/page.js` constructs the engine's typed `RendererOptions` -- `win`, the two reducers, `drawHover`, the label settings, the camera clamps, `zoomingRatio` / `zoomDuration` / `minEdgeThickness`, `enableCameraPanning` -- and nothing else: the fourteen Sigma-only keys (`renderLabels`, the three label-grid settings, `zIndex`, `zoomToSizeRatioFunction`, `enableCameraRotation`, `enableCameraZooming`, `defaultEdgeType`, the two program registries, `enableEdgeEvents`, `allowInvalidContainer`, `defaultDrawNodeHover` renamed) are gone with their guards (`RENDERING`, `haloOn`, the `EdgeCurveProgram` test in the edge reducer); the comments that carried measured reasoning stay beside the settings that remain. `deps.Sigma` is `deps.Renderer`, `deps.rendering` is gone, `preventSigmaDefault` is the payload's `preventDefault`, and the page's typedefs are `RendererLike` / `RendererSettings` / `RendererEvent` / `MouseCoords` / `RendererCtor`. shell.html hands over `VaultGraphEngine.Renderer`; plugin/main.js imports it. **Deleted:** `vendor/` (sigma 3.0.2 and its NOTICE), `src/vendor.mjs`, `src/engine/compat.ts`, the `vendor-no-network` esbuild plugin, the exporter's `--renderer` switch and licence header; `check-network.mjs` owns the network-primitive list and scans our sources and the built artifacts (18 files); `make-package.mjs` ships `package.json` and the lockfile instead of `vendor/`, since the exporter bundles the engine with esbuild (`npm ci` once); `src/engine/NOTICE.md` carries Sigma's MIT attribution for the ported camera math and shaders; decision 0008 is superseded by 0012; README, CONTRIBUTING, architecture, releasing and the invariants say so. **Measured:** `main.js` **694 KB → 417 KB**; lint 0 errors, 0 warnings; `check-network`, `check-scope` (199 rules, 49 ids), `check-pii` clean; **against Sigma-rendered reference pages of all three fixtures built before the switch, `render-diff` reports 0 pixels differing at ratios 1.08 / 0.35 / 4.2, at rest and in a search, camera max \|Δ\| 0 px, edgeInk equal**; golden layout snapshots byte-identical ×3; the suite's hover, drag, hub, pin, focus-web, camera, zoom, pan and click checks **28/28 on the demo and 10k fixtures, 27/28 on the dominant-folder one in the parallel run** -- `a link's stroke holds its width at any zoom`, which passed alone on that fixture twice straight after with identical numbers (rest 1px/fan 103px → 1.71px at 5x and 10x, cap 4px) and had passed in the same parallel run against the pre-switch engine build, so it is filed here as the camera-timed flake under three concurrent Chromes that `invariants.md` already describes, not as a difference. `scripts/render-diff.mjs` now compares the current build against reference pages (`--against-dir`) so it outlives the switch as the before/after harness for any renderer change. |
| 2026-09-04 | **The engine's renderer draws Sigma's picture, byte for byte, behind a build switch (github#58 steps 3.1–3.5).** `src/engine/{viewport,camera,captor,colors,programs,renderer}.ts` port sigma 3.0.2's camera and viewport math, its MouseCaptor, its colour packing, and four WebGL2 programs -- circle, the border program expanded for the page's one halo configuration, rectangle edge, and @sigma/edge-curve's curve -- with the picking framebuffer, the label grid, touch, edge events and WebGL1 left out. `build-graph.mjs --renderer sigma\|own` (default `sigma`; `VG_RENDERER` in the environment is the default for harnesses) ships both and shell.html hands the page whichever, through a shim that turns page.js's Sigma-shaped settings bag into the engine's typed options (`src/engine/compat.ts`, transitional). **Measured by the new `scripts/render-diff.mjs`**, which builds a fixture both ways, loads each in turn in one Chrome tab (two tabs would leave one in the background, where the page's frame-deferred edge-cap refresh never runs), sets the same camera state and compares: **camera parity exact -- max \|Δ\| 0 px over every node's graphToViewport and scaleSize on all three fixtures at ratios 1.08 / 0.35 / 4.2; pixels byte-identical -- 0 of 1296×731 differing at any ratio on any fixture, edgeInk equal to five decimals (shape 0.10255 / 0.54906 / 0.01236, demo 0.11274 / 0.6719 / 0.01293, 10k 0.11442 / 0.61436 / 0.01264); and byte-identical again in a search (`--query note`, demo), which is the state that paints the labels, hovers and hoverNodes layers.** The suite's own interaction and camera checks run against the engine build (`VG_RENDERER=own node scripts/smoke.mjs --only hover --only drag --only hub --only pin --only "focus web" --only camera --only zoom --only pan --only click`): **28/28 on each fixture.** **Three things measured that reading Sigma had not given:** (1) the first camera comparison was off by a constant ≈28 px × ratio at every ratio (25.9 / 79.8 / 6.65 px) -- Sigma's default `stagePadding` of 30, which the page never set and the port had as 0; one constant, then 0. (2) `focus web stays above dim notes` failed on every fixture (shape: 23 blue, 921 dim samples) because the page's `drawFocusWeb` opens with `if (data.key !== f) return` and Sigma had put `key: node` into the hover data; typed now as `HoverData` and handed over the same way -- 985 blue, 0 dim after. (3) The lint's program and the engine's disagreed on whether a listener cast was necessary, because only the strict one had `strictFunctionTypes`; the shared `tsconfig.json` now sets that one flag so a fix for one program cannot fail the other. **Two behaviours differ by design and are recorded in decision 0012:** picking is by geometry (within `size / ratio` px, last-drawn wins) where Sigma read a colour-coded framebuffer at half resolution, so aiming is exact rather than 2 px-quantised; and the label grid is gone, which drops the plain label Sigma occasionally drew for the hovered note during the first half of the hover ramp when that note happened to be the largest in its 150 px cell (the grid held every visible node, empty labels included; a look at `LabelGrid.getLabelsToDisplay` shows it always admits at least one per cell). Lint 0 errors, 0 warnings with the engine under the five no-unsafe errors and the preset's TypeScript rules; `check-scope` clean with the layers positioned by page.css rather than inline styles. Sigma still draws by default; the switch is the next step. |
| 2026-09-04 | **graphology is gone: the graph store is ours (github#58 step 2).** `src/engine/store.ts` implements the 16-member `GraphStore` over three Maps -- node attributes held by reference exactly as graphology held them, edges in insertion order under keys of our own, adjacency per node -- and reproduces graphology's one non-obvious behaviour: a node's neighbours and per-node edges came back in JavaScript property order (graphology kept them in a plain object and `neighbors()` was `Object.keys` over it, read in its 0.26.0 source), so `inPropertyOrder` sorts array-index ids ascending and keeps the rest in insertion order. No event emitter: `quietWrites` and the listener trio leave `src/page.js`, its three bulk position loops are plain loops again, and the one write that leaned on Sigma's subscription -- the node-drag frame -- now makes the same `refresh({ partialGraph: { nodes: [id] }, schedule: true })` call Sigma's own handler made. The exporter gained its esbuild step here: `build-graph.mjs` bundles `src/engine/index.ts` into one IIFE (`window.VaultGraphEngine`) inlined where the two vendor scripts sat, and shell.html and plugin/main.js hand `GraphStore` into `deps.Graph`. `vendor/graphology.umd.min.js` (graphology 0.26.0) deleted; `vendor.mjs`, `NOTICE.md`, `check-network.mjs` (now reads every `.ts` under `src/engine`) follow. **Two things measured that the plan had not foreseen.** (1) Sigma holds the graph itself and validates it with graphology-utils' `isGraph` -- `addUndirectedEdgeWithKey` and `dropNode` as functions, `multi` as a boolean -- then subscribes with `on`/`removeListener` and indexes through `getEdgeAttributes` and `edges()`; without those the page threw `Sigma: invalid graph instance.` and every check reported `page never finished its intro`. The store carries them as a marked transitional facet (no-op listeners, since nothing is ever emitted) that goes with Sigma at step 3.6; none is on the `GraphStore` interface or called by the page. (2) The private edge map and the compat `edges()` method shared a name: `tsc` refused it with TS2300 while esbuild bundled it happily -- and in that bundle the field shadowed the method, which is a second boot failure the typecheck gate caught first. **Measured after:** lint 0 errors, 0 warnings (typecheck included); `main.js` **694 KB → 597 KB** (the 74 KB bundle plus its CommonJS wrapping); golden layout snapshots **byte-identical on all three fixtures** (954 / 1403 / 10002 notes, band and positions); the `--only` set for hub, drag, focus web, hover, pins, lazy edges and the (unlinked) row **23/23 on each fixture**; `check-network` clean over 12 files, `check-scope`, `check-pii` and `check-build-order-determinism` clean. No automated check drags a note into the hub, so the drag-frame refresh is covered by the identity of the call and by the visual review at the switch. |
| 2026-09-04 | **The engine's interface exists and the toolchain reads it: github#58 step 1, zero runtime change.** `src/engine/types.ts` declares what the page needs from a graph store and a renderer -- `GraphStore` (16 members), `Renderer` (16), `Camera`, `RendererSettings`, `RendererOptions`, the nine `RendererEvents`, `MouseCaptor` -- lifted from the JSDoc typedefs github#60 wrote for the vendored bundles, and `src/page.js` re-points those typedefs to it (`@typedef {import("./engine/types").Renderer} SigmaLike`): a comment-only diff, 27 lines in for 116 out. `CameraLike` gains `setState`, which the suite calls and the typedef never named. **Toolchain:** `tsconfig.json` includes the engine; `tsconfig.engine.json` extends it with `strict: true` over the engine alone; `scripts/lint.mjs` runs `tsc --noEmit -p tsconfig.engine.json` ahead of eslint, so the hook and `release.ps1` inherit the check; `eslint.config.mjs` puts `src/engine/**/*.ts` in the block carrying the five `no-unsafe-*` errors. **Why two configs, measured:** `strict: true` on the shared program put **29** errors on `src/page.js` (all `getAttribute()` → `string \| null` under `strictNullChecks`, github#55's Phase 4) and had esbuild -- which reads `tsconfig.json` -- inject three `"use strict"` directives into `main.js`; `alwaysStrict: false` removed the directives but not the 29. With `strict` off the shared program and on the engine's: **lint 0 errors, 0 warnings (4.4 s including the typecheck; tsc alone 0.3 s), `main.js` byte-identical to `develop@79d829a`'s build, goldens unchanged on all three fixtures** (954 / 1403 / 10002 notes, band and positions). The gate was verified rather than assumed: a `g.nope` on a `GraphStore` parameter in a throwaway `src/engine/gate-probe.ts` failed `npm run lint` with `TS2339`, then removed. **The vendored versions are identified**: hashing against the npm tarballs, `sigma.min.js` is byte-exact `sigma@3.0.2/dist/sigma.min.js` (3.0.1 and 3.0.3 differ) and `graphology.umd.min.js` is `graphology@0.26.0`; recorded in `vendor/NOTICE.md`, which had said nobody could tell. Decision record `0012-own-graph-store-and-renderer.md` carries the seven decisions from the plan gate. |
| 2026-09-04 | **`compact axis: a year's width tracks its own note count` was asserting on a foreshortened total, and failed on the dominant-folder fixture with the axis behaving correctly.** It compared the busiest year's drawn pixels against the quietest's, but the year holding the **incomplete final month** is deliberately narrower: github#51 gives that month only the share its elapsed days have earned (`lastFrac`), so the range handle cannot sit past the newest note. That deduction is a fraction of one month's width, which is bigger than the gap between two years of similar size once both sit at or near `yearRef`'s ceiling. **Measured on the fixture the day it was found:** span 2025-07-01 → 2026-09-04, so 2025 held 460 notes over 6 months and 2026 held 494 over 9; `yearRef` came to 494, giving 2025 a weight of 11.24 average-months against 2026's full ceiling of 12 -- **the right order**. Then 2026's September, 4 days into a 30-day month, gave back 0.87 of its 1.33-month share, leaving 10.84 against 11.24 and drawing **465px against 483px**. The axis and its ordering were both correct; the assertion was reading a foreshortened total as an allotted one. The check now sets aside the year holding the final month and compares the full years, reporting NOT ASSERTED with the reason when fewer than two remain. **Still asserts where it matters** -- demo `2025 (247 notes) 235px` vs `2016 (16) 38px`, 10k `2022 (1011) 96px` vs `2016 (311) 35px` -- and **verified it still catches its own failure mode**: pinning `yFrac` to a constant so note count stops mattering made both years draw 87px and failed both fixtures. It had been failing on `develop` since the fixture store regenerated that morning, blocking every push. |
| 2026-09-04 | **The five `no-unsafe-*` rules are ERRORS and the budget is retired — github#60 is done.** With the count at zero a budget says nothing `error` does not say better, so `eslint.config.mjs` sets the five to `error`, `scripts/lint.mjs` drops the budget arithmetic and fails on **any** finding (a `--budget` argument is still accepted and ignored, so an old hook or script does not break), and `package.json`'s lint script is a bare `node scripts/lint.mjs`. **The formatter needed a matching change, and missing it would have been the quiet failure:** `lint-summary.mjs` folded the five into per-rule counts instead of printing them, which is exactly right for 7,000 warnings and exactly wrong for one error -- a failing run would have said "1 error" without saying where. It now folds them **only while they are warnings**. **Verified the gate catches what it exists for**, the way the golden-snapshot check was: `function gateProbe(x) { return x.nope; }` added to `src/page.js` failed the run with `3 errors, 1 warning`, naming file, line and rule; removed again. The invariant is retitled from "The lint warning count does not grow" to "Our own code lints clean", and `CONTRIBUTING.md`, `.githooks/pre-push` and `release.ps1` no longer describe a budget. **The whole of github#60, measured: 6,977 → 0** over eleven batches in one day. |
| 2026-09-04 | **The no-unsafe meter reaches ZERO: 176 → 0, and two real defects fell out of the last stretch (github#60, batch 3k).** The typing itself was the long tail of the debug API -- `bandStat`, `edgeReport`'s `take`, `probeReport`'s sample series, `checkFocusWeb`'s pixel readers, `demoFind`'s year chips -- plus the ribbon drag's `lo`/`hi`/`follow`, the `ResizeObserver` the popout window is viewed through, and the last multi-name `var` splits. **Two findings the types surfaced, both fixed here rather than typed around.** (1) `demoBigInnerNote` read `geomLock.bandR.i[0]` and `[1]` as a `[lo, hi]` pair. `geomLock.bandR` holds **one number per band** -- the outermost row radius, what `seamAngle` divides a width by; the pair shape belongs to `drawWedgeDebug`'s own local of the same name. Indexing the number gave `undefined` for both ends, so every `rNorm < lo \|\| rNorm > hi` was false and the inner-ring filter **never excluded anything** -- the function returned the biggest note anywhere, which its own comment says in capitals it must not do. Now computed as `drawWedgeDebug` computes it, from `r0` and `INNER_FILL`. (2) `nodeReducer` had `withAlpha(r.color \|\| a.color, al)`, but `NodeAttrs` carries no colour -- the reducer is where colour comes from -- so `a.color` was always `undefined` and the fallback dead; removed, behaviour identical either way (both paths reach `toRgb`'s grey). One suppression was written and then removed: an `eslint-disable-line` on the `ResizeObserver` line, replaced with a type naming the constructor the popout window carries, because a relaxed check is not a fix. Golden layout snapshots unchanged on all three fixtures. |
| 2026-09-04 | **The demo driver's two contracts are stated: 263 no-unsafe findings → 176 (github#60, batch 3j).** `DemoTarget` is the one that was worth writing down. `demoFind` resolves a beat's `[kind, arg]` to **either a real element or a plain rect-like stand-in** that `demoNoteRect`, `demoBigInnerNote` and `demoPoint` build for things with no element of their own -- a note on the disc, a heatmap cell, a point on the ribbon. Both shapes answer the same three questions `demoWhere` asks (`getBoundingClientRect`, the `expect`/`gap`/`demoLabel` annotations, `getAttribute`), which is why the code passes them interchangeably; the typedef states that duck-typed contract rather than pretending one is the other, and it took 36 findings off `demoWhere` and `demoFind` together. `animateTo`'s `from` map is the other: it carries **two shapes, one per branch of `polar`** -- cartesian `x`/`y` plus target `tx`/`ty`, or polar start radius and angle plus the deltas to walk -- read back under the same flag, so both are declared optional on one record rather than split. Also the last of the small helpers: `hubSlots`, `togglePin`, `select`, `centerOn`, `showTip`, `showRTip`, `ribbonMsLinear`, `winEndCentredAtPx`, `demoRibbonPoint`, and the two demo point arrays. Per rule: 124 / 42 / 25 / 61 / 11 → 75 / 28 / 20 / 43 / 10. Golden layout snapshots unchanged on all three fixtures. |
| 2026-09-04 | **The demo storyboard is typed and the last `Object.create(null)` sites are gone: 314 no-unsafe findings → 263 (github#60, batch 3i).** `DemoBeat` names what `demoMode()` returns -- one beat is settle / click / dblclick / rightclick / hover / drag / wheel / park, every field optional, with `act` and `why` labelling it and `target` a `[kind, arg]` pair `demoFind` resolves -- and `DemoSpot` what `demoWhere` reports back. That types the three entry points the driver actually calls (`demoMode`, `demoAct`, `demoFullStoryboard`). The nine remaining `Object.create(null)` sites went through `dict()`, which retires that pattern from the file: **the only one left is inside `dict()` itself**, where it belongs. Per rule: 142 / 65 / 26 / 65 / 16 → 124 / 42 / 25 / 61 / 11. What is left is a long tail of one-off locals inside the debug API's ~70 members, none of which ships. Golden layout snapshots unchanged on all three fixtures. |
| 2026-09-04 | **`src/page.js` down to the stripped regions: 343 no-unsafe findings → 314 (github#60, batch 3h).** The residue the section batches left behind, all of it in code that ships: `hubRing`, `unpin`, `inHubHole`, `ringColors`, `dotPx`, `capEdge`, `curvatureFor`, and the frame coalescer's `onFrame(fn)` callback -- the last of which is shared by the ribbon drag and the hub-pin drag, so one signature covered both. **What is left is 133 in the demo/debug region and 130 in the `go` block**, plus 51 scattered in the animation, logo, tooltip and detail-panel sections. The first two sit inside the marker pairs `scripts/build-plugin.mjs` cuts out before esbuild parses the file, so **none of the remaining majority ships in the plugin** -- it exists only in the standalone build, where the invariant suite drives it over CDP. Per rule: 162 / 66 / 27 / 69 / 19 → 142 / 65 / 26 / 65 / 16. Golden layout snapshots unchanged on all three fixtures. |
| 2026-09-04 | **`src/page.js` render path and date ribbon typed: 810 no-unsafe findings → 343, budget lowered to match (github#60, batch 3g).** Everything between the hub and the reducers, in the file's own order: the pinned hub (51), the note drag, the position tween, the animation and hover ramps (49 + 43), the render block, edge width and curvature (55), the tooltip and detail panel. The shapes that carried it: **`nodeDrag`**, declared `null` and read as `.moved`/`.over`/`.wasPinned`/`.x0` from four handlers -- now a `NodeDrag` typedef, with `x0`/`y0` optional because they are set on the first move, which is what makes `NODE_DRAG_MIN` measure from where the pointer actually started; **`focusSetCache`** and its `set`; **`hl`**, the per-note highlight ramp; **`mixCache`**, **`neighbourCache`**, **`lazyShown`/`lazyAdded`**, and the frame coalescer's `pend`. Then **Sigma's two reducers**, which is where the typing pays back most: `nodeReducer(id, a)` and `edgeReducer(id, a)` are called once per note per frame and every attribute read inside them was `any`; with `NodeAttrs` and `EdgeAttrs` on the parameters they are checked against the same attributes `addNode` writes. `nodeStyle`, `drawHover` and `drawFocusWeb` take a real `CanvasRenderingContext2D` and `NodeDisplayData`. The ribbon came with it, since it draws through the same `fitCanvas` the heatmap batch had already reached: `BrushDrag` names the in-flight drag (`mode`, the pending `pFrom`/`pTo` that `brushEnds()` reads back mid-drag), and `fitCanvas` returns a real `CanvasRenderingContext2D`. Per rule on the page: 481 / 129 / 92 / 78 / 30 → 162 / 66 / 27 / 69 / 19. **Golden layout snapshots unchanged on all three fixtures, with the hub, hover, edge and pin checks green.** What is left is 181 in the demo/debug region and 136 in the `go` block -- both inside the markers `scripts/build-plugin.mjs` strips, so none of it ships in the plugin -- plus ~80 scattered across the small sections. |
| 2026-09-04 | **`src/page.js` heatmap typed: 1,112 no-unsafe findings → 810, budget lowered to match (github#60, batch 3f).** The band carried 244 findings on 139 lines, and two declarations held nearly all of them: `heat`, declared `null` and holding the whole grid -- geometry, the per-day buckets, the level cuts -- so every `heat.days[k]`, `heat.cell` and `heat.keys[i]` was `any`; and the canvas contexts, where `$("heatc")` returns an `HTMLElement`, which has no `getContext`, so both `ctx` bindings were **error-typed** and every one of the ~60 `ctx.fillStyle`/`ctx.stroke()` calls under them followed. `Heat` and `HeatDay` name the first; the three canvas ids (`heatc`, `heatkey`, `ribbon`, all `<canvas>` in `src/page.html` -- checked, not assumed) are cast to `HTMLCanvasElement` at their five lookups, and the contexts to `CanvasRenderingContext2D`. Casting the ribbon's canvas took **302** off rather than the section's 244, since the ribbon draws from the same pattern two sections down. Signatures on every heat function: `heatParse`, `heatKey`, `heatMonday`, `heatLevel`, `heatTile`, `heatRect`, `heatHit`, `heatShowTip`, `heatDrawKey`, and the `q`/`setHover` closures. Per rule on the page: 654 / 175 / 131 / 110 / 42 → 481 / 129 / 92 / 78 / 30. **Golden layout snapshots unchanged on all three fixtures and the heatmap checks green.** |
| 2026-09-04 | **`src/page.js` legend, settings panel, search and context menu typed: 1,499 no-unsafe findings → 1,112, budget lowered to match (github#60, batch 3e).** The UI section carried 322 findings on 173 lines, and **one line was worth 51 of them**: `$()`, the id helper every one of ~200 call sites goes through, returned `Element | null` from `querySelector` and so was `any` to every caller reading `.style`, `.hidden` or `.textContent`. It is `HTMLElement` now -- every id it resolves belongs to markup this file owns (`src/page.html`) -- and a caller wanting something narrower casts at its own site: `HTMLInputElement` for the search box's `.value`, `HTMLCanvasElement` for a context. **The type had to be declared in the JSDoc block, not as a cast inside the line**: `check-scope.mjs` matches that accessor **verbatim** (`js.includes('var $ = function (id) { … }')`) to assert it is still root-scoped, and a `/** @type */` around the `querySelector` fails it -- caught here, by the check doing its job. A second measurement from the same fix: an escaped comment terminator inside a JSDoc block silently breaks the whole block, and the meter went **up** to 1,816 rather than down; the number is the tell. `each()`, the legend's own `querySelectorAll` walker, was the second: its untyped callback was behind every `b.getAttribute("data-…")` chain in the delegated handlers. Then the shapes: `kids` (path → child → count), `play`, `ptr`, `colorsBefore`, `movesFrom`, `refreshSettingsPanel`, `logoMaskImg`, and `bandColors`' two parallel arrays; signatures on `swatchFill`/`swatchTitle`/`countText`, `subtree`/`srow`/`eyeBtn`, `onlySubs`/`onlyUnder`, `swatchButtonsHTML`, `openCtxMenu` (eleven parameters), `subfolderRows`, `pickColor`/`pickSubColors`/`pickVisible`, `regroup`, `sweepTo`, `ringColorsSmooth`. **Range: 322 → 0.** Two casts record a real gap rather than hiding one: `ev.target` is an `EventTarget` and the three "only" handlers read `getAttribute` off it, and `esc()`'s escape table needed a `Record<string, string>` binding because an object literal indexed by a `string` is `any`. Per rule on the page: 874 / 248 / 181 / 147 / 49 → 654 / 175 / 131 / 110 / 42. **Golden layout snapshots unchanged on all three fixtures, and the legend and right-click checks green** -- this section owns them. |
| 2026-09-04 | **`src/page.js` timeline and wedge-debug overlay typed: 2,057 no-unsafe findings → 1,499, budget lowered to match (github#60, batch 3d).** The section (`buildTimeline`, `buildDateSpan`, the range controls, `wedgeEdges`/`drawWedgeDebug`/`drawWedgeLegend`, the hover and highlight predicates, `timeFactor`) carried 384 findings on 178 lines. Two shapes carried most of them. **`dateSpan` was declared `null`**, so the axis every control reads -- `months`, `years`, `index`, `lo`/`hi`, `axis.segs` -- was `any` from its declaration outward; it is now `DateSpan` with `Month`, `YearCount` and `AxisSeg` beside it. **`DBG` was `{ on: false, cells: null, canvas: null }`**, so `DBG.canvas` was `any` and every `g2.*` call in the ~350-line overlay followed from a `getContext` on it; `DebugState` types the three fields, the context is asserted to `CanvasRenderingContext2D` at the one `getContext`, and `DbgCell` names what the overlay actually records per cell -- **not** a `Cell`: it is the arc a cell was drawn at this frame (`seams`, `f0`, `f1`, the optional locked `pLead`/`pTrail`, and the ids), which is why the three `edgeSweep` calls that hand one to a `Cell` parameter go through an explicit `unknown` cast rather than pretending the two are the same shape. **Range: 384 → 0.** Per rule on the page: 1,211 / 359 / 244 / 182 / 61 → 874 / 248 / 181 / 147 / 49. **Golden layout snapshots unchanged on all three fixtures, and the date-range and compact-axis checks green** -- this section owns them. The bundle differs from batch 3c's by the split declarations, seven `dict()` sites, and esbuild's reformatting around the casts. |
| 2026-09-04 | **`src/page.js` reveal cascade typed: 2,531 no-unsafe findings → 2,057, budget lowered to match (github#60, batch 3c).** `cascade()` and its helpers, ~1,375 lines carrying 409 findings on 180 lines. Three sources, as measured beforehand: the per-note and per-group dictionaries (32 `Object.create(null)` sites -- `keep`, `sweepOf`, `isMove`, `to`/`from`, `delay`, `arriveAt`/`crossAt`, the toggle tallies, `rowsSrc`/`rowsDst`, `bandSrc`/`bandDst`, `rgbCache`, `dotFit`, `posSrc`, the `pairUp` records), the untyped `opts` object (`CascadeOpts`: `fullRing`, `colToggle`, `movesFrom`, `order`, `spread`, `totalMs`, `onFrame`, all optional), and the in-flight record plus the interpolation pairs (`cascadeRun`, `WalkPair`). Every helper and closure has a signature -- `toRgb`, `withAlpha`, `present`, `windowFor`, `clockwise`, `staticPlan`, `cellsOfG`, `deepen`/`record`, `roomOf`, `pairUp`, `rowsAt`, the four `*Walk` interpolators, `walkPair`. **Range: 409 → 0.** Typing `anim`/`animGuard` (the tween's frame and its guard timer, declared in the animation section) took 17 more off that section on the way. Per rule on the page: 1,497 / 473 / 258 / 228 / 75 → 1,211 / 359 / 244 / 182 / 61. Code-shape changes: multi-name `var` declarations split so each carries its type, and `Object.create(null)` → `dict()`; nothing else. **Golden layout snapshots unchanged on all three fixtures, and the suite's own cascade checks (`--only cascade`) green** -- the goldens cannot see motion, so those are the check that matters for this section; the bundle differs from batch 3b's by those sites and comments only. |
| 2026-09-04 | **`src/page.js` rings layout typed: 3,653 no-unsafe findings → 2,531, budget lowered to match (github#60, batch 3b).** The wedge planner -- `allocateBand`, `buildWedgePlan`, `ringsLayout` and the seam helpers, ~2,560 lines and 816 of the file's findings, the largest section in it -- is typed from the objects outward. Typedefs declared beside the code, inside `mountVaultGraph`: `Cell` (a group, subfolder slice or the merged small-groups wedge, with the fields the plan fills in as it goes), `Slot` (one placed note), `Plan` (what `buildWedgePlan` returns), `GeomLock` (the radii and band totals frozen at load), `SpHold`, `AllocOpts`, `BandNum` (per-band numbers keyed `"i"`/`"o"` -- as a `Record`, because indexing a `{ i, o }` literal with a string key is implicit `any`), and `Probe` for the standalone's per-frame probe. Every function in range has a signature (`weightOf`/`rowsOf` callbacks and the `shareOf`/`fracOf` closures included), every dictionary declares its shape through `dict()` (36 sites, the cascade declarations included), and the cascade state the planner shares -- `alpha`, `pinnedPlan`, `planKeep`, `colWalk`, `splitHold`, `roomNow`, `lastGapN`, `lastStart`/`lastArc`/`lastBand`, `cellRoom`/`cellNow`/`edgeCap`/`edgeNow`/`hubRow0` -- is typed at its declaration in the cascade section, since the findings it caused were here. The two cell literals are asserted to `Cell` where they are pushed, since the rest of their fields are written before anything reads them. **Range: 816 → 0.** Per rule on the page: 2,210 / 774 / 310 / 262 / 97 → 1,497 / 473 / 258 / 228 / 75. Code-shape changes: `var a, b` declarations split so each can carry its type (`gw`, `groupPres`/`nG`, `big`/`smallIds`, `ins`/`outs`, `recs`/`acc`, `rowW`/`rowFirst`/`rowLast`, `edgeA`/`edgeB`, `lastAt`/`firstAt`, `seams0`/`before0`/`frac0`, `over`/`under`/`live`) and `Object.create(null)` → `dict()`; nothing else. **Golden layout snapshots unchanged on all three fixtures** (10k pinned at `--end 2026-08-28`); the bundle differs from batch 3a's by those sites and comments only. |
| 2026-09-04 | **The 10k fixture's `--end` is pinned to 2026-08-28, the day its golden was taken, and a pinned fixture no longer ages out of the store.** The 10k golden layout snapshot is not day-invariant, and nothing had measured that: `invariants.md`'s "a snapshot taken today stays valid indefinitely" was measured on the demo and shape vaults (3.5 years apart in `--end`, byte-identical) and holds there; `check-generator-determinism.mjs` covers the same two. The 10k vault files its daily, meeting and archive notes into year-month subfolders derived from their dates (`YM(18)`, `YM(14)`, `YM(8)` in `make-test-vault.mjs`), so moving `--end` moves notes between subfolders and the subfolder cells with them. **Measured on the first weekly refresh after the goldens were recorded** (store stamp 2026-08-28 → regenerated 2026-09-04, exactly `FIXTURE_MAX_AGE_DAYS`): `893 note(s) moved past 0.1 units, worst is #5296: radius 9317.1 -> 9637.1, angle -80.4° -> 169.1°`, identically on `develop@3aa9401`, on the #60 branch and on the comment-strip branch, while the demo and shape goldens survived the same regeneration. Every push to `develop` was being refused at that check. Now `smoke.mjs`'s `resolveVaults()` and `update-layout-snapshots.mjs` both generate the 10k with `--end 2026-08-28` (the args are part of the store digest, so they have to agree), and `fresh` ignores the age for any fixture whose args carry `--end`. **Measured after:** the pinned vault regenerated under digest `0f1d5979` and matched the golden byte-for-byte -- `10002 notes checked ... band unchanged; positions unchanged` -- with the other two unchanged; a second run reused it (no `generating` line). The cost is the live half of the heatmap-window check on that one vault, which the two ageing vaults still carry. Re-recording the 10k golden now means choosing a new `--end` in both scripts in the same commit. |
| 2026-09-04 | **`src/page.js` preamble through grouping typed: 4,377 no-unsafe findings → 3,653, budget lowered to match (github#60, batch 3a).** The first interior slice, lines ~380–1840 in the file's own order: theme and colour maths, `state`, graph + base layout, grouping. Measured before: two sources carried nearly all of the range's ~250 findings. **`Object.create(null)` is `any` to the type program** and cannot be typed at the call site (a cast is read as its inner expression; `{}` would change prototype semantics, and a folder named `constructor` or `toString` has to keep working), so the file gained one `dict()` helper -- generic over the value type, the `any` laundered through `unknown` once -- and each of the ~35 dictionaries in range declares its own shape: `@type {Record<string, number>}` on the var, `= dict()` after it. Same null-prototype object, same behaviour; the one mechanical change, and the only code the diff touches besides comments. The other source was **functions with no `@param`** (`css`, `relLum`, `hex2lab`, `groupOf`, `colorOf`, `colorWalk`, `bandOf`, `subTintIndex`, …) whose locals carried `any` into everything downstream; ~40 now have signatures, and `THEME`, `state`, and the two bands have typedefs (`Theme`, `State`, `Band`) declared inside `mountVaultGraph`, next to the object each describes -- internal, so the module-scope block stays the three boundaries. After the pass the range holds **0** findings (the last one was `mixHex`, three sections down, typed by its signature alone). Per rule on the page: 2,643 / 989 / 355 / 252 / 138 → 2,210 / 774 / 310 / 262 / 97; the argument count *rose* by 10 because typed callees now see the `any` their callers hand them, which is the meter doing its job. **Golden layout snapshots: demo and dominant-folder unchanged; the 10k fixture FAILS, and not because of this change.** `893 note(s) moved past 0.1 units, worst is #5296: radius 9317.1 -> 9637.1, angle -80.4° -> 169.1°` -- reproduced byte-for-byte on batch 2's commit (`15318ef`), which had passed the same check at 10:45 the same morning, and on `develop@3aa9401` itself. Between the two runs the shared fixture store was regenerated (`.fixtures/test-vault-4476580a/.stamp.json` now says `day: 2026-09-04`; the previous generation was 2026-08-28, exactly `FIXTURE_MAX_AGE_DAYS`), and the 10k golden is not day-invariant: `--end` defaults to today, so a regenerated 10k vault lays out differently from the one the golden was recorded against. The demo and dominant-folder goldens survive the same regeneration. Found independently the same day by the comment-strip branch (`a2f2733`: "the 10k golden snapshot is not day-invariant, reproduced identically with the generators restored to HEAD"). Not a #60 finding and not fixed here; it needs its own issue -- either a fixed `--end` for the 10k fixture or a golden that is re-recorded with the store. The dict()/JSDoc change is behaviour-identical by diff: every changed line is `Object.create(null)` → `dict()` or a comment. Batch 3b, the rings layout section (861 findings, the largest in the file), is next and has not started. |
| 2026-09-04 | **The page's three boundaries are declared: `src/page.js` 6,467 no-unsafe findings → 4,377, `plugin/main.js` 1 → 0, budget 6,468 → 4,377 (github#60, batch 2).** Before this nothing stated what `mountVaultGraph(root, data, deps)` takes or hands back: two producers emitted `data` with no contract between them, the plugin built the 25-member `deps` by hand, and the `__vg` object was reached into blind. All three are now JSDoc typedefs at module scope in `src/page.js` -- `VaultData` (`VaultNode`, `VaultEdge`, `VaultStats`), `MountDeps`, `VgApi` with `MountHandle` -- and `plugin/main.js` imports them as `import("../src/page.js").X`, dropping its own batch-1 copies. **Graphology and sigma are typed structurally**, as exactly the members the page calls: `GraphLike` (20 members over `NodeAttrs`/`EdgeAttrs`), `SigmaLike` (16, with `CameraLike`, `NodeDisplayData`, `EdgeDisplayData`, `SigmaSettings`, the event payloads) and `RenderingLike`; both bundles are vendored without typings (`vendor/NOTICE.md`) and #58 replaces them, so no dependency was added and the replacement can implement the same interfaces. Measured attribution beforehand: of the page's 3,860 member-access findings, `graph` 127, `renderer` 120, `ctx` 101, `WIN` 60, `deps` 35, `data` 16, across 418 distinct receivers -- the three boundaries are the root of those families, and typing them took 2,090 findings off, a third of the file, without touching a function body. Per rule on the page: 3,860 / 1,365 / 870 / 212 / 160 → 2,643 / 989 / 355 / 252 / 138. **144 of the remaining findings now read "error typed" rather than `any`**: `ctx.*` (a 2D context off a `getContext` on an element the type program sees as `HTMLElement`), `el.style.*` (elements from `querySelector`, typed `Element`), and `c.bandRef` on plan cells whose literal never declared it -- findings before as `any`, findings still, now worded by what the type program knows, and each one names the typedef the interior batches owe (a typed `$()`, a `Cell`). `VgApi` names the 21 members that ship in the plugin and deliberately not the ~70 the standalone adds through `Object.defineProperties` inside the stripped region: no typed consumer reads them (the suite drives them by name over CDP) and naming them would let the plugin claim members it does not have. Two code-shape changes, both forced: `var renderer, neighbourCache = null` split into two statements so `renderer` can carry a type, and `main.js` holds the graphology export as `unknown` before asserting it to `MountDeps["Graph"]` at the hand-over. **Golden layout snapshots unchanged on all three fixtures**; the bundle differs from batch 1's build by those two sites and comments only. |
| 2026-09-04 | **`plugin/main.js` typed with JSDoc: 510 no-unsafe findings → 1, budget 6,977 → 6,468 (github#60, batch 1).** Measured first, read-only through ESLint's `lintText`: one `@param {string}` on `singular` took the file 510 → 503 under today's config (`allowJs`, `checkJs: false`), so the JSDoc route works with no rename and no compile step, and #55's toolchain decision does not gate this. Per rule, 278 / 101 / 99 / 19 / 13 → 0 / 1 / 0 / 0 / 0. Most of the 510 were four values with no type: `this.plugin` in the view and the settings tab (constructor `@param`), `this.handle` initialised to `null` -- which JavaScript widens to `any`, so every `handle.api.*` read was blind -- (`MountHandle` and a `VgApi` typedef of the 19 api members this file reaches), `app` on the five functions taking one, and `this.settings` from `loadData()`'s `Promise<any>`. **Two things the rules taught:** (1) typescript-eslint reads the expression inside a JSDoc cast, not the cast -- `/** @type {string} */ (PAGE_HTML)` at the use site changed nothing, a cast counts only once its value sits in a declared variable; (2) `any` may flow into an `unknown`-typed variable and nowhere else, so `readConfigJson` now resolves to `unknown` and the loaded settings blob is held as `unknown` and merged over `DEFAULTS`. Three code-shape changes, each forced by a type and behaviour-identical: `readFolders` reads its three config fields through one `strField(obj, key)` (same `typeof`/`trim` checks, in one place); the tag flattening is `flatMap` over `unknown[]` instead of `[].concat(...).reduce(...)` on an untyped literal; `readConfigJson` parses into a const before returning. The bundler's `raw:`/`b64:` imports were an unresolved module, an error type every use carried: `plugin/bundler-modules.d.ts` declares them, `tsconfig.json` names it, `eslint.config.mjs` ignores it (the preset's `**/*.ts` scoping ran type-aware rules on it with no parserOptions and aborted the run). `obsidian` declared as a devDependency at 1.12.3, the version already resolved transitively -- the program is unchanged. **Golden layout snapshots unchanged on all three fixtures** (1403 / 10002 / dominant-folder: band unchanged, positions unchanged); `refresh-check.mjs` against the generated demo vault 9/9 in a real Obsidian (1403 → 1404 on Refresh, dates `{frontmatter: 385, filename: 414, stamp: 604, none: 0}`), since `buildData` was touched; the plugin bundle differs from the previous build only by the JSDoc comments esbuild keeps and those three sites. The 1 left: `Graph: graphology.Graph \|\| graphology`, the vendored UMD with no typings -- #60's boundary batch types the constructors with `MountDeps`. |
| 2026-09-04 | **A camera check read `busy()` and the ratio in two round trips, and under the pre-push hook's three concurrent Chromes that gap was long enough to fail a camera that behaved.** `hiding the biggest group auto-fits the camera, but only once it has finished leaving` refused the #55 merge push twice on the dominant-folder vault with every measured number right -- landed 0.6497 against 0.6502 promised, `camAtRest` true -- and only `moved early: true` wrong; the same commit run against that fixture alone passed 72/72. `watchDuringCascade` sampled `__vg.demo.busy()` then, a CDP round trip later, the camera: when the cascade ended and fit()'s 380 ms tween began between the two, a stale `true` paired with a ratio already on its way (the tween moves ~0.0011/ms, so a 10-50 ms gap under load crosses the 0.01 threshold). Both now come from one eval. Thresholds untouched. |
| 2026-09-03 | **The lint is a gate, reads all of our own code, and carries the directory's five `no-unsafe-*` rules as a meter held at 6,977 (github#55, Phase 0).** `npm run lint` had run when someone remembered: 27 warnings, 26 of them values in `src/page.js` assigned and never read, and 40 more once the scope widened from two paths to `plugin src scripts`. All gone. The page.js pass took 133 lines out, including a chain nothing read -- `EXCESS_KEEP`, `dotTyp`, `DOT_TYP_I/O`, `measureDotTyp`, `dotUnits`: every mount sorted every node's size to compute two numbers no code consumed -- and **the golden layout snapshots are byte-identical on all three fixtures** (1403 / 10002 / dominant-folder: band unchanged, positions unchanged). The Obsidian directory's review of 1.9.0 had shown 77 findings against our 28 because it also runs `@typescript-eslint/no-unsafe-{member-access,assignment,call,argument,return}`; measured here they fire **510 on `plugin/main.js`** (the board's figures exactly) and **6,467 on `src/page.js`**, now `warn` under `scripts/lint.mjs --budget 6977`, which fails on any warning outside the meter and on a meter that differs from the budget in either direction (eslint's own `--max-warnings` gates only the total, so meter findings removed without lowering it would have hidden new unused values); the whole run takes 4 s. `tsconfig.json` now names `src/page.js` -- measured to change nothing today, since the plugin's import already put the page in the program, kept because Phase 1's `tsc` reads the same `include`. Gated in `.githooks/pre-push` (develop/main only, unskippable, fails closed without `node_modules`) and in `release.ps1` after the dirty-tree check, out of #10's way. `prefer-setting-definitions` is off in config with its reason, github#59, because the preset forbids disabling `obsidianmd/*` inline. `typescript-eslint` and `globals` are declared at the versions that were already resolved. |
| 2026-09-02 | **An animated frame on a 10k vault costs 30 ms of script instead of 41, because four of its terms had no output (github#19).** The issue attributes 14 fps to the cascade re-planning the whole vault every frame. It does, deliberately (`animation.md`), and that was **not** the largest term — a large share of every frame was work whose only product was discarded. **(1)** Sigma subscribes to the graph, so each of the ~10 000 `mergeNodeAttributes` calls in a bulk position loop emitted `nodeAttributesUpdated`, whose handler runs `updateNode(id)` and then `refresh({partialGraph:{nodes:[id]}})`, which runs `updateNode(id)` **again** — three whole-vault `nodeReducer` passes per frame where the explicit refresh at the bottom of the frame does the only one that survives. `quietWrites` detaches that event's listeners for the length of a bulk write; **measured, a 10 002-note position loop is 5.3–5.6 ms with sigma listening and 0.5–0.6 ms without.** **(2)** `ringsLayout` opened with a `graph.forEachNode` counting visible notes into a local nothing read — the input to the `REPACK_BELOW` threshold `decisions/0001` removed; eslint had been reporting it as unused all along, ~1 ms/frame. **(3)** `buildWedgePlan` ran its two-clause membership test twice per note, once in each of its two walks; the first now records who passed, in graph order, and the second walks that array — same set, same order, because `planTotal` is a running sum of doubles that divides into `density = sqrt(fullTotal/planTotal)`. **(4)** `ringsLayout` computed `willShow(sl.id)` for every note and threw it away: the reader is `(fullRing || !will)` and `fullRing` is true in every non-empty state, so inlining it lets `||` short-circuit; with `pathKey` no longer allocating a throwaway array and string per ancestor, and `visible()` extending one key through the folder chain instead of rebuilding the prefix per level, **`visible()` went from 4.3 ms per frame to 0.7.** Also the two per-note room maps are flattened once per cascade rather than rebuilt per frame (four 10 000-entry `Object.keys` arrays and two 10 000-property dictionaries a frame), which is inside the run-to-run noise on its own and kept for the allocation rather than for a number. **Measured on `.fixtures/test-vault-4476580a`, showing `05 - Meeting Notes` (4 358 notes) from rest, two alternating A/B runs: script per cascade frame 40.4 / 41.1 → 30.3 / 28.8 ms; cascade frames drawn in the same ~2.3 s 57 / 56 → 72 / 75; rAF period p90 50.1 / 50.1 → 33.6 / 33.5 ms; frames over 50 ms 11/56 and 19/56 → 5/74 and 6/78; `buildWedgePlan` at rest 9.6 → 8.0 ms, `ringsLayout` 6.6 → 3.8 ms** (`node scripts/probe-frame.mjs`, new). The suite's own guard sees it: **`a range change animates instead of snapping` on the 10k fixture got 96 frames of a nominal 407 when github#19 was written and gets 250 now.** **The picture is unchanged**: `layout matches its golden snapshot` reports "positions unchanged" on all three fixtures, and `the last frame of a cascade is the resting layout` reports `dr 0 dtan 0 dot 0%` on all three. The `visible()` and `pathKey` rewrites were additionally checked against the old forms over **200 000 random folder/`dirs`/`hiddenSub` shapes (120 000 of them hitting the hidden path) and 1.6 million `pathKey` comparisons, 0 mismatches** — worth doing because **no suite check hides a subfolder**, which is a gap this did not close. And the question github#19 raised and could not answer — why `skipIndexation: true` measured *slower* than a full reindex — is answered in `perf-cascade-frame-cost.md`: without a `partialGraph` it is the identical branch, and even the flag it guards is set anyway by `i || !t`. None of the issue's four routes has been taken; what each would cost is written up there. |
| 2026-09-02 | **Every folder that holds notes keeps its row, its slot and its colour, so nothing renumbers the twelve-slot rotation any more (github#50, resolving the recolour reported as github#48).** Slots are assigned by POSITION in `order[state.dim]`, and `computeOrder` built that list from `Object.keys(count)` — the groups currently holding a member — so a group emptying moved every group behind it one slot along and each inherited the colour of the one in front. **Measured on the dominant-folder fixture, at rest**, turning the membership toggle off: **7 rows → 5** (`gcount` `(7)` → `(5)`), `(vault root)` and `tiny` vanishing — the two folders made entirely of unlinked notes — and **4 of the 7 recoloured**: `misc #d95926 → #3987e5`, `notes #199e70 → #d95926`, `projects #c98500 → #199e70`, `refs #008300 → #c98500`, each taking the colour of the row in front. **After: 7 rows in both states, 0 recoloured, 0 vanished** — and the same on the demo and 10k fixtures (18 groups each). `computeOrder` now seeds the list from where each note is **filed** (its own `folder` attribute) in the same walk that tallies groups, which is the answer github#48's suggested "keep its place in the queue" needed and could not give: a note's folder does not change when its group does, and no filter moves it either, so the list is identical in every state the page can be in. The two membership states now agree on it name for name, which is why the answer is **0 recoloured** rather than merely fewer. A row whose wedge draws nothing reports its count parenthesised — `tiny (6)` — extending the convention `(unlinked)` already used for "a total folded into no folder's own count", and drops its eye and `only` chip, neither of which has anything to act on. **The bracketed form is reserved for a row drawing NOTHING**: a `drawn (held)` form on every row that differs was implemented, measured and rejected on sight — kept separate, every folder on the fixture under-draws (`misc` 25 of 27, `projects` 708 of 738), so it put two numbers on almost every row to reconcile a total nobody had asked about. **Two near-misses worth keeping:** the first measurement was taken 400ms after the toggle and reported `misc` at `#3f85dd`, a blend — github#48's `colorWalk` was still tweening, so a renumbering read as a near-miss rather than a change; and dropping the `only` chip outright moved the count out of `.lg`'s last grid track, so `nav counts share one right edge` measured **2 edges** against the 1 it demands. Both controls now leave an invisible placeholder (`.eye.none`, `.only.none`, as `.tw.none` already did). **And the new check's own first cut was vacuous:** it compared `colorOf` either side of the flip and passed on the unfixed code, because `colorWalk` answers from `colorShown`, which starts at the OLD value for exactly the groups that changed — so for `TWEEN_MS` a renumbering reads as "nothing moved". It reads `autoSlotOf` now, the rotation position itself, which is never animated; verified by disabling the seeding and re-running — **7 groups, 6 disturbed, lost `(vault root)`, `tiny`, renumbered `misc`, `notes`, `projects`, `refs`**. A second check covers the one thing dropping those controls could have stranded: `(unlinked)` at zero is the default state, and its right-click menu is the only way back to its own toggle from the main view. Golden snapshots: band and positions **unchanged** on all three fixtures — a memberless group seats no cell, so it cannot repeat the zero-weight cell that moved the band split on this same fixture. |
| 2026-08-29 | **`minEdgeThickness` 1.7 → 1.0, so the resting web stops fogging the middle of the disc (github#42).** The setting was never set at all, so it sat at sigma's default — while at rest every link's natural width is **0.55–1.02px**. The floor caught **100%** of them and inflated each two to three times. One link 1.2px too wide is invisible; 3737 of them sweeping through the middle of the disc and stacking ink at every crossing is a grey wash that veiled the inner rings and filled the hub hole. Same setting family as github#39 the day before, from the other end: that was strokes too thick when you zoom **in**, this at rest. **Measured on the 10k fixture** — one degree-55 hub's fan, and mean coverage of the edges canvas alone via the new `__vg.edgeInk()` (the web with no note or label mixed in): at 1.7px, fan **93.5px**, web covers **30.49%** of the stage, mean alpha where lit **0.76**, ink **0.231**; at 1.0px, fan **55.0px**, coverage **25.76%**, mean alpha **0.44**, ink **0.114**. **The coverage barely moved and the alpha halved** — that is what fog is, and it is why the stroke widths alone did not describe the defect; the width number was what made this look not worth filing. Inflation of the median natural stroke: **3.06x → 1.80x** (1.79 on the demo shape, 1.80 on dominant-folder). **Flat, not scaled by edge count, and the issue predicted the opposite:** fog grows with the number of links while the hairline risk is worst when there are few, so a curve looked necessary — but at 0.5px the web on a generated 450-note shape all but vanishes (the hairline risk is real) and at **1.0** both that shape and the 10k one read well, so one number does it and the mechanism was not built. That sparse shape is deliberately **not** a suite fixture (`--notes 450 --years 4 --seed 7` regenerates it); the DDR carries the command, since it sets 1.0's lower bound by eye and nothing guards it automatically. **The lit web got thinner too, by 23%, which the plan for this got wrong:** a focused link is sized toward 1.4, i.e. 1.30px at rest — *below* the old 1.7 floor, so the floor had been inflating the hover web as well, and the plan asserted it was "clear of either floor". It now draws its own 1.30px. `checkFocusWeb` reports **0 dim** on all three fixtures either way, which was the real risk: it samples a lit curve's centreline for blue and a thinner stroke antialiases more. **Weight is still encoded nowhere and this did not change that** — the three weights draw 0.556 / 0.787 / 1.019px at rest so 1.0 still flattens the first two, separating them needs **≤0.55**, and that is exactly where the sparse web disappears; the two goals conflict through this one number. Zoomed in it is marginally better (deep-zoom range widens 1.7–2.12px → 1.5–2.12px, the light edges no longer floored up to meet the heavy ones). Scale of the prize, before anyone spends on it: **3 distinct weights in the whole graph, 97–98% of links weight 1** (3628/3737 on 10k, 3219/3288 on demo), and `edgeReducer` overwrites weight entirely when a link is lit (every focused edge sized toward 1.4 — 3.50px at 10x at every floor value). Also recorded: **nothing reaches `EDGE_SIZE_MAX`** — observed max size 1.10 against the 1.6 ceiling, so github#39's 4px cap is never reached and the real deep-zoom maximum is 2.75px. |
| 2026-08-28 | **A link's stroke is now capped in pixels, so zooming in stops burying the notes it joins (github#39, reported by Angel Bartolli / @bartolli, who named the exact cause — "under the identity `zoomToSizeRatioFunction`" — in the issue itself, and offered a patch branch with a working fix and a measured cap).** Sigma's edge shader draws `max(minEdgeThickness, size / sizeRatio)` and `sizeRatio` **is** the camera ratio here, because `zoomToSizeRatioFunction` is identity — so a stroke grew as `1/ratio`, exactly like a dot. `measureSizeScale` already cancels that same `1/ratio` for nodes (`pitch *= cam`); edges never got the equivalent. Measured on the 10k fixture against one hub of degree 55, drawn stroke and the total ink of its fan: **rest (ratio 1.08) 1.70px / 93.50px, 5x (0.216) 3.94px / 153.94px, 10x (0.108) 7.87px / 307.87px** against a 20.44px dot — 307.87px converging on 20.44px is why the fan read as one mass with no link traceable through it. After: **1.70px / 93.50px unchanged at rest, 2.12px / 93.93px at 5x, 2.12px / 93.93px at 10x** — identical at the two deep readings, which is the point. Other shapes: dominant-folder (node 158, degree 103) 3.38px / 184.6px, demo (node 452, degree 71) 2.75px / 130.63px. **One multiplier for the whole web** (`edgeMult = min(1, EDGE_MAX_PX * ratio / EDGE_SIZE_MAX)`, 4 and 1.6) rather than a per-edge `min()` against the cap — which is the form the reporter's own patch branch takes, and `EDGE_MAX_PX = 4` is their number, measured there (4.0px at 10x against a 21px hub dot); only the distribution differs. The `min()` form was carried into the plan first and rejected on prediction, then confirmed by arithmetic — it flattens all 55 onto 4.00px for a 220px fan with the weight ordering gone, while a single `k` holds the weight ratios at any zoom and makes `size * k / ratio` a **constant** below the knee, so the web is invariant under zoom rather than merely bounded. That is why the new check asserts an *equality* and not just `maxPx <= cap`. **Above the knee (`ratio >= 0.4`) it costs nothing at all** — `k` pins at 1, no refresh fires, the resting disc does not move by a pixel; the camera hook is rAF-throttled and a **pan never fires it at any zoom** because a pan does not change the ratio. Cost of the reducer pass it does run, measured not quoted: **14.5ms on the 10k shape against 3.3ms for a render alone**, i.e. ~50fps through a 120ms zoom notch — the historic 30.2ms figure for a refresh predates the lazy-edge budget (36k edges then, 3737 now) and no longer applies. **The bug inside the fix, worth recording because the suite did not catch it:** `capEdge` was first applied once before the reducer's final return, and `edgeReducer`'s query branch **returns early** — so every link went back to growing as `1/ratio` for as long as anything sat in the search box, while every check stayed green. A cap applied at one exit of a function with three is not a cap. The check now reads that state and **fails at 7.87px** without the fix, verified by reverting it. Also: `edgePx(size)` is the shader's law in one place, shared by the focus-web overlay and `__vg.edgeReport()`, for the reason `edgeCurveGeom` is shared with `checkFocusWeb`. **Deliberately not touched here:** `minEdgeThickness`, sigma's default **1.7px** — the same setting family from the other end, fixed the next day under github#42, which is where the corrected account of it lives. What this entry originally said about it was overstated: the framing was "the floor hides link weight, own issue", which survived one measurement (100% of strokes on the floor at rest) and did not survive looking at it. The floor's real cost was **ink, not weight** — 3737 links each inflated two to three times, stacking into a grey fog over the middle of the disc — and the weight it hides is nearly empty anyway: **3 distinct weights in the whole graph, 97–98% of links weight 1**, and the range is 0.60–1.10 rather than the 0.60–0.85 written here. Corrected rather than deleted, because the mistake is the instructive part: counting said "not worth filing" and a screenshot said otherwise. |
| 2026-08-28 | **Hiding or showing a folder now auto-reframes the camera to what's actually on screen, but only while nothing has panned or zoomed it since it last rested (`camAtRest`, github#14).** Originally filed proposing that `r0` (the hub radius) move to hold the hole's SHARE constant when the only deep folder gets hidden; set aside during this same session -- inner-band thickness is deliberately `(geomLock.rOuter - geomLock.r0) * INNER_FILL`, a comment on that exact line recording a past regression ("one row holding 34 notes... three overlapping pairs") from thickness depending on a live number, which is precisely what a moving `r0` would reintroduce mid-cascade. Reworked instead around `fitRatio()`/`fit()`, which already existed and already computed the right ratio (`1.08 * clamp(lastMaxR/geomLock.maxR, 0.12, 1.35)`) but only ran from double-click, the Reset button and Refresh -- never on a filter change, so the geometry was fine by its own logic and the camera simply never reframed to it. Measured on `make-shape-vault.mjs` (954 notes, `projects` at 77%): hiding it drops `reach` to 0.602 and the outer band to zero notes (`shownOuter: 738 -> 0`), and pre-fix the camera stayed at ratio 1.08, framing a disc that had shrunk to a fraction of it -- an island in otherwise empty stage. **The mechanism is one flag plus one guard**: `camAtRest` (new module state, `src/page.js`) starts true (the page loads already fit) and flips false the instant `cam.on("updated", ...)` fires from anything that ISN'T `fit()` itself -- sigma's own drag-to-pan and wheel-zoom, the `zoomBy()` buttons, `centerOn()` -- with no per-source special-casing needed, since all of them already flow through that one listener. `fit()` is guarded with a private `fitting` flag (set before `.animate()`, cleared -- and `camAtRest` restored to true -- in the completion callback on both its branches) so its OWN tween's repeated `"updated"` firings are never mistaken for a user touching the camera. **Direction decides the timing, not just whether to fit** (added after direct feedback: "the zoom in should happen smoothly after all outer ring notes are gone, and zoom out happen while the outer ring notes appear"): inside `cascade()`, right after the destination `ringsLayout()` call updates `lastMaxR` to the DESTINATION's own reach, a shrink (`fitRatio() < current ratio`) sets a cascade-local `deferredAutoFit` flag instead of firing immediately, and `settle()` -- cascade's own completion, once the outgoing notes have actually finished fading, not a fixed delay -- fires `fit()` there instead; a growth fires immediately, alongside the incoming notes' own fade-in. Verified by sampling the camera ratio through the whole cascade: hiding held at 1.0800 for the entire ~1.8s fade of a 738-note toggle, then landed at 0.6497 (target 0.6502) only after settle; showing moved within 25ms of the click and finished zooming out by ~450ms, well inside the same ~1.8s fade. **One real bug found only by testing against the built page, not reasoned from the code**: `densityReport()`'s `reach`/`lastMaxR` are recorded by every `ringsLayout()` call including the per-FRAME ones mid-cascade (same "interpolate the output" mechanism `animation.md` documents for spacing) -- so a value sampled shortly after a toggle reads close to unchanged (reach ~1.00) for most of the fade's own duration and only converges to the true destination once `settle()` re-derives it at rest; the first draft of this fix's own smoke checks sampled too early and silently asserted nothing on 2 of 3 fixtures as a result, fixed by moving the sample to after the cascade (and any deferred fit) has fully landed. **New `__vg.camAtRest` getter** (beside `hoverT`/`hoverBusy`, same "sample it, don't infer it from the reducers" motive) and three new smoke checks (`node scripts/smoke.mjs --only "auto-fit" --only "manually moved camera"`, 9/9 across all three fixtures): hiding the biggest group holds-then-drops, showing one moves-immediately, and a manually-moved camera (via `.animate()`, not a raw `setState()` -- the raw form measured as silently no-op'ing on the 10k fixture once other checks had already been driving the same camera, `.animate()` is the primitive `fit()` itself uses everywhere else and proved reliable) is left untouched by a toggle. Existing camera checks (`"fit frames the disc that is actually there"`, `"dragging the stage pans the camera"`, `"the pan toggle locks the camera and flies home"`, `"the zoom buttons step by one wheel notch"`) re-verified unchanged on all three fixtures -- none of them exercise `cascade()`'s `colToggle` path. `geomLock`, `r0`, `HOLE`, band thickness and `placeLogo` are untouched, as intended; the original issue's `holeShare` smoke-check escape hatch (`reach >= 0.95`) stays as it was, since this fix does not change `holeShare` at all. **Given its own beat, not a new act**: a second `{click: eye}` beat added to the existing `folders` act in `demoMode()` (`src/page.js`) shows the SAME folder hidden and then shown again, so the storyboard demonstrates both directions; verified end-to-end via `node scripts/demo.mjs --act folders` (18/18 beats, no errors) but the act's own clip (`assets/features/folders.webp`) has not been re-recorded yet -- that needs `record-demo.ps1`'s physical-mouse step, done separately. `docs/features/folders.md`, `docs/features/camera.md` (cross-referenced both ways) and the `docs/features.md` gallery's own folders paragraph updated in prose; the gallery image's `alt` text is deliberately left describing the CURRENT clip until the clip itself is re-recorded, so it doesn't claim to show a beat that isn't in it yet. |
| 2026-08-28 | **A hovered note that is also the current selection no longer flicks on mouse-out (`hoverAmount()`, github#38, reported by Angel Bartolli / @bartolli, who also diagnosed the exact cause -- "`hoverAmount()` returns `hoverT` for any hover, including the selected note" -- in the issue itself).** `state.hovered` is set on `enterNode` regardless of whether that note is already `state.selected`, so `hoverAmount()`'s `state.hovered ? hoverT : 1` switched a selected note from its pinned-at-1 branch onto the ramping `hoverT` branch the instant the pointer entered it -- the leave-tween then drove its size and lit web down toward the unselected base value before `state.hovered` cleared at `hoverT === 0` and the pinned branch took back over, reading as a ramp-down-then-snap. Measured on the demo vault before the fix, hovering the selected note (base size 1.9377, selected/full size 2.8096) then moving off: size ramped 1.96 -> 2.81 on entry (as `hoverT` climbed 0 -> 1, redundantly re-tweening a note already fully shown), then 2.79 -> 2.07 across the leave before jumping straight back to 2.8096 the frame `state.hovered` cleared -- reproduced on both the demo and dominant-folder fixtures. Fixed with one line: `return (state.hovered && state.hovered !== state.selected) ? hoverT : 1;` -- hovering a *different*, unselected note is untouched (still ramps via `hoverT`), hovering nothing is untouched (still `1`), only hovering the selected note itself now stays pinned at `1` for the whole enter/leave cycle. Verified the same way post-fix: `hoverT` still ran its full 0 -> ~0.98 -> 0 cycle internally (the rAF loop and its `state.hovered = null` cleanup on landing are untouched), but the sampled node size was byte-identical (min === max, 3.3442792144829285 on the dominant-folder fixture) across every frame of both the hover-in and the leave -- confirmed on 2 of 3 fixture vaults; the 10k vault's click missed its aim target (a harness targeting issue in the throwaway probe, the same "too dense to aim" class the permanent hover check already accounts for, not a defect in the fix). The offered patch linked from the issue (a fork branch, `bartolli/vault-graph@0004-hover-pin-on-the-selected-note`) was read but not pulled in or merged; this fix was derived independently from `src/page.js` and matches the issue's own diagnosis. `node scripts/smoke.mjs --only hover` stayed green throughout (none of the existing hover checks hover a pre-selected note, so none exercised this branch before). |
| 2026-08-27 | **An unlinked note now JOINS its own folder's group by default -- wedge, band, colour, count, filter, all of it -- instead of sitting apart with every other unlinked note (`unlinkedByFolder`, default ON, github#3 reopened).** The original fix under this issue number (see the two entries below) made every unlinked note fall back to the flat `(unlinked)` swatch unconditionally -- correct as the *only* behaviour, wrong as the *only choice*: reopened with a request (via a comment, not the issue title) that started as "make the colour optional" and was expanded, over several rounds of live review the same day, into "actually move it, don't just recolour it." The shipped design is the second one; the first (a per-note colour override that left membership alone) was built, verified, and then removed again once the scope grew -- worth recording since a future reader diffing this area will otherwise wonder why `nodeColor()` briefly had, and no longer has, an explicit `unlinkedByFolder` branch. **The fix lives in exactly one place, `groupOf(id)`** (`src/page.js`): `if (!adj[id]) return unlinkedByFolder ? graph.getNodeAttribute(id, "folder") : UNLINKED;`. Every downstream consumer -- and there are over two dozen call sites, from wedge/band assignment (`buildWedgePlan`, `bandLock`) to visibility, highlighting, search, tooltips and the cascade's own arrival grouping -- already asks `groupOf` and nothing else, with no caller anywhere hardcoding `=== UNLINKED` as a special case (checked by grep before touching it, not assumed). That is what made this a one-function change rather than a sweep: `nodeColor()` itself reverted to reading `groupOf(id) === UNLINKED` with no extra condition, exactly as it read before the original fix, because `groupOf` now already tells the truth. **Toggling it live is a REGROUP, not a repaint** -- membership changing means every group's size changes, which can move the inner/outer band split, which is a different shape of change than a colour override or a hub pin (`hubChanged()`, which only replays the ring around unaffected membership, was the wrong precedent -- pinning excludes a note from the ring plan entirely and never touches `groupOf`). Factored into `hardRelayout(animate)` (`src/page.js`, beside `regroup()`), extracted from `__vg.relayout()`'s existing reset sequence rather than copy-pasted a second time (flagged in review: "the logic should be similar to the hub pinning logic right, don't duplicate code" -- correct that it shouldn't be duplicated, and `hubChanged()` was confirmed to be solving a different, lighter problem before ruling it out as the shared function to call instead): cancel whatever's animating, drop every locked room/cell/edge/band/geom cache, `regroup()` (recomputes counts, colours and, since `bandLock` is now null, the band split itself from scratch), lay out fresh. `setUnlinkedByFolder` and `__vg.relayout()` both call it now; `applyLayout(false)`, not `(true)`, since an animated tween would need to reconcile two plans that disagree about which wedge a note is even in, not just where in the same wedge it sits -- untested territory, deliberately left instant rather than shipped animated and unverified. Reachable two ways, both wired on both hosts: a third optional trailing pair on `openCtxMenu` (`byFolderOn`/`onToggleByFolder`, following the exact shape `visShown`/`onToggleVisible` added for github#34) on the `(unlinked)` row specifically, and a second `OPTION_ROWS` entry in the standalone settings panel plus a matching `Setting().addToggle()` in the plugin's native tab -- default **ON** in both `DEFAULT_SETTINGS` and `deps.unlinkedByFolder === false ? false : true`, the opposite polarity from every other toggle in the file, since this one changes shipped behaviour on purpose rather than only ever improving on it. **The `(unlinked)` row is ALWAYS in the legend, even at zero** -- `computeOrder()` forces `count[UNLINKED]` to exist (defaulting to 0) before `names = Object.keys(count)` runs, rather than earning its row the ordinary way every other group does. First shipped the ordinary way (no row for an empty group, matching every other group's convention) and corrected same day, on direct feedback that pointed at the actual asymmetry: the ordinary rule made the row appear once turning the toggle OFF, but never appear to turn it back ON, so with the default now ON there was no way back to OFF except through settings ("how do you want to enable it again from the main view?"). A permanently-empty group costs nothing in the layout: `buildWedgePlan`'s cell list is built from `liveG`/`liveN`, tallied purely by walking notes and calling `groupOf` on each -- with zero notes ever answering `UNLINKED` while the toggle is on, those maps simply never gain the key, `cellsOf[UNLINKED]` is never written, and `all.filter(g => cellsOf[g])` drops it before any wedge is built, the identical path an ordinary group filtered down to zero already takes. Confirmed live: golden-snapshot check still green with no snapshot regeneration needed (the layout genuinely does not change), and right-clicking the always-present row toggles both directions from the legend alone -- off (13 notes return, row reads "13") and back on (row reads "0") -- with no settings-panel trip required either way. With members it sorts second, right after `_`-prefixed archives (which keep top billing by explicit preference: "archives are just special for my personal preference"); refined again same day (below) to rank last instead while empty, rather than holding that seat permanently. **Its swatch is also no longer in the ordinary twelve-slot colour rotation**: `(unlinked)` now takes the same recessive grey slot archives do (`ARCHIVE_SLOT`, "g11") by default, at explicit request ("don't use the normal colour table for unlinked, make it gray like the default off ones") -- an override still wins, same as an archive's own colour can be overridden, this only changes what *automatic* means for it. Verified live via CDP through the whole matrix: default state (on, 0 unlinked, row present at 0), toggled off through the settings panel and through the row's own right-click menu (`checkPlanParity()` returned `parityOK: true` after the DOM click, not just the API call), a specific note's rendered position and colour confirmed to actually change (not just its bookkeeping) when crossing the toggle, and the grey slot confirmed byte-identical to `palette()`'s own `g11` hex. Two smoke checks rewritten for the new default (`"every unlinked note wears the (unlinked) swatch"` now forces the toggle off before measuring and restores it after, since the behaviour it guards is no longer the default) and renamed to describe what they actually assert (`"...moves unlinked notes into their folder"`, not "...recolours"). **Found in passing and fixed in the same branch, not filed separately:** the github#34 "hidden by default" right-click toggle never persisted on the Obsidian plugin host -- `plugin/main.js` wired no `onFolderShown` deps callback (only the native settings tab wrote that key directly), so the toggle repainted the view live and then silently reverted on the next reload. Fixed with the same two-line writer shape every other view-side setting already uses there. **Also found in passing:** `make-test-vault.mjs` (which `make-demo-vault.mjs` delegates to, and which the 10k fixture is built from directly) produced unlinked notes only incidentally -- the standard demo fixture had run at 0 orphans, meaning every orphan-dependent check was silently skipping ("nothing to measure") on two of the three standard fixtures. Fixed at explicit request ("we need orphans always in the test vaults from now on") with a small fixed quota (`ORPHAN_COUNT = 6`, mirroring `make-shape-vault.mjs`'s existing per-folder `orphans:` counts) of notes excluded from the `titles` pool everything else links from and given zero outgoing links of their own, including the separate `person: "[[Name]]"` frontmatter path meeting-kind notes use -- and excluded from orphan eligibility if the note is itself People/Colleagues/Family/Authors-titled, since `pick(PEOPLE)` draws those names independently of `titles` and could otherwise hand an orphan-marked person note a real incoming link anyway. Confirmed via `check-generator-determinism.mjs` that reproducibility still holds. This shifted the demo and 10k fixtures' PRNG stream, breaking their golden layout snapshots from github#37 (below) in the expected way; the default-on membership change then broke all THREE fixtures' snapshots a second time the same day. Both regenerated via `scripts/update-layout-snapshots.mjs`. Full `node scripts/smoke.mjs`, run twice (once per snapshot break), green on all three fixtures both times. **Refined twice more the same day, both from direct live feedback.** First: an always-present `(unlinked)` row leaking into unrelated code -- `"highlighting ramps per note and is additive"` picked its two test groups off `groupOrder()[0]`/`[1]` unconditionally, which used to be safe because only non-empty groups ever appeared in that list; with `(unlinked)` now permanently present and usually empty, `pick(gs[0])` returned `null` and `__vg.hl[null]` read 0 by construction on all three fixtures, a false failure rather than a real one. Fixed by filtering `groupOrder()` to `groupCount(g) > 0` before picking, restoring the check's own pre-existing "say so rather than measure nothing" discipline (grep confirmed no OTHER call site in either `page.js` or `smoke.mjs` indexes `groupOrder()` positionally, so this was the only casualty). Second: the row's own RANK now depends on its count, not fixed -- with a member it keeps the second-place seat right after archives; at zero it drops to LAST, past every real folder, and its row gets a new `lgr-empty` CSS class (55% opacity, full on hover/focus) so a dormant control reads as dormant rather than as a folder that happens to hold nothing. Right-click still works from wherever it lands. Costs nothing in the wedge geometry either way, same reasoning as the always-present fix above: a zero-weight group never earns a `cellsOf` entry regardless of its position in `order[dim]`, confirmed again by an unchanged golden-snapshot pass with no regeneration needed. Full suite green on all three fixtures after both refinements. **Reordered a third time, same day, on further live feedback**: both pseudo-groups now sort at the very end of the legend instead of near the top, `(vault root)` then `(unlinked)` last of all -- explicit precedent cited for the change ("vault root is also last in the original obsidian navigation"), and `(unlinked)`'s conditional rank (second with members, last while empty) was dropped in favour of always-last regardless of count, so the toggle is found in the same place every time rather than one that relocates under a user mid-session. Real folders now rank ahead of both pseudo-groups (previously behind), which reorders the automatic twelve-slot colour rotation too -- colour is positional, not identity-locked, so this is an accepted side effect, not a regression. **This is also the first time in the ticket a rank change moved the LAYOUT, not just the legend**: `order[state.dim]` drives wedge ANGULAR POSITION as well as display order, something a purely-additive change (an empty group costing nothing) had not exercised before -- confirmed by the golden-snapshot check failing on all three fixtures with radius unchanged and only angle differing (up to several degrees, hundreds of notes per fixture), a real and expected rearrangement rather than a bug. Regenerated via `scripts/update-layout-snapshots.mjs`; full suite green again after. **Half-reverted the same day, on direct feedback that caught a real regression**: moving `(vault root)` behind every real folder (above) shifted the automatic twelve-slot colour of every real folder in every existing vault on upgrade -- a far bigger blast radius than tidying two pseudo-group rows should have. `(vault root)` moved back to its pre-ticket rank, right after archives and ahead of every real folder; only `(unlinked)` still sorts last, unconditionally now rather than with-members-second/empty-last, which costs nothing in colour since it already takes the recessive grey slot rather than consuming a rotation position. Verified via a fresh golden-snapshot regeneration (angle changed again, moving most of the way back toward the pre-reorder values) and a live check that `(unlinked)` alone moved while every real folder's `slotOf()` returned to what it was before any of today's reordering. **Re-read the original reopen comment closely and found a second ask this ticket had silently dropped when the scope moved to full membership**: "giving them a mixed color dot in the navigation ... possible from the right click color menu for the unlinked group only" describes COLOUR ONLY, independent of "make unlinked optional completely ... to have those notes just as part of their folders" (the membership/move ask, in the same comment, hedged with "maybe" and "completely" as the bigger of the two). The shipped `unlinkedByFolder` covers the second half; the first half -- a note that STAYS in the `(unlinked)` group but still takes its own folder's tint -- had no toggle. Restored as `unlinkedTintByFolder` (default off), the near-verbatim original colour-only implementation from earlier the same day (`nodeColor()`'s `groupOf(id) === UNLINKED && !unlinkedTintByFolder` gate, `buildUnlinkedTint()`/`unlinkedTintColors`, the conic-gradient `swatchFill`/`swatchTitle` pair) reinstated as an ADDITIONAL toggle rather than the replacement it briefly was -- only meaningful, and only offered in the right-click menu, while `unlinkedByFolder` is off (nothing left in the group to recolour once every unlinked note has joined its folder). Also added on the same re-read: the `(unlinked)` row's own count reads `(13)` rather than `13` while kept separate, since -- unlike every other row's count, which is exactly that folder's own total -- this one counts a population no folder's own total includes. Two new smoke checks (`"...tint toggle recolours notes without moving them"`, explicitly asserting the count is unchanged after the colour flips, and `"...count is parenthesised..."`) plus a new settings-panel id-drift check for the third `OPTION_ROWS` entry. Full suite green on all three fixtures after both fixes. **Given its own gallery entry and demo beat the same day**, at explicit request: `docs/features/unlinked.md` split out of `docs/features/colours.md` (which now just cross-links it) rather than staying a paragraph inside an existing page, matching how compactaxis and subfoldercolor earned their own entries before it. A new `act: "unlinked"` in `demoMode()` runs LAST of all, after "colours" -- both toggles here touch colour (the tint half) and membership (the join half moves notes between wedges), inheriting "runs last" from colours and adding a reason colours never had: nothing later needs a clean starting membership, because nothing runs after this. Two new `demoFind` kinds, `ctxbyfolder`/`ctxtint`, mirror `ctxvis` exactly; the right-click target itself needed no new kind at all, since `demoGroup("(unlinked)")`'s existing prefix match already resolves the literal pseudo-group name, and the row is now guaranteed present on every fixture (see the always-in-the-legend fix above) so the beat needs no vault-specific setup the way subfoldercolor's twisty-opening does. Verified beat-by-beat via `scripts/demo.mjs --act unlinked` (real CDP input, not `el.click()`) before recording for real -- all 18 beats resolved and landed clean on the first attempt. Recorded both the per-feature clip (`assets/features/unlinked.webp`) and a fresh full hero (`assets/demo.webp`, 102 beats now, up from 84) against the synthetic demo vault, never the real one -- `record-demo.ps1`'s own default. Full `node scripts/smoke.mjs` green on all three fixtures once more after the storyboard change, and `check-pii`/`check-scope`/`check-network` clean. |
| 2026-08-27 | **New golden-snapshot regression check, `"layout matches its golden snapshot"` (github#37).** Every existing check asserts a *property* of the layout; none would catch a layout that stays internally consistent and simply *changes* from one build to the next -- exactly what github#35 turned up as a side observation (the same folder split differently across rebuilds with no intentional change, cause never isolated, later traced to an in-progress reverted fix rather than the fixtures). New `scripts/update-layout-snapshots.mjs` writes `scripts/layout-snapshots/{demo,test,shape}-vault.json` -- band assignment per folder, `(x, y)` per note, deliberately by hand, never automatically. The check compares a live build against the checked-in snapshot for that fixture, failing with the specific folder that flipped band or the specific note id and its radius/angle delta, not just "changed." Verified the premise first: built the demo and shape vaults twice each, 3.5 years apart in `--end` (the same technique `check-generator-determinism.mjs` uses, extended from folder counts to the actual computed layout) -- band and every note's `(x, y)` identical to the full float64, both vaults, both dates, so a snapshot taken today does not go stale on the fixture store's own weekly `--end` refresh. Hit, then fixed, the same class of bug github#21 already named: reading positions straight off `demo.busy() === false` (even with a bare `applyLayout(false)`, once or twice) left two consecutive measurements of the IDENTICAL build disagreeing by several graph units on 90%+ of the demo and 10k vaults' notes -- a still-running cascade's next animation frame landing after the snap, exactly what `.ai-context/invariants.md`'s github#21 section already recommends `__vg.relayout()` for. Switched both the writer and the check to `__vg.relayout()` (cancels the in-flight frame, clears `roomNow`/`cellNow`/`edgeNow`/`bandLock`/`geomLock`, rebuilds) -- repeated measurements became byte-for-byte identical, confirmed across multiple runs on all three vaults. Also switched the writer from an independently-generated private vault to the SAME shared `.fixtures/<name>-<digest>` store `resolveVaults()` uses, so the snapshot and the check are guaranteed to read identical underlying vault content rather than two separately-generated (if supposedly-identical) copies. Confirmed non-vacuous: temporarily changed `INNER_SCALE` 0.8 -> 0.75 and reran without regenerating the snapshot -- failed on all three vaults, naming the exact folder that flipped band on the demo vault (`04 - Daily Notes: outer -> inner`) and every moved note's id and delta on the other two; reverted immediately after. `.ai-context/invariants.md`'s "A vault's layout matches its golden snapshot" section carries the full account. |
| 2026-08-27 | **Row 0 of the inner band is capped against the hub's own radius, closing the dot-sizing half of github#35.** The hub-boundary-position half (`inHubHole`/`placeHubDrop`/`placeLogo` testing the wrong radius) shipped in 1.8.0; this is the other half, left open because a first attempt regressed. `placeCell` puts row 0 of the inner band at `r0 * INNER_SCALE` exactly -- the same circle the hub boundary is drawn at -- for every inner band, always; normally invisible since a healthy dot is a small fraction of that radius. Soloing a folder down to a single note that lands alone in the inner band collapses the band to that one row, so `spInner = thickI / rows` becomes the band's WHOLE thickness (measured: 573 units) instead of a normal row's slice, and `rampFor` sizes the dot off that pitch -- the ramp balloons, not the room/pitch shrink factor (measured `f = room/pit` at 1.02, barely above 1, on the reported repro; that was the first, wrong guess). A band-wide cap on `room` (the `sqrt(full/now)` density ratio `bandDensity()` already uses for row spacing) was tried and **reverted same day**: it fixed the reported case but broke `"filtered to the bone, the disc stays drawable"` on two other fixture vaults -- ordinary aggressive date filtering also produces sparse bands, unrelated to the hub, and a band-wide cap can't tell the two apart. Measured bisecting it: dominant-folder vault `range last 2.5%` diameter/step 0.07 against a 0.15 floor, 10k vault `range last 0.5%` at 0.12, both passing at 0.33 and 0.2+ with the cap reverted. **Fixed instead** with a new PER-NOTE cap scoped to row 0 of the inner band only (`HUB_ROW0_FRAC`, `src/page.js`), following the existing `edgeCap` pattern (a hard geometric bound computed once during placement, consumed alongside `edgeCap`'s own cap at the end of `dotPx`) rather than touching `room`/`sp`/the ramp -- it cannot affect any other row or band, so it cannot repeat the regression. `HUB_ROW0_FRAC = 0.08`: measured a healthy row-0 dot's own radius as a fraction of `r0 * INNER_SCALE * UNIT` on the demo vault at rest, 4.2% (36 of 851 units, the biggest dot in a 6-row inner band) -- doubled for margin. Verified live: soloing `01 - Projects` (the originally reported real-vault repro, 5 notes) no longer overflows the hub; on the demo vault, forcing a single note alone into the inner band (varies by folder and by the vault's own day-anchored generation, since note weight decides how far the uncapped size would have gone) now caps at exactly 68 of 851 units, 8.0%, matching `HUB_ROW0_FRAC` precisely, against 505 (59%) and 81 (9.5%) uncapped on two different folders that hit the same shape before this fix. `scripts/smoke.mjs`'s `"filtered to the bone"` stayed green throughout (unlike the reverted attempt), and a new check, `"a soloed hub-adjacent note stays inside the hub's own radius"`, tries every folder in turn and asserts the worst result stays under 15% -- the shape none of the three fixtures' resting state hit, which is how this shipped unnoticed the first time. |
| 2026-08-26 | **`settle()` now refreshes dot SIZE, not just position, closing github#21.** `dotPx()` -- called by the Sigma node reducer on every paint -- reads `bandOf().room`/`cellRoom`/`edgeCap` as plain persistent globals, and those are side effects of whichever `ringsLayout()` call ran most recently. For the ~90 frames of a cascade that was the frame loop's own calls, walking `roomNow`/`cellNow`/`edgeNow` from the source packing toward the destination's *endpoint capture* (`roomOf()` over a separately-built `staticPlan`) -- a different computation from the one `finalPos` (what `settle()` actually assigns for POSITION) came from. Once the loop stopped nothing ever re-derived the correct figure, so every dot rendered at whatever the last animated frame's walked room happened to be, until an unrelated relayout (a resize, a fit, the next toggle) silently corrected it by accident. Measured live on the demo vault: `05 - Meeting Notes / 2025-09-19 Vendor call` rendered at 26.5px right after a range-change cascade settled, 68.1px after nothing but a bare `__vg.ringsLayout()` call with no position change -- 157% off, position exactly right throughout (`dr`/`dtan` both 0). 211 of 213 sampled notes were off by more than 5%; a folder toggle showed the same defect smaller (152 of 689, worst 11.6%). Invisible to the existing "last frame of a cascade is the resting layout" check (`scripts/smoke.mjs`) because it compares the last ANIMATED frame against rest and both read the identical stale globals -- confirmed by reverting the fix and re-running that check, which still reported `dot 0%` clean. **Fixed** by having `settle()` call `ringsLayout()` again after `assignPositions`, discarding its position output (reusing it would reintroduce the "two plans disagree on seats" 1517-unit jump the adjacent comment already documents fixing once) and keeping only the room/cellRoom/edgeCap side effects. **Once was not enough**: room and position are a fixed point (same reason `finalPos` itself is computed twice, "TWICE, and the first one is thrown away"), and a single settle-time call still measures its margins against the stale room the frame loop left behind -- measured, one call alone left 58 of 213 notes over 5% (worst 39.1%), a second immediate call converged all of them to 0%, a third changed nothing. Also corrects the original github#21 writeup, which credited a settle-time refresh already shipped for folder toggles to branch `fix/small-folder-animation` -- that branch turned out to be an unrelated, stale (~30 commits behind, two reverts at its tip) wedge-seam-overlay experiment, and no such fix existed on `develop` before this entry. `.ai-context/invariants.md`'s "A settled dot is the SAME size a fresh relayout gives it, not just the same position" section carries the full account. |
| 2026-08-26 | **`walk()`'s directory listing is now sorted, closing github#32.** The generator-determinism check (github#33) ruled out the synthetic fixtures as the cause of a band-assignment discrepancy, but `walk()` in `src/build-graph.mjs` was still reading each directory with bare `readdirSync(dir)` — Node documents its order as filesystem-dependent, not a contract. That order flows straight through, unsorted, all the way to the layout: `walk()`'s return becomes `files`, which becomes `notes` in the same order (the per-file loop is a plain `for...of`, no sort in between), which becomes graph node insertion order (`graph.addNode` in file order), which becomes the group iteration order `balanceBands()` searches over in `src/page.js`. That search is exhaustive over which folders go inner vs outer and picks by `cm < bestCost` — strict less-than — so two candidate splits that tie on cost keep whichever the mask loop reached first, and the mask loop's order comes from `movable`, which comes from `names`, which comes from iterating `cells` in the same file-derived order. A tie that used to resolve on unspecified disk order could therefore pick a different inner/outer split for the exact same vault content between two otherwise-identical builds — the github#32 report (`04 - Daily Notes`, 4 sub-wedge cells, `inner: true, rows: 6` in one build and `inner: false, rows: 9` in another, same demo-vault directory, same command). Fixed with one line — `readdirSync(dir).sort()` — plus a comment at the call site tracing the chain, since nothing about `readdirSync`'s contract says this without it. New `scripts/check-build-order-determinism.mjs` guards it two ways, because the dynamic probe alone is not adversarial on every filesystem: on this NTFS checkout, `readdirSync` already comes back alphabetical even for directories populated in shuffled creation order, so a build with the `.sort()` removed still passes a same-vault-different-creation-order probe here — confirmed by reverting the fix locally and re-running the check, which still reported clean. So the check also does a static read of `walk()`'s own source (same idea as `check-scope.mjs`/`check-network.mjs`) asserting the `readdirSync(...).sort(` call is still there; reverting the fix and re-running with *that* half in place failed immediately with "walk() no longer sorts its readdirSync(dir) result". Wired into `.githooks/pre-push` alongside the generator-determinism check, no skip flag, same "cheap enough there's no reason to skip" bar (two tiny subprocess builds, no Chrome, well under a second). Verified against all three fixtures post-fix via `smoke.mjs --only lattice --only "row depth"`: demo vault inner 6 / outer 9 rows, 10k vault inner 15 / outer 23, dominant-folder vault inner 5 / outer 7 — all passing, band assignment unchanged from before the fix on every fixture that had a stable split to begin with. `.ai-context/invariants.md`'s "Build order does not affect the band split" section carries the full account. |
| 2026-08-26 | **Per-subfolder sub-wedge row-depth gate, unconditional (github#31).** `splitFor(g)` already refuses to split a folder into sub-wedges when its TOTAL note count can't fill the band's rows — an aggregate, folder-level test that says nothing about each individual resulting subfolder cell once a folder does split. Found by reviewing the demo vault after github#23 shipped: `04 - Daily Notes` (50 notes, 16 month subfolders) clears the folder-level bar as a whole, then 3 of its 4 resulting sub-cells sit below the outer band's own row depth — `"0:6/9*", "1:6/9*", "2:4/9*"` against a healthy `"3:34/9"` pooled tail — measured via `__vg.buildWedgePlan(false)`, while every other split folder on that vault (`01 - Projects`, `02 - Areas`, `03 - Resources`, `05 - Meeting Notes`, `06 - Zettelkasten`, `07 - Weekly Reviews`, `08 - Archive`, `15 - Courses`) had zero sparse cells. Each sparse cell independently triggers `placeCell`'s deliberate one-note-per-row spread (the right call for one genuinely sparse cell; visually odd repeated three times side by side in the same folder). New `subCellIndex(folder, sub, n, depth)` sits beside `subTintIndex`, not inside it: below `depth` (the caller's own already-computed `bandDepth[bk]`) a subfolder's notes redirect to the pooled-tail slot instead of its own rank; at or above, unchanged. Kept as a separate function specifically because `subTintIndex` also drives a subfolder's COLOUR (`buildSubShades`) — folding the row-depth check into it would have shifted a subfolder's tint whenever its geometric slot changed, an unwanted coupling this entry deliberately avoids. **`n` was, for one review cycle, a lookup this function made itself — `subCount`, a whole-vault tally built once at load and never refreshed — and an adversarial review caught it before it shipped.** Under a filter, a subfolder large in the whole vault but with almost nothing currently visible would still clear the threshold on its stale total, reproducing the exact sparse-cell defect this feature exists to remove — the identical bug class `buildWedgePlan`'s own comment already documents fixing for the FOLDER-level gate ("it also cannot see a filter"), just not carried over to this new per-subfolder one. Fixed the same way: a new `liveSub` map built in the same cascade-stable, filter-aware loop that already produces `liveG`/`liveN`, keyed by `folder + "/" + sub`; the call site passes `liveSub[...] || 0` in, and `subCellIndex` makes no count lookup of its own. New check *no non-tail split cell holds fewer notes than its band's row depth* drives `__vg.buildWedgePlan(false)` directly and is deliberately vault-agnostic — it asserts the general rule rather than hardcoding which folder is expected to be sparse, so an even vault with nothing to gate is a legitimate pass. Measured on the demo vault: 42 cells at rest, 24 non-tail split, none sparse (down from the 3 sparse cells measured pre-fix). A second new check, *the row-depth gate reads LIVE counts, not the whole-vault tally, under a filter*, drives `buildWedgePlan(true)` (not `(false)`, which every other check here uses) under a window sized to the fixture's own first 20% of history (`dateSpan.lo`/`hi`, not a fixed date) — a first cut reused the fixed two-day window the pin-and-filter check uses and reached zero split cells on any fixture, a check that cannot fail being no check at all, so it was widened until it wasn't. Confirmed non-vacuous by reintroducing the fixed bug: 2 of 3 fixtures caught it immediately (`01 - Projects` sub-cells reported at 1/2 and 5/6), the third (dominant-folder, only 6 live cells under this window) reports "vacuous on this fixture" honestly rather than a silent pass. Also added a read-only `__vg.subTailRank` (= `SUB_SLOTS - 1`) so both checks derive the pooled-tail rank instead of hardcoding it. **Shipped as a dev-only toggle first (`__vg.setSubwedgeGate`, off by default, no settings row) and the toggle is gone — the right call at the time, since this started as a hypothesis about a look rather than a confirmed fix, and the wrong call to still be carrying by the end of the same day's testing.** Live A/B rounds found the merge itself correct every time; what they also found, one round at a time, was three real bugs that existed only because the toggle needed to recompute and re-render LIVE, mid-session: a cascade race (flip the gate mid-intro, the plan updates correctly but the still-running intro cascade's own next frame overwrites the rendered positions with its stale target — measured **7** angular clusters instead of **1** at 200ms into the ~5.6s intro, fixed at every timing after `cancelCascade()`), a stuck timeline (the same interruption skipped the intro's own completion cleanup, `endSweep()`, leaving the date-strip handle and tooltip parked wherever the cut-off frame left them — fixed by also calling `stopPlay()`, which `cascade()` itself already calls at its own start and which `setSubwedgeGate` had not been), and an animation that only ever jumped (`applyLayout(false)` is an instant, unanimated snap — right for something meant to give console-fast feedback, wrong for something a person is watching happen; tried switching to `applyLayout(true)`'s 380ms tween, which fixed the jump). All three were real, all three were fixed, and all three stopped mattering the moment the gate became unconditional: with no runtime flip, the very first and only cascade of a normal session already computes the correct, merged layout from the start, so there is nothing left to race, no completion cleanup to skip, and no live re-render to animate. `relayout()` picked up the same `cancelCascade()`/`stopPlay()` treatment along the way (identical shape, identical latent gap, at explicit request) and was reverted back to its original form along with everything else once baking the gate in removed the need for any of it — a real, general finding about that function (and any future caller of the same reset-and-snap shape) that is now simply unexercised by this ticket rather than fixed by it; worth its own pass if `relayout()` is ever driven live again. `.ai-context/invariants.md`'s "A sub-wedge only earns its own slot if it can fill one" section carries the full account. |
| 2026-08-26 | **New gated check, `scripts/check-generator-determinism.mjs` (github#33), wired into `.githooks/pre-push` alongside PII/scope/network (no skip flag).** Prompted by chasing github#31/#32: a band-assignment discrepancy turned out to have nothing to do with `make-demo-vault.mjs`/`make-shape-vault.mjs` defaulting their `--end` date to today, but establishing that took re-deriving it from the source by hand rather than reading it off an existing guarantee. The check runs each generator twice, `--end` years apart (`2024-02-10` vs `2027-09-28` — different year, month, and quarter) on the same seed, and diffs the resulting folder/subfolder note-count trees. Confirmed clean as-is: `make-test-vault.mjs`'s per-note subfolder pick (`pick(f.paths)`) is a seeded-PRNG draw over a fixed, hardcoded label list (`YM(18)`/`YQ(n)`, anchored to a constant base year, never reading `END`), and `make-shape-vault.mjs`'s `subFor` is an index-share split with no date or PRNG involved at all — only the *calendar date string* embedded in a note's frontmatter/file-stamp moves with `--end`, never which folder it's filed under. Measured: 71 folders / 531 notes (`make-demo-vault.mjs`) and 11 folders / 954 notes (`make-shape-vault.mjs`), byte-identical at both `--end` dates. Confirmed the check actually catches a regression, not just a property that happens to hold today: a temporary probe (`+ new Date(END).getUTCMonth() % 2` added to `make-shape-vault.mjs`'s subfolder accumulator) was caught immediately — 7 of 11 folders off by one note between the two dates — then reverted. Deliberately excludes `make-mirror-vault.mjs`, which reproduces a real vault's own structure and dates rather than generating synthetic ones, so day-dependence isn't a question that applies to it. `.ai-context/invariants.md`'s "A synthetic vault's folder/subfolder note counts do not depend on which day it was built" section carries the full account. |
| 2026-08-25 | **CORRECTION to the row below: the real-duration weighting it shipped was superseded the same day, by actually looking at it against the author's real vault rather than only the three synthetic fixtures.** The row below's design (a populated month keeps its own real ms duration, only literally-empty runs collapse) was an exact, provable no-op whenever nothing was empty — and that hid the actual defect, because "no month is literally empty" said nothing about how BUSY a month was. Measured live: **2023 (21 notes, thin enough to touch nearly every month) drew 172px against 2026 (399 notes, the vault's busiest year by far) at only 114px** — 2023 read as "every month populated" and kept full real-time weight throughout eleven months, while 2026 is only partway through its own calendar year. None of the three synthetic fixtures could have caught this: it needs a vault where note density and month-coverage disagree, which a generator tuned for "sparse vs dense YEARS" never produces on its own. **Replaced with note-count weighting, not calendar-time weighting.** Each YEAR gets a width between `YEAR_FLOOR_MS` (~1 month) and `YEAR_CEIL_MS` (~1 real year), scaled by its own note count against `yearRef` — the same p90-with-floor shape `nRef` already uses for bar height, reused here rather than invented fresh. After: 2026 draws 408px against 2023's 90px. A first pass also weighted the MONTHS within a year by their own note count; asked to make them equal-width instead during the same review — "month-to-month noise inside one year isn't a signal worth spending width on" — and that's what shipped: every month in a year gets an identical share of that year's own width. Two more defects surfaced only by looking, neither visible in any automated check: (1) `buildYears()`'s decision to skip alternating year labels was estimated from a vault-wide average (`totalWidth * 365.25days / totalSpan`), accurate only on a linear axis — once a cluster of sparse years can sit far closer together than that average while one busy year eats the rest of the strip, the average never notices the local squeeze, and year chips rendered overlapping while the check that should have skipped them read "plenty of room". Fixed to measure the real minimum gap between consecutive years' actual rendered positions and skip below ~28px (a chip's own approximate width) — confirmed clean at 260px and 1600px alike, where the old estimate had reported "fine" at both. (2) Driving the actual Obsidian plugin under CDP (`scripts/spike-check.mjs`, against a `make-mirror-vault.mjs` copy so no real vault content left the machine) measured year-chip buttons at **30px tall inside a 16px row** — `line-height`, `font-size` and `box-sizing` all read correctly as declared, meaning Obsidian's own theme CSS was contributing vertical padding the button's own padding shorthand did not survive against. Invisible in every one of this ticket's Chrome-only checks, since the standalone page has no competing `button` CSS to lose that fight to. Fixed with an explicit `height: 14px`, authoritative under `box-sizing: border-box` regardless of what else contributes padding. **A last request from the same review: a view-level toggle, since the plugin's gear opens Obsidian's own settings tab rather than an in-view panel, and "we cannot record the settings" from inside the graph itself otherwise.** A small icon button (`#vg-compact`, converging-chevron glyph) now sits left of the date-range fields at the top-right of the timeline row, mirroring `#vg-pan`'s corner-button shape exactly — a mode, `aria-pressed`, persists on click. Its addition reversed an earlier call in this same entry's history: `compactAxis`'s plugin-side dep was made read-only on the reasoning that "no in-view control exists on that host, only the settings-tab toggle" — true when it was written, false the moment this button shipped, so `onCompactAxis` was added back to `plugin/main.js`'s deps, matching `onPanEnabled` exactly. Verified against the real plugin under CDP a second time, not assumed from the standalone: rendered at 22px tall (no overflow), clicking flips `aria-pressed` and `__vg.compactAxis` and does not overlap the date fields (14px clearance). `.ai-context/invariants.md`'s "The date axis weighs years by note count" section carries the full account; this row exists so the row below reads as superseded rather than as what shipped. |
| 2026-08-25 | **The date axis compacts a sparse vault's empty runs, gated on `compactAxis` (default on, github#23).** On the author's own 457-note vault, 97 of 138 months (70%) hold zero notes and five whole years took 43.5% of the strip to represent seven notes. `buildDateSpan` now builds a segment table alongside the dense month list: a populated month keeps its own real duration as its weight, a maximal run of consecutive zero-note months collapses into one segment weighing one *average* month (30.436875 days) regardless of how many real months it spans. `ribbonX`/`ribbonMs` dispatch between the untouched linear formula and a new compact one that maps through this table; every existing caller (year chips, brush, window pill, tooltip, sweep) inherits it for free, since none of them reimplement the mapping — confirmed by reading every call site rather than assumed. `drawRibbon`'s bar layout, which used an index-based uniform pitch rather than `ribbonX` itself, was switched to iterate the same segment table so the bars agree with the control reading them; a collapsed run paints a dim marker rather than reusing the ordinary (invisible-when-empty) bar treatment, so the axis *shows* it is compacting rather than narrowing silently. **A flat weight of 1 per month was tried first and was wrong**, not merely imprecise: with zero gaps every segment is still exactly one month wide, which is a uniform axis, not the genuinely time-proportional one the linear formula computes — measured 0.56–4.45px drift on all three standard fixtures even with nothing to collapse. Weighting a month by its own real ms duration makes the sums telescope back to `dateSpan.lo`/`.hi` exactly, so "no gaps" now reduces to the linear formula byte-for-byte: **0px** delta across a dozen samples on all three fixtures, down from 0.56–4.45px. A hand-built 4-month gap (a scratch copy of a generated vault with three years of notes force-relocated out of a window) measured **8.2px compacted against 31.7px linear — 74% narrower**, confirming the collapse itself works. **None of the three standard fixtures contain a real empty-month run** — the 10k vault's own comment claims "one year holding none at all" but that describes the *real* vault it was modelled on, not the synthetic 10,000-note/10-year output, which lands a note in every month by chance (`hasGap: false`, measured directly on all three). The two new `smoke.mjs` checks read this from the live vault and report a skip rather than a false pass, so the gap-collapsing path itself is exercised only by the hand-built vault above, not by the committed suite — flagged in `invariants.md` as a fixture gap for a future `make-shape-vault.mjs`-style addition, not fixed here since it touches the shared, digest-invalidated fixture store rather than only this ticket. Settings plumbing mirrors the existing `panEnabled` precedent on both hosts: a `DEFAULTS.compactAxis`/View-heading toggle in `plugin/main.js` (read-only dep on that host — unlike pan, nothing in the view itself writes it back, only the settings-tab toggle, which pushes live via a new `setCompactAxis` API method), and a `localStorage`-persisted dep plus a new generic options row in the standalone's `#vg-settings` panel (reusing the existing `.mini button[aria-pressed]` convention, so no new CSS was needed). `scripts/smoke.mjs`'s `xOfMs` test helper, which reimplemented the linear ribbonX formula independently instead of calling `__vg.ribbonXOf`, was fixed to delegate — it would otherwise have silently computed the pre-compaction position on any check run against a vault with compaction engaged. **An adversarial review pass caught a second real defect the manual verification above had not exercised**: the new options row's rendered button id was a literal `"opt-compactAxis"`, but `$()` prepends `"vg-"` to every lookup (`src/page.js:103`) — the same convention the pre-existing `setPan`/`#vg-pan` pair already follows, correctly, for a *static* template id. `setCompactAxis`'s own `$("opt-compactAxis")` therefore never resolved, and the button's `aria-pressed` sync was silently dead code — masked because the only path that fires today (a click) always calls `buildOptions()` right after, which rebuilds the row wholesale from live state regardless. Fixed by rendering `id="vg-opt-<key>"` to match what `$()` actually looks up. A new check drives the real DOM — opens the gear, clicks the row, reads `aria-pressed` and `__vg.compactAxis` back — rather than only the API surface the earlier two checks used; confirmed to fail with the bug's id reintroduced (`"gear opened but #vg-opt-compactAxis was not found"`) and pass with the fix, so it is a genuine regression guard rather than one that would have passed either way. |
| 2026-08-25 | **The focus web is drawn above the dim notes (github#2).** Sigma paints every edge on its bottom layer and every node above that, so the edges lit by a hover or a click ran under the notes they crossed and each dim disc cut a grey gap out of a blue line -- on a well-connected hub the web read as dashed. `drawFocusWeb` strokes every edge with both ends in the focus set (neighbour-to-neighbour included -- the edge reducer lights those too) once more on the hovers canvas, above the nodes layer, with the curve program's own geometry, then re-draws the focus neighbours' discs over it, and the label pill is painted last: dim notes < web < lit notes < pill, alpha riding the hover ramp so nothing pops. (Marking the neighbours `highlighted` would lift them onto `hoverNodes`, above the pill too -- in the plugin the lit discs covered the focus note's name; halo-typed neighbours are left alone so their ring isn't flattened.) The approach follows the diagnosis and geometry already diffed on the fork branch linked from the issue (`bartolli/vault-graph@21a618c`), reimplemented against this file's current state. Measured across the three vault shapes `scripts/smoke.mjs` builds, sampling every lit curve at 1% steps and keeping the samples that fall inside a non-focus disc: the demo vault (node 452, degree 71, 364 in-disc samples) **107 dim before, 0 after**; the 10k synthetic vault (node 1192, degree 54, 152 in-disc samples) **36 before, 0 after**; the dominant-folder vault (node 157, degree 103, 1259 in-disc samples) **530 before, 0 after** -- confirmed by temporarily flipping the fix off and re-running the same harness, which failed with exactly those `dimAtGaps`. `__vg.checkFocusWeb()` reports it, and `scripts/smoke.mjs` now gates it as an automated invariant ("focus web stays above dim notes") rather than leaving it console-only. See `design/0005`. |
| 2026-08-25 | **The automatic swatch is now marked even after a folder is overridden (github#29).** Since folder overrides (github#22), the picker's only mark of "automatic" was the checked ring itself — so pinning a folder to a different swatch erased every trace of what `Auto` would give back, and the only way to check was to click `Auto` and lose the override. `buildColors` already computed the automatic slot per folder (the position-based `i % 12`, archives skipped) before deciding whether an override wins; it just discarded that value right after using it to advance the rotation counter. `groupAutoSlot` now records it in the same loop `groupSlot` already uses, exposed as `api.autoSlotOf(g)` alongside `api.slotOf(g)`, and it answers unconditionally — an archive's automatic slot is always `g11`, a folder's is always its rotation position, whether or not something currently overrides it. The mark itself reuses the existing `data-auto="1"` attribute, widened to fire on the automatic swatch regardless of whether it is also the checked one (previously it only fired when the two coincided), with a new CSS rule — `.swatch[data-auto="1"]:not([aria-checked="true"])` — for the state that could not previously exist. **The visual mark is deliberately not a second ring**: the checked ring already answers "what colour is this folder using", so marking the same fact twice would be redundant, and the two rings could not coexist legibly on different swatches without one reading as more "chosen" than the other. It reuses the exact motif and token the heatmap band already uses for "today" (`page.js`'s `heatDraw`) — a small triangle in `var(--today)`, the one colour guaranteed not to be one of the twelve hues. Landed in all three picker surfaces: the sidebar settings panel, the right-click context menu (a new 5th `autoKey` parameter on `openCtxMenu`, passed only by the folder call site — the subfolder one is untouched, since a subfolder's automatic colour is a computed tint with nothing among the twelve to mark), and the Obsidian settings tab (`plugin/main.js`'s `renderColours`, threaded through both the pre-view fallback list and the live `refreshFromView`). Verified manually via a driven Chrome (`cdp.mjs`): overriding "00 - Inbox" (auto slot `g3`) to `g8` shows the ring on `g8` and the triangle on `g3` in both the settings panel and the context menu simultaneously — the state that was previously unreachable — and clicking `Auto` moves both back onto `g3` together, reverting to the existing dim-ring-only treatment. `node scripts/smoke.mjs --only "recolours exactly one group"` unaffected (1/1 on all three vault shapes, unchanged target/slot/moved-count), confirming `groupAutoSlot` is read-only bookkeeping that touches no actual colour assignment; `check-scope`/`check-network`/`check-pii` clean. |
| 2026-08-25 | **Fixed colours for subfolders, and a right-click menu in the legend to set them (github#22).** `folderColors` had no equivalent one level down: a subfolder always got an automatically-derived tint of its parent's colour, with no override. `subfolderColors` now pins one, keyed `"<folder>/<sub>"` to match `subShade`'s own keys, and skips the tint ladder entirely for a pinned subfolder -- including the neutral-group branch, which previously overwrote every subfolder unconditionally. Right-click on a legend row (folder or subfolder, including the pooled "N smaller subfolders" row, which applies one pick to every subfolder it stands for in a single write) opens the same 12-swatch + Auto popover the settings panel already had, without needing the panel open. Both settings surfaces -- the standalone's gear panel and the Obsidian settings tab -- grew matching nested subfolder rows, gated by the same `state.collapsed` flag the legend's own twisty reads (`plugin/main.js`'s tab keeps separate tab-local state, since it is a different DOM entirely). Unlike a folder row, no swatch in a subfolder row ever rings "current" while unpinned: an automatic tint is a computed shade, never one of the twelve slot hexes, so there is nothing among them to match -- only the Auto button's pressed state says so. The Obsidian plugin's graph view previously had no way to persist a colour pick at all (only its settings tab could write settings); `onFolderColors`/`onSubfolderColors` now let a right-click in the view itself save straight to `saveData()`. **A pin bypasses the nesting gate, not just the tint ladder** (`groupHasPinnedSub`, and its `plugin/main.js` mirror `hasPin`): the first version gated a folder's subfolder disclosure on `subOrder.length > 1` everywhere, which meant a folder that shrank to one differentiated subfolder (its siblings moved or deleted) still honoured an existing pin on that subfolder but hid the only control that could reach it, in the legend, the settings panel and the Obsidian tab simultaneously -- caught by an adversarial review before it shipped, verified live: pinning `09 - Maps of Content/` (a genuine one-subfolder group) flips its twisty from absent to present, and clicking through to Auto removes it again. `subCount` (`"folder/sub"` -> note count) moved from a per-render local inside `buildLegend` to a module-scope value built once alongside `subOrder`, since the settings panel and the `__vg` api both need it too. Verified: a subfolder pin survives a parent folder recolour exactly (`buildSubShades` reads the pin before consulting `colorOf`) and reverts to the original computed tint on Auto; `check-scope`/`check-network`/`check-pii` clean; all 45 invariants pass on all three vault shapes on both the pre- and post-fix builds -- colour is not a layout input, confirmed rather than assumed. |
| 2026-08-25 | **The demo storyboard's 58 beats can now be played one act at a time (`__vg.demo.act(name)`, `demo.mjs --act`, `record-demo.ps1 -Act`), for the per-feature gallery at `docs/features.md` (github#27).** Every beat in `demoMode()` got an `act:` tag matching the file's own eight section comments -- `intro` 3, `note` 2, `timeline` 13, `heatmap` 7, `folders` 6, `subfolders` 8, `camera` 10, `colours` 9, summing to the full 58 with none missed or double-tagged. Isolating an act needed no reset click before it, only a leading `settle`: under `?demo` the page loads at rest with no filter and the gear closed, which is already every act's assumed starting point in the combined run. **First version double-parked `colours`** -- its own trailing `{park}` beat, tagged into whichever act's comment block it sat under, plus a second one `demoAct` appended unconditionally -- caught by actually running `--act colours` end to end rather than trusting the code; fixed by skipping the append when the act's last beat is already a park. Verified against the demo vault, off-screen, no `--cursor`: `--act colours` (10 beats) and `--act timeline` (15 beats) both complete clean with exit 0 and exactly one park each, `--act nonexistent` fails loudly with exit 1 instead of silently recording nothing. `node scripts/smoke.mjs` unaffected: 45/45 across all three vault shapes, unchanged from before this diff. Also fixed in the same pass (github#26, same file already open for `-Act`): `record-demo.ps1`'s capture region came from `GetWindowRect`, which on Windows 10/11 includes the invisible DWM resize border -- measured **14x7px** trimmed off an 800x600 test window once switched to `DwmGetWindowAttribute(DWMWA_EXTENDED_FRAME_BOUNDS)`, with `GetWindowRect` kept as the fallback and the result clamped to the window's own screen's working area. That switch hit a second, unrelated bug on the way in: PowerShell 5.1's `Marshal.SizeOf([Win.U+RECT])` throws (`"System.RuntimeType cannot be marshaled as an unmanaged struct"`) because the bracket-type-literal binds the object overload, not the Type one -- fixed by passing an actual struct instance (`Marshal.SizeOf($r)`) instead, the standard PowerShell P/Invoke idiom. Caught by running the DWM call against a real window rather than trusting it from the parse check passing. |
| 2026-08-25 | **CORRECTION to the row below, and the real cause: the resting-layout check sampled one frame early.** That row blamed contention and it was wrong. The check blocked three pushes with `dtan` **30.3 / 35.9 / 26.7** on the 10k vault; classifying it as frame-reading and moving it to the serial lane changed nothing, and running it **alone** with nothing else on the machine still failed **3 times in 6** (0, 26.4, 22.1, 0, 0, 25.6). **The shape of the numbers was the tell and was there from the first run: bimodal, exactly 0 or 22-27, never in between.** Noise scales; a discrete outcome does not -- so the question was binary, and the binary question is whether the sampler caught the final frame. It did not. The rAF loop snapped `last` only on iterations where `busy()` was still true, so when `busy()` flipped it stopped **without** snapping, leaving `last` one whole frame before the animation's last drawn frame. That frame is worth nothing on a vault drawing **120-127** frames per cascade and **22-27 units** on the 10k, which draws **43** because every frame re-plans the whole vault (github#19) -- against a 16-unit threshold, a coin flip on one fixture and invisible on the other two. Snapping once more on the transition (still before the final assignment, which lands after `busy()` clears -- the reason the 320ms beat exists) gives **dtan 0 on all three vaults across 18 consecutive runs**, and `dr` on the 815-note vault went 0.1-0.2 -> 0 as well, which is the same off-by-one showing up small. **The page was right the whole time**; the check was measuring its own sampling. The frame-reading classification is kept as precautionary, not as the fix. |
| 2026-08-25 | **The most frame-dependent check in the suite was not in the frame-sensitive list, so it was measuring the harness.** *the last frame of a cascade is the resting layout* blocked a push twice with `dtan` **30.3** and then **35.9** against a 16 threshold -- a tenth of a 160-unit row -- on the 10k vault and only there, while passing **every** time it was run with `--only` and passing **three consecutive full runs**. Same code, same vault, same command: values from 0 to 36. That spread is contention, and the number GROWING between the two blocked runs is the tell. The mechanism is in the check's own method: it runs a `requestAnimationFrame` loop snapshotting every frame until `busy()` clears, then compares the last drawn frame against rest 320ms later. `JOBS` defaults to **4**, so a sharded check runs beside three other Chromes; rAF then drops frames, and the ones it drops are at the END of the cascade, where a 10k vault is slowest because every frame re-plans the whole vault (github#19, 14fps). `last` is therefore sampled further back from the true final frame and its distance to rest grows -- so the check reports the harness's frame pacing rather than the handover it exists to measure. Added to `FRAME_READING`, which puts it in the serial lane: 45/45 on all three fixtures with it there. **Worth noting what this was NOT**, because two wrong theories were considered first: not the ribbon measurement (`measureRibbon` forces a layout, but `drawDateUI` is not on the per-frame path -- `afterRender` calls `heatDraw` only, and the intro's sweep calls `drawRibbon`, which reads the cached width), and not a real regression in the cascade. The file's own header already said "numbers from a contended run are weaker evidence"; the omission was that this check was never listed as one of the ones reading a frame. |
| 2026-08-25 | **The date strip had never resized, and the ResizeObserver watching it was innocent.** Reported as "the timeline does not rescale when I resize the window". `fitCanvas` pins an inline pixel width on the strip's canvas -- it has to, since the bitmap is sized in device pixels and the CSS box has to stay in CSS pixels -- and an inline width beats the stylesheet's `width:100%`. `ribbonW()` asked the **canvas** how wide it was, so it got back the width it had last been drawn at, for ever: the first measurement won and every later one confirmed it. Measured on the real vault by driving a headed Chrome through four window widths: strip **1168px in a 668px slot, 1168px in a 1568px slot, 1168px in a 428px slot**, with the last year chip parked at 1101px in all four -- 673px off the right edge of a 428px band. On a page whose very first call ran before layout the strip came up at the **600px fallback in a 1284px band** and stayed there. The observer was wired, was firing, and redrew at the same stale number every time, which is exactly why this read as "there is no resize handling" when the handling was the one part that worked. `measureRibbon()` drops the inline width, reads the box the stylesheet gives, and **puts the inline width back**, so measurement has no side effect -- the first version restored it only on failure, which left the canvas stylesheet-sized after any observation the guard short-circuited: the same number by luck, and a stretched bitmap plus a sub-pixel box mismatch the moment a slot came out fractional. It runs from the draw path and the observer only, never from a pointermove, which reads the cached `ribW`. The observer guards on the width having actually changed -- that both saves a redraw per band reflow and breaks the feedback loop, since drawing writes the canvas size and the canvas is inside the observed element -- and a `window.resize` fallback was added for the no-ResizeObserver case, which had no redraw path at all. After, on all three fixtures: strip tracks slot to within **1px** at 62%, 128% and 100% of the lane's width, inline width equals the box at every step, and the year chips move (`468/468`, `1302/1302`, `948/948`). **Not reproducible in a hidden tab**: ResizeObserver notifications are delivered as part of the rendering steps, so a non-compositing pane reports `document.hidden === true` and never fires them -- the first attempt to measure this produced a page that appeared not to have the bug and did. |
| 2026-08-25 | **The intro is the right-hand scrubber travelling, and the mapping had to go through the rank.** The sidebar's Timeline block was deleted (a rank slider, Play, All) because the ribbon under the band scrubs the same history, in the place that draws it -- and that left the intro with nothing to say what it was doing. `sweepTo` now moves the strip's `to` handle, with the same `#vg-rtip` tooltip a real drag shows. **A preview, not a state write:** `state.from`/`state.to` stay null throughout, verified per frame, because writing them would put a hard date cap in `timeFactor` on top of the rank ramp the cascade is already animating -- and a range change goes `applyRange` -> `cascade` -> `stopPlay`, so the second reveal would cancel the first. `brushEnds()` checks a live drag BEFORE the sweep, so a hand on the handle wins. **The mapping was the real decision, and the obvious one is wrong.** Interpolating `dateSpan.lo -> dateSpan.hi` linearly desynchronises by construction: the reveal is ordered by rank, so at half the run the handle would read 2020 while every note from 2026 was already lit. Going through `tlDateMs[pr * tlMax]` makes the handle mean "everything left of here is on screen", the same thing it means under a hand. Measured, sampling the drawn handle and the lit count every 120ms on the real vault: handle **0.002 -> 0.698 of the strip in the first 240ms** (24 of 457 notes lit, ranks 1-24 spanning 2015 to 2023), 0.98 by 1.4s, then the last 2% of the strip over four seconds while 330 more notes arrive -- the handle crawls where the vault is dense and sprints where it is empty. That is this vault's own distribution and not a mapping artefact: the demo vault (1402 notes, 2 dense years) runs **0.030 -> 0.211 over the first second** and the 10k synthetic (10 even years) **0.010 -> 0.169**, both near-linear. The tooltip's date advances continuously throughout, so the apparent stall is not dead motion. Pinned by *the intro sweeps the range end across the strip*: 24-26 sweeping frames at `timeScale 0.25`, first sample 0.002-0.054, last **1.000**, **0** backwards steps, landing on `x1 === w`, state null throughout, handle labelled while sweeping and not after -- on all three fixtures. |
| 2026-08-25 | **Refresh never cleared the date range, and "Mark today" was a second answer to a question the band already answered.** Two things found while removing the Timeline block. (1) `resetView` reset hidden folders, highlights, the fold state, the marked day, `until`, the query and the selection -- and not `from`/`to`. So Refresh, whose own tooltip says it clears every filter and replays the intro, replayed the intro through whatever range was applied and grew the vault to a slice of itself. Harmless-looking while "the timeline" meant the rank slider (which it did reset); a real bug the moment the ribbon became the timeline. Cleared in `resetView` **and** in `playTimeline`, because boot and the debug API reach the animation without going through `resetView`. (2) **"Mark today" removed at the author's call** -- *use the heatmap for that*. It was already the same predicate as clicking the band's today column (`created === the key`, which for the last cell of the grid IS today), from a place where the answer is not drawn, and it is the one that got the predicate wrong twice: `created` alone matched **0 notes against 3 touched** on 2026-08-21, and `created || touched` then matched **111 files** on 2026-08-19, which was a folder renumbering. What was NOT redundant was the **fill**: `markToday` recoloured its notes to `--today` (the neutral extreme, deliberately not a group hue) while a marked day only haloed, so the fill moved onto `state.markDay` rather than being deleted with the button -- otherwise the band would have inherited a weaker version of the job. Gated on `state.markDay` and **not** `isMarkedDay()`, which also answers for `hoverDay` and a hovered year: recolouring a whole year of notes as the pointer crosses a label is far too loud, so a hover asks and a click chooses. `state.markToday`, `isToday()` and the `__vg.isToday` export are gone with it, along with two `smoke.mjs` checks -- one duplicated the marked-day push check, the other pinned two predicates together as set equality and is tautological with one. Replaced by *a marked heatmap day recolours its notes*, which settles the ramp rather than racing it and asserts **12/12 recoloured and 12/12 back to their own hue** (3/3 on the shape vault). |
| 2026-08-24 | **Fixtures moved to a shared, self-invalidating store (github#20).** `gen()` regenerated a fixture only when its directory was missing, so every worktree kept whatever it generated whenever -- and the push gate's verdict depended on which directory the push ran from. Measured cost the day it was filed: the develop checkout held a **468-note** demo vault from before the generator fix, the feature worktree the **1,406-note** one from after, and two checks failed on fixture content while the code was innocent ("hovering '2021' haloed **0 of its 0** notes" -- a year that vault genuinely did not populate; a range change measuring **2766 units** of travel where the current fixture travels 144). The issue's own premise was half wrong and the research corrected it: the generators ARE seeded (mulberry32, `--seed 1`) -- the hidden input is `--end` defaulting to today, deliberately, so the 52-week heatmap window stays exercised. A fixture's identity is therefore (generator sources, args, generation day), and staleness has two modes: the generator changed, and the fixture AGED. Fixtures now live at `<main repo>/.fixtures/<name>-<digest8>` -- digest = sha256 over the CONTENTS of all three generators plus the args (content, not mtime: branch switches rewrite mtimes without changing a byte, and all three sources feed every digest because make-demo-vault delegates to make-test-vault) -- resolved through git's common dir so every worktree and the hook share one copy. A `.stamp.json` carries the generation day; older than **7 days** regenerates, so the today-relative checks never drift more than a week off the clock. Generation goes into a `.building-` scratch dir and is renamed into place only after the stamp is written, so an interrupted run can never be mistaken for a fixture; older digests of the same name are pruned. A leftover fixture directory in a checkout root is IGNORED with a one-line notice -- honouring it would rearm the exact incident -- and `--vault` stays the explicit override. |
| 2026-08-23 | **The gap that was too wide was the CHANNEL, and two rounds of shrinking other things proved it.** Channel over an ordinary step, measured on three vaults: **1.60, 1.72, 1.52** -- and almost none of it the seam. Taking the seam from 24 units to 12 moved that number from 1.60 to **1.60**; adding an excess on top of the existing margin made it **1.90**, because the margin plus each end note own radius already WAS the excess. The quantity to name is therefore the channel: what a boundary has over one note-to-note step, with half a pitch per side and nothing added as the zero point (two wedges each holding their end note half a step in from their own edge put those notes exactly one step apart, which is invisible). Scaled as one quantity by the rows falloff: **1.32 / 1.23 / 1.07** against 1.30 / 1.18 / 1.00 asked for. Verified neutral first -- at EXCESS_KEEP 0 it reproduces 1.60 / 1.72 / 1.52 exactly, so the new phrasing is the old geometry until the constant moves. The end margin also stopped ADDING each note radius and corrects for how it differs from a typical one instead: adding it bought edge symmetry at a cost of ~126 units of width at every boundary, and the difference buys the same symmetry for nothing on average. An ordinary step widened as a side effect, **150/143/156 -> 155/156/163** -- margin the wedges are not spending is room the notes get. |
| 2026-08-23 | **Dot size was fighting the camera, and no seam could have survived it.** Sigma's default `zoomToSizeRatioFunction` is `Math.sqrt`, so a node's drawn radius goes as `1/sqrt(ratio)` while its POSITION goes as `1/ratio` -- the two diverge on every zoom, and no choice of size holds a dot at a fixed fraction of the gap to its neighbour. Measured drawn diameter over row pitch: **0.76 at rest, 1.44 three notches in, 3.51 at the far end** -- dots swallowing their neighbours. Set to identity, so sizes follow the same law as positions, and the figures handed in are pinned to the lattice once rather than re-derived per frame: **0.787 / 0.786 / 0.786 across a 21x zoom range.** Two earlier attempts were measured with `getNodeDisplayData().size`, which is the reducer's output and NOT the drawn radius -- both reported a flat 0.786 while the screen showed dots touching. `renderer.scaleSize()` is the honest number; the label placer goes through it too, having been out by a factor of 20 zoomed in. |
| 2026-08-23 | **The size floor stopped tracking the lattice, and that was the dots overlapping.** `SIZE_FLOOR` 0.45 existed so dots could not vanish, calibrated against vaults whose rows land 22-30px apart; a 10,000-note disc fitted to the stage puts them **4.5px** apart, where the honest scale is 0.16 and the floor held it at 0.45 -- **2.8x too big for the lattice.** Measured: biggest dot **175 units of radius against a 160-unit row pitch**, 349 across, and the MEDIAN dot 192 across. Replaced by handling the two ends separately, because they want opposite things: the biggest dot is capped at a fraction of the pitch (or notes collide), the smallest is floored in PIXELS (or notes disappear), linear between, so the degree ordering shows wherever there is room and the range flattens rather than overflowing where there is not. Biggest diameter **349 -> 126** against a 160 pitch. A NODE_MAX pixel ceiling survived one round and had to go with it: it froze dots at 11px while the lattice kept growing, which read as swelling and then dwindling. |
| 2026-08-23 | **Every dot is sized against its own nearest neighbour, measured rather than assumed.** The row pitch is a constant; the along-row pitch is not, because a wedge's end margins come out of the same circumference its notes are spread across -- so a band carrying many wedges packs its rows tighter, and dots sized against the row pitch alone came out **126 units of diameter against a 120-unit step**. One figure for the whole disc was tried first and is worse: it has to be the tightest spot anywhere, and it shrank every dot for the sake of a handful, **0.79 -> 0.24 diameter over spacing disc-wide.** Per note now, keyed by id, taking the smaller of its two sides, and measured **across wedge boundaries as well as within them** -- those are the notes with the least room, and missing them left boundary dots overlapping by 27 units on the 1402-note vault (now +9). The 10k keeps a **-17 unit** worst case where the pixel floor wins over the cap; that is 0.48px on screen, against dots that would otherwise not be drawn at all. |
| 2026-08-23 | **The seam: a width rather than an angle, one width for every boundary, parallel-sided, measured to the dots' edges, and falling off as a band gets deeper.** Five changes, each from looking at it. (1) A degree is not a width -- it buys arc in proportion to radius, so the same 2 degrees was 75 units on a 454-note vault and 129 on a 1402-note one, which is where "the gaps are too big" came from. (2) Group boundaries take the SUB-wedge width, since the colours and the legend already say which is which and the wide version read as a missing slice. (3) Sizing it as a width and dividing by each note's own radius makes the channel's edges genuinely parallel: a point at radius r offset by w/2r sits at perpendicular distance w/2 for any r. Measured on the 1402-note vault, boundary arc at the inner row against the outer: **214 -> 205 units across a 4.37x change in radius**, where a constant angle would have given 4.37x. (4) The margin is measured to the note's EDGE rather than its centre: dot radius runs 4x from leaf to hub, so equal centre distances put edges visibly unequal distances from the boundary -- reported from a screenshot of the 12 o'clock axis with a hub on one side and a leaf on the other. (5) It falls off as `(5/rows)^1.5`, fitted to three judged points: half the base seam at 5 outer rows, a quarter at 9, none to speak of at 23. Final widths: **12, 4 and 1 graph units.** Third rule to stand where `gapScale`'s note-count ramp used to, and the first measured against the geometry rather than a proxy for it. |
| 2026-08-23 | **Date fields, year buttons, and a debug axis that earned its keep and then left.** The range readout became two `<input type="date">` fields bounded to the vault's span, grouped with All dates at the end of the heat row -- it had been loose in the middle, reading as a third unrelated thing, and the only way to set a range was to find a two-pixel handle at the end of eleven years of strip. The year labels became real buttons: they were painted on the canvas, which meant hit-testing a pixel band by hand and no keyboard, no focus ring, no hover state the browser could give us. Hovering one haloes that year's notes through the same state a marked day uses (582 of 582 measured), clicking selects the calendar year, and the button the range sits on reads pressed. A `?north` rule from the ring centre to 12 o'clock settled what "not centred" meant -- the wrap seam is within 0.35 degrees on the outer band, and the sparse inner rows that read further off hold one or two notes in that row, which is sampling rather than geometry. Removed once it had answered. |
| 2026-08-23 | **Two ribbon checks failed on the shape vault for two reasons, and neither was the page (github#18).** Filed as "the window and the brush are not independent, and a track press lands 26 days off". The brush was never touched: a `win`-mode drag returns early in both `pointermove` and `endDrag`, and the check's `detail` printed `brush MOVED` whenever the WINDOW failed to move because the two conditions shared one flag. **The first real fault was the fixture.** `make-shape-vault.mjs` hardcoded 2025-09-01 and ran its 420-day cycle forwards, so by 2026-08-24 it stamped **68 days of notes with a `created` date in the future** — which no real note has, whatever date the note is *about*. Discount them and its history was **357 days against a 364-day window**: the band already covered everything, `clampWinEnd` returned one value for every input (`first` 2026-08-31 was *later* than `last` 2026-08-24, so both bounds collapsed), and pinning was correct. The reported numbers follow exactly — window 2026-08-24 unchanged, centre 2026-08-24 − 182d = **2026-02-23**, which is the figure in the issue to the day. The end date now defaults to today with the cycle running backwards, the same deliberate break from byte-reproducibility `make-test-vault.mjs` takes and documents; `--end` restores it. Measured: newest note **2026-10-25 → today**, travel **−7d → 84d**, fixture shape unchanged (954 notes, 77% dominant folder, 139 unlinked). **The second was the checks, and re-anchoring alone would not have fixed them** — measured 33d off afterwards. Both pressed at fixed ribbon fractions (0.90/0.62 and 0.35) that reach somewhere only when the window is small against the span; where the pill fills most of the rail they aim past the end of its travel and measure the clamp. They now press at each end of the rail, read the travel off the control, and aim inside it — full travel for the drag, midpoint for the press. **Not a relaxation:** the tolerance is the same 14 days, at positions where centring is promised, and end-at-pointer — the regression they exist for — still misses by ~180d wherever there is travel. A window with nowhere to go reports the number and passes instead of failing. Centring lands **7d off on both the 10k and the shape vault**, which is the Monday-quantisation floor (`mid = aim + 7d − (0..6)`), not slack. **This failure was calendar-dependent and would have cleared itself around 2026-09-07** as today drifted a full week past `dateSpan.lo + 364d` — the fixture's fixed anchor is why nobody would have known which change fixed it. 39/39 on all three fixtures. |
| 2026-08-23 | **The README hero is animated WebP at 30fps, and `make-gif.ps1` is now `make-hero.ps1`.** Same 34s take at 700px: the GIF was **9.2 MB at 15fps**, WebP is **3.08 MB at 30fps** (q60 is 2.64, q80 is 4.23) and 1.81 MB at 15fps -- double the frame rate for a third of the bytes, and full colour, so the two-pass `palettegen` + bayer-dither workaround that existed only because a GIF carries 256 colours is off the default path entirely (kept behind `-Format gif`). mp4 was measured and rejected: `api.github.com/markdown` strips `<video>` outright -- a relative src, a `raw.githubusercontent` src, a `github.com/<owner>/<repo>/raw` src and a `user-attachments` src all render as an empty `<p>`, and an `<img>` fallback nested inside the tag is dropped with it -- and the rendered page's CSP `media-src` omits raw.githubusercontent. WebP is an `<img>`, so it survives GitHub and Obsidian's community plugin browser alike; that browser fetches the README from raw and sanitizes with DOMPurify, whose default allowlist keeps `video` as well, so a video would play there and nowhere else. **The 10 MB ceiling was never ours** -- that is the comment-box upload cap, and camo's 5 MB applies only to externally hosted images, while a relative path renders as `/<owner>/<repo>/raw/<branch>/<path>` and is not proxied. What the size actually cost was the clone: six `demo.gif` revisions came to **49.9 MB of a 61 MB `.git`**. Never transcode the GIF -- that gave 3.43 MB at a *lower* frame rate, because its baked-in bayer dither is noise to the next encoder. `compression_level` stays at 4: level 6 saved 6% and took 96s against 4s. |
| 2026-08-23 | **A 500-note vault and a 1500-note vault filtered down to 500 drew different discs, because the lattice spacing was a constant (github#13).** Reported as an impression -- "a 1500-note vault toggled down to 500 will render differently" -- and it was exactly right. `SP`, the lattice spacing that sets both the radial gap between rows and the tangential gap along them, was a hard 1. With the normalisation box pinned to the full-vault extent and the camera still, screen pixels per lattice unit is a constant, so **screen row pitch was a constant per vault**: measured 19.481px at every filter state of a 500-note vault and 12.064px at every state of a 1500-note one, while filtering 503 notes down to 62 moved the median dot 4.254px -> 4.208px and the hole from 32.8% to 43.9% of the disc. That last number is the exact bug `r0`'s own formula comment was written to prevent -- pinning `r0` into `geomLock` reintroduced it for every filtered view. **The solve is exact, not a fudge.** A lattice of spacing `s` holds `1/s^2` notes per unit area, so an annulus with its hole at `HOLE*R` holds `n = pi*R^2*(1-HOLE^2)/s^2`; setting that equal to the full-vault `R` gives `s = sqrt(n_full / n_visible)`, on the same uniform-density assumption the `r0` formula already makes. After: `pitchPx * sqrt(shown)` spread **2.85x -> 1.10x** (500-note) and 2.82x -> 1.09x (1500-note), and every state below the density cap is **exactly** 436.919 / 467.219 -- invariant rather than approximately so, with the whole residual in the capped step. Median dot 4.238px -> 10.854px; hole holds 0.304-0.328. `r0` needed no change at all: pinned at `HOLE * maxR` it holds the hole at a constant share once the disc keeps its radius. **Three things were got wrong on the way and are worth recording.** (1) A **correction pass** that fed `maxR` back and scaled `SP` until it landed on the locked extent made it *worse* -- spread 1.10x -> 1.15x -- because it drove `SP` back to 1 nearly everywhere. The reason contradicts the premise the work started from: **filtering barely moves the disc's radius.** `maxR` is the max over cells, so the deepest survivor still reaches the rim -- `reach` was 1.000 at 481, 465 and 382 of 503 notes showing, i.e. there was never any empty margin to reclaim. Filtering makes the disc *sparser* at a radius that hardly moves, and that radius is quantised in whole rows and already flush with the box, so 2.3% more spacing buys 7% more radius and no spacing exists between "no change" and "one row over". (2) **`HL_PUSH` was scaled by `SP` and should not be.** It is quoted in rows, so scaling looked right, but it was sized as a fraction of the *radius* ("0.9 rows on a ~13.3-row disc is 6.8%") and the radius is what the solve holds still -- it is the row count that falls. At the cap the disc is ~5 rows deep, so a scaled push would have been 18% of it and a highlighted note would have protruded off the stage. (3) The **cascade must be handed the spacing**, not left to derive one: deriving it from `planTotal` mid-animation samples alpha-weighted membership, which slides every frame, and the biggest single-frame radial step went **0 -> 94 units against a row of 160**. Fixed the way rows already were -- both endpoint packings built from binary presence, per-frame value interpolated between them on the same clock as `rowsAt` -- back to 0. **`fitRatio()`'s upper clamp came off 1**, which was correct only while the disc could exclusively shrink; a spread lattice puts the outermost row a few percent past the lock (measured 1.079) and that is disc, not margin, so fit was framing a filtered disc with its rim cut off. **Continuous on purpose.** The retired `REPACK_BELOW` mechanism switched plan basis at 55% and all three of its recorded failures were failures of the *threshold*; a smooth function of visible weight has no side to be on, and it keeps both plan invariants for free -- a departing note sits at weight 0 and adds nothing to `planTotal`, and the lean and padded plans each derive it from their own. Two new checks in `scripts/smoke.mjs` and a new `scripts/measure-density.mjs`, which is the instrument this was built with rather than an afterthought. |
| 2026-08-23 | **A camera cluster in the bottom-right corner, and Fit fits what is there (github#4).** Four 31px buttons -- zoom in, zoom out, fit, pan -- stacked with zoom furthest from the corner (the pair reached for repeatedly) and pan at the bottom, apart, because it is a MODE rather than an action. The Fit button in View is deleted: it did exactly what the corner control does, and two buttons for one job is one too many to explain. **Fit now scales the ratio by how much of the locked extent the disc actually uses**, which is what "centring should also zoom when the outer ring got smaller" asks for -- measured, hiding 18 of 20 groups takes the disc to 850 units and the ratio from 1.08 to 0.2274, still centred at (0.5, 0.5). It is normalised **plan against plan** (`plan.maxR` over `geomLock.maxR`), not against node positions: the outermost NOTE sits inside the lattice radius it was packed against, so measuring positions made a full unfiltered disc read as 96% of itself and fit zoomed slightly in on nothing (ratio 1.0372 where 1.08 was correct). An earlier attempt compared `geomLock.maxR` with graph coordinates directly, which is a factor of UNIT out -- 160x, which clamps to 1 and looks exactly like the feature not being wired up. **Pan defaults ON** -- github#4 proposed off, and the reverse won: the rim of a big vault is unreachable without it, and a control in the corner is a cheaper way to find that out than a settings tab. Absent means on, so only an explicit `false` locks it, and a cleared store returns to the default rather than to "off". Turning it off **flies home first and re-locks in the animation callback**, because sigma's `Camera.validateState` drops x and y while panning is disabled -- a camera left off-centre with the lock on cannot be recovered by anything that sets x or y, `fit()` included. `--controls-inset` is 12px on the page and **44px under Obsidian**, where the status bar floats over exactly that corner; `#vg-detail` caps its own height against `--controls-h` so the card yields the corner rather than the control moving out of the card's way (measured clearance: 12px with a card taller than the stage). Four new invariants, and they found three of the bugs above. |
| 2026-08-23 | **The gap total no longer counts notes, and the step across `settle()` is zero on every path.** The first attempt walked the group presences between the cascade's two packings, which fixed the reservation DURING an animation and left the handover to rest wrong in the other direction: measured across `settle()`, **nG 7 -> 3.38 and a wedge start moving 3.774 degrees** on a date change, because the resting plan seats notes by `visible()` -- date-blind -- so an excluded note kept its SEAT while its weight went to zero and presence read survivors over everything, 0.48 where the truth is 1. The animation was interpolating to the right number and rest was recomputing a wrong one. Two changes finished it: `geom` asks `willShow` rather than `visible`, so the date range is a way out like any other; and presence is **`min(1, weight)`** rather than weight over seats, because a wedge earns one gap by existing rather than by being full. Seats are not consulted at all any more and the parameter is gone -- every reading of them divided by a number that had nothing to do with the question. The collapse case that weight-alone was rejected for in the first place (a 55-note folder pinned at 1 down to its last note, then closing in two frames) is a group on its way OUT, which the cascade knows about before frame one and now supplies itself. Measured, step across `settle()`: **104 units / 3.774 deg -> 0** on a range change, and 0 on all four paths (hide a folder, show a folder, narrow a range, clear a range). Timeline gap at rest across a scrub: **nG 6.716 / 6.968 / 6.985 / 6.987 / 7 -> exactly 7 at every position**. 33/33 on both vault shapes. |
| 2026-08-23 | **The step across `settle()` had never been sampled, which is why a jump nobody could find survived three fixes.** `probeSample` ran inside the animation loop only, so a report covered every frame of the animation and stopped one frame short of the frame that ENDS it -- the handover from "interpolated by cascade progress" to "recomputed at rest". Anything the two sides disagreed about landed exactly there, after the motion had visibly finished, which is precisely the shape of the complaint ("it still jumps at the end of each animation") and precisely what every in-animation number said was fine. `settle()` now samples either side of itself and `probeReport()` calls the handover out separately, with the wedge start that moved furthest and the group it belongs to. Also `__vg.lastGap()`, since a resting disc has no frames and "do two rest states agree about the gap" is the whole question behind a jump at the end of one. |
| 2026-08-23 | **The probe only ever measured radius, and a whole class of jump lived in the half it could not see.** Every number `probeReport()` returned was a radial extent, so a change that leaves radii alone and slides every wedge round the circle measured as perfectly smooth -- and "a range change animates instead of snapping" passed while the disc visibly jumped. It now records the **tangential** step per frame in graph units (angle moved times the radius it moved at, shortest way round the 12 o'clock seam, counting only notes present in both frames), the distribution behind it (how many notes moved further than one row, and the mean), each group's **wedge start angle**, the rendered gap angle and the continuous group count. On the 1402-note vault the radial worst step across six range changes was **0-80 units against a 160-unit row** while the tangential worst was **2668-4669**. Also `__vg.lastCascade()`, because "did it animate at all, and over how many notes" was being inferred from frame counts, and an instant apply and a one-frame animation look identical from outside. |
| 2026-08-23 | **The gap reservation was derived from live weights against stale seats, so thinning a folder moved every wedge boundary twice.** A group's gap presence was `min(1, weight / seats)`, which is the right reading only while the seats are the group's own final count. During a cascade they are not -- the plan is rebuilt each frame from what is still on screen -- so a group **keeping 30 of its 100 notes read 0.3** while the departing 70 were still seated and **1.0** the moment they left the plan. Nothing about that group's gap should have moved at all. A legend toggle cannot show it, which is why it shipped: hiding a folder takes every note to zero together, so presence runs 1 -> 0 cleanly and survivors stay pinned at 1. The date range is the first filter this page has had that thins groups **without emptying them**. The reservation is now walked between the cascade's two packings by its own progress, exactly as row counts already were -- one planner, one clock. Measured over six range changes on the 1402-note vault: worst single-frame step in the continuous group count **0.04-0.433 -> 0 exactly**, the end-of-cascade value **3.38 / 3.98 / 5.05 / 5.90 -> 7 becomes a flat 7 -> 7** (so the last frame and rest now agree by construction rather than by luck), and notes moving further than one row in the worst frame **37 / 42 / 107 / 173 -> 18 / 8 / 65 / 99**. Asserted at **exactly zero** by "the gap reservation holds still while groups only thin", which fails on the old code. Note this is inert above 10,000 notes, where `gapScale` is 0 and there are no gaps to reserve. |
| 2026-08-23 | **Not fixed, measured and left standing: a lone note in a folding row crosses the wedge from its middle.** `u` within a row is shared by weight, so a note alone in the outermost row sits at `t = (0 + w/2)/w = 0.5` -- dead centre -- and when the row folds it lands at the *edge* of the row below, `u = 0.98`. The serpentine reversal makes row crossings continuous **at t = 0/1**, which is where notes normally cross; this one crosses from the centre. On the 1402-note vault that is the last note of `04 - Daily Notes` at r=3879, a **0.48 x 1.47 rad = 40 degree** step, **2652-3089 units in one frame**, and it is the largest single movement in a range change by a factor of twenty. Both fixes change the resting disc -- normalising within-row position by the row's CAPACITY rather than its occupancy packs partial rows from one end instead of spreading them -- so it is a design call, not a bug fix. Also outstanding: wedge starts still step up to **2.0 degrees** in a frame near the end of a cascade (137 units at that radius), which `nG` holding at 0 proves is `avail` redistribution and live sub-gap presence rather than the gap itself. |
| 2026-08-23 | **A vault with one dominant folder and an unlinked root note failed three checks, and only one of the three was a defect in the disc.** Reported with the shape, the numbers and a per-check diagnosis in [#5](https://github.com/luke321/vault-graph/issues/5) by Angel Bartolli. Reproduced with a generated fixture at the reported proportions -- 954 notes, one group at **77%**, 139 unlinked, one of them at the vault root -- which gave the reporter's three failures with the reporter's numbers. **(1) Zero-weight invariance, the real one.** `rowsNeeded` ends in `Math.max(1, k)`, so a cell seated at weight 0 still asked for one row, and that row reaches the band balancer's split search -- so the hub radius the padded plan chose differed from the lean plan's: **`leanMaxR 13` against `paddedMaxR 14`** with the dominant folder hidden, and clean on all four other groups, because anywhere else one row disappears into the maximum. The codebase already knew about the floor and worked around it at *one* call site (the cascade's row recorder, `src/page.js`, which skips `wsum <= 0.0001`); the geometry never did. `rowsNeeded` returns 0 with no weight to place, and that guard stays as it is -- skipping and recording-zero are different things there. After: 13 vs 13. **Worth being precise about the blast radius**: nothing here reaches the screen at rest, since the resting plan seats only visible notes, and mid-cascade `geomLock` pins the geometry -- measured `innerMaxStep 0`, `outerMaxStep 0` across 122 frames. The guarantee was broken; the symptom was masked. **(2) Two highlight checks were picking by folder and highlighting by group.** `isHighlighted` keys on `groupOf()`, which answers `(unlinked)` at degree 0, so "the first note of the alphabetically-first folder" is unreachable by highlighting that folder when the note is an orphan -- `hl` stayed 0 and the size ratio 1.00x on code that was fine. They pick from `groupOrder()` now. Same `folder` vs `group` confusion as the colour bug the day before, this time in the harness. **(3) The invariance check could not see its own subject.** It compared the lean plan's cell keys only, so a cell existing solely in the padded plan -- the seated zero-weight cell -- was never compared, and the failure surfaced as an empty `rowDiffs` beside a differing `maxR`. Union of keys now, missing counted as zero rows; `checkPlanParity` always did the union. With the fix sabotaged it flags **four** hidden folders where the old form flagged one. **The suite runs a third vault shape from now on** (`scripts/make-shape-vault.mjs`, measured **+13s** per push, against an estimate of 1.5 minutes made before anyone timed it -- 954 notes is a cheap fixture next to the 10,000-note one): neither existing fixture has a group over ~40% or an unlinked root note, and both passed this bug happily. Its dominant folder is nested five deep-ish on purpose -- a flat vault gives the nav-alignment check no twisties to open, and a fixture that silently disables one of the suite's own checks is a worse fixture. |
| 2026-08-23 | **Unlinked notes were painted in their folder's tint under a legend row promising one colour.** Reported with the diagnosis, the numbers and a screen recording in [#3](https://github.com/luke321/vault-graph/issues/3) by Angel Bartolli, who also offered the fix on a fork branch. `groupOf` has answered `(unlinked)` for degree 0 since unlinked notes became a group, and every dimension except `folder` routed through it; the folder branch of `nodeColor` went straight to the note's own `a.folder` sub-shade. Measured before: **0 of 12 orphans on the swatch, 9 distinct colours** on a 700-note generated vault, and **6 of 148** on the 10,000-note synthetic. After: 12 of 12 and 148 of 148, one colour each. One `return`, and it reaches the ring band colours, the label ink contrast and the heatmap stacks for free, because all three already call `nodeColor` — which is also why fixing it at the band level was rejected. `(vault root)` is not a second instance: the builder writes it as a real folder value, so `colorOf` resolves it already. **The interesting part is why the suite missed it.** There *was* a colour check — the folder-override blast-radius one — but it reads `colorOf(group)`, and the group colour was right the whole time; nothing read what a note is actually painted. The new check goes through `renderer.getNodeDisplayData().color` and asserts **all** orphans, not any: those 6 coincidental matches on the synthetic mean an `any` form passes on a broken build. On `demo-vault`, which mirrors a real vault and has **0 of 452** unlinked notes, it reports having nothing to measure rather than passing — the vacuous-check trap this file already records once. |
| 2026-08-22 | **The hub grew until it was half the disc, and the logo grew with it.** `HOLE` is 0.3 precisely so the hub stays the same FRACTION of the disc at any size -- its own note records that a fixed r0 once gave "a 32% hole at full size and a 69% one when filtered down". Letting the band balancer grow r0 freely to hit a thickness ratio reintroduced exactly that: measured **47.3% of the disc on a 450-note vault and 58.2% at 10,000**, r0 running 2.05x and 2.4x its formula value. The centre logo is drawn at half the hole, so it visibly grew -- 160px on one balancer target, 130px on the next, for a reason that has nothing to do with the logo. The hole share is now priced into the search (`HOLE_MAX` 0.36, penalised rather than forbidden) and comes out at **35.4% and 34.4%**. The cost is real and worth stating: the 450-note vault's thickness ratio slips from 0.53 to 0.40 against a 0.55 target, because keeping the hole honest is worth more than the last 0.13 of ring balance. |
| 2026-08-22 | **The logo is a fixed 128px, capped by the hole.** It was purely a fraction of the hub, so it changed size whenever the layout did -- once r0 became something the balancer solves for, the mark was resizing with the folder split. `LOGO_OF_HOLE` survives as a CEILING rather than the rule: zooming out shrinks the hole in pixels, and a genuinely fixed size would spill the mark under the innermost notes. |
| 2026-08-22 | **The note-aim check is density-aware instead of flaky.** At 10,000 notes the dots are ~2.5px in radius and the most isolated one has ~6px of clearance, so aiming at its centre lands on the dot some runs and in the gap on others -- the same build passed at 5.2px and missed at 6.1px. Below 10px of clearance a miss is now REPORTED as too dense to aim rather than failed. A flaky check is worse than an honest gap: it trains you to re-run instead of to read. |
| 2026-08-22 | **The split and the hub radius are searched JOINTLY, and the target moved to 0.55.** Scoring a split at the base radius and only then solving r0 is two-stage and measurably wrong: on the 450-note vault it chose 2/4 rows for a **0.27** ratio when 3/3 reaches 0.80 -- closer to the target -- because the split had been scored against a radius the layout then moved away from. `share()` depends only on the split, so it runs once per split and the radius scan reuses it: cost is now the best over the radius range rather than the value at one point in it. Retargeted from 0.75 to 0.55, which reads as the outer ring carrying the disc rather than two rings competing. Final: **0.53 at 450 notes (3/4 rows), 0.55 at 10,000 (12/17)** -- both on target, where before the joint search they were 0.27 and 0.56. |
| 2026-08-22 | **Balanced bands, and four ways of getting it wrong first.** Which ring a group sits in was decided by size alone -- every group too small to earn its minimum wedge went inner -- which on a 10,000-note fixture gave **35 rows inner against 9 outer**. Groups are now moved between rings to target an inner thickness of 0.75x the outer. The mistakes, in order: (1) the balancer sat above the declaration of `r0`, so `var` hoisting handed it undefined, `rowsNeeded` returned 1 for every group, and it was a **silent no-op** -- 35/9 became 34/9 and the suite stayed green because nothing asserted the thing the feature was for; (2) it optimised `rows x pitch` while the eye sees `(rows-1) x pitch`, a fifth of the answer on a shallow band -- it believed 0.40 where the disc showed 0.32; (3) single-move greedy descent got **stuck at 0.32** on a vault where a two-group move reaches 0.80, so splits are now enumerated exhaustively when the movable set is small; (4) it bought even rings by **exiling small folders to the rim**, where a three-note folder is three lonely dots holding open a 6-degree wedge. Small folders are pinned inner now -- and the pin threshold had to become **absolute** (fewer than 10 notes) rather than `smallAt`, which scales with the vault and made a 148-note folder "small" at 10k, nailing 32 cells into the hub and leaving no assignment that could recover. Final: **0.80 at 450 notes, 0.72 at 10,000**, inner thinner than outer in both, no small folder outside. |
| 2026-08-22 | **The hub radius is solved, not assumed.** `r0` came from `HOLE` and the note count and knew nothing about how many notes ended up pinned inside it. With small folders unable to leave, moving groups is not enough: the inner band is a full circle shared among its groups, so adding a big group takes arc *away* from the small ones and costs them rows. The radius is scanned outward to 3x and the best kept -- bounded because past that the hole stops being a hole and becomes the composition. |
| 2026-08-22 | **Unlinked notes became a group.** They were sunflower-packed into the hub hole, which put them outside every rule the rest of the language follows: no wedge, no legend row, no filter, no count, and their folder silently under-reported its size. `groupOf` returns `(unlinked)` for degree 0, so they flow through colour, legend, filtering and band assignment untouched. `hubPositions` deleted with it. |
| 2026-08-22 | **Wedge gaps scale with density.** `SLICE_GAP` of 2 degrees was tuned where rows sit ~28px apart; the disc's pixel size is fixed, so 10,000 notes close the lattice around a seam that does not close and it reads as a missing slice. Scaled to nothing by 10,000 notes, sub-gaps riding the same factor so the hierarchy between the two does not invert. Note COUNT is the proxy on purpose: the radius is pinned by the normalisation box, and using it would be circular since `gapFor` is called to compute the reference width the radius comes from. |
| 2026-08-22 | **Prepared for a public repo: install guide, third-party notices, and a README that leads with the demo.** `INSTALL.md` covers the whole requirement (Node 18 and a vault), the six-step vault resolution, the flags, and — the part documented nowhere until now — **what the output actually contains**: every note title, path, folder, tag, `created` date and the link graph, but no note bodies. That distinction decides whether the file is shareable, and it was undocumented. Also states plainly which helpers are Windows-only, since only the builder is cross-platform. `vendor/NOTICE.md` carries the MIT texts for Sigma.js and graphology, whose minified builds have had their own headers stripped upstream — and `build-graph.mjs` now emits the same notice into every generated HTML, because the output **inlines** them and so redistributes them too. README opens with `assets/demo.gif` (760px, 11fps, 4.4 MB, generated from a real `record-demo.ps1` take) and no longer hardcodes a source path. |
| 2026-08-22 | **Both rings rotated back by half a gap, so the wrap gap is CENTRED on 12 o'clock instead of starting there.** The allocator places a full gap before the first group and sweep 0 is 12 o'clock, so the gap between the last group and the first spanned `[0, gap]` -- 12 o'clock sat on its leading edge, which reads as the disc being rotated half a gap clockwise. Applied at the sweep-to-angle conversion rather than by changing the leading offset: that offset arithmetic is what keeps every term continuous when a group fades out, and it is not worth disturbing for a constant rotation. `gap` there is the BAND's own already-clamped gap, so one line rotates each ring by its own half-gap. Constant, deliberately not presence-weighted -- anything weighted there would rotate the ring as notes arrive, which is exactly the bug class the leading-offset note describes. Measured: outer wrap-gap centre **+1.019 deg to +0.019 deg**, inner **+2.608 deg to +1.607 deg**, every note in both bands moving by exactly **1.0 deg** (`SLICE_GAP` 2 deg, so `gap/2`). The inner band's residual is not an error and not a rotation the change missed -- it is that a 1-note wedge centres its note ~3 deg from each edge, so note positions do not mark the boundary there; the outer band, whose rows are full, lands on 12 o'clock to 0.019 deg. |
| 2026-08-22 | **The lattice invariant is checked after all — I had written it off too early.** It was listed as uncoverable because the literal form, "radius is base + an integer row x SP", needs `SP`, `INNER_SCALE`, `UNIT` and `geomLock`, none of which are on the debug handle. The EQUIVALENT claim needs none of them: *the distinct radii within one band must be evenly spaced*, from node positions plus `buildWedgePlan(false)` for band membership. It is also stricter, catching a band whose spacing drifted and not just a fractional radius. Measured inner **2 rows at 128, spread 0** and outer **7 rows at 160, spread 0** -- and those gaps are the constants themselves, `UNIT x SP` and `UNIT x INNER_SCALE`. Excludes notes below full alpha (mid-cascade radii are legitimately fractional -- that is what `rowsOf` is for) and degree-0 notes (sunflower-packed into the hub hole, never on the lattice; moot on this vault's 0 orphans but not on one with them). 15 checks now; the only manual invariant left is the per-frame animation step. Sabotage-tested, the same way the hook was: nudging one note 40 units -- a quarter of a row -- takes the outer band from `7 rows, spread 0, even` to `8 rows, gap 40, spread 120, NOT even`. |
| 2026-08-22 | **`v1.2.0`, and the invariant suite gates the push.** `.githooks/pre-push` runs `scripts/smoke.mjs` and blocks on failure -- tracked in the repo rather than written into `.git/hooks`, which is not version controlled, so a hook living only there is a hook the next clone does not have. One command to enable per clone: `git config core.hooksPath .githooks`. pre-push and not pre-commit because the suite builds the page and drives a real Chrome, ~30s: a fine price per push, an intolerable one per commit. `SKIP_SMOKE=1 git push` skips it deliberately; the hook does not mention `--no-verify`, which silently disables every other hook and is the habit that makes hooks worthless. Verified all three paths, including sabotaging a check to watch it actually block -- a hook nobody has seen fail is a hook you do not know stops anything. |
| 2026-08-22 | **The builder is in `src/`, and two scripts were looking for it in `scripts/`.** `smoke.mjs` hit it on its first real run. `record-demo.ps1` had the same bug latent in its no-`-Url` path since it was written -- it never showed up because every test run passed `-Url`. Both now resolve `../src/build-graph.mjs`, the way `refresh-graph.ps1` always did. |
| 2026-08-22 | **`scripts/smoke.mjs`: every automatable invariant in one command.** The documented failure mode of this repo is reasoning about the code instead of measuring it, and until now the measuring was something a person had to remember to do, one console command at a time. This builds to a **temp file** (never the vault snapshot, which a pre-push check must not rewrite), drives a real Chrome over the same `cdp.mjs` the demo uses, and prints the number it measured for each of 14 checks -- exiting non-zero so it can gate a push. Deliberately a real window, not headless: half the checks read pixels back out of a canvas and measure a laid-out sidebar, and a software rasteriser is not the thing shipping. It also states what it does **not** cover -- the resting lattice (needs the base radius and SP, which are not on the debug handle), per-frame animation steps (automation's frame pacing is not a person's), and anything about how it looks. |
| 2026-08-22 | **One of the new checks was vacuous on its first run, and that is the interesting part.** The nav-alignment check meant to test the count column at two tree depths; it cleared `state.collapsed` before clicking the twisties, which does not re-render, so the clicks toggled the groups **shut** and it measured the folded tree twice while reporting it had checked both. It passed. Now it clicks the twisties and nothing else, and asserts that the row count actually went up -- measured `folded 9 counts / 1 edge, open 24 counts / 1 edge`. A check that cannot tell whether it did anything is worse than no check, because it reads as coverage. |
| 2026-08-22 | **The legend opens folded to top-level folders.** It used to open with one level of subfolders showing, on the reasoning that that level is what the pie already draws as sub-wedges. On this vault that is **24 rows before you have asked anything**, and the names it is trying to show are the ones that get truncated. Now 9 rows, 0 subfolder rows, 5 twisties -- the tree is still one click deep, it just is not unfolded for you. `collapseAll()` has one definition used by both boot and `resetView`, so Refresh returns to the folded default rather than to fully open (verified: 9 rows -> 13 after unfolding 03 -> 9 again after Refresh). Applied once, guarded by a flag, because `__vg.relayout()` also calls `regroup()` and re-collapsing there would throw away whatever you had opened. |
| 2026-08-22 | **The demo unfolds 03 before highlighting People, through the twisty.** New `["twisty", g]` target. This is not decoration -- with the tree folded by default the `[data-hsub]` row the next beat clicks **does not exist** until this one has run, so the sequence the demo shows is the sequence a person actually has to perform. |
| 2026-08-22 | **Highlighted notes are bigger, and they get there by animating.** They were already scaled 1.3x, but that was room for the halo ring to sit outside the dot rather than the note growing, and it landed on one frame while the radial push beside it was tweened. `HL_GROW` adds 0.2 on top, so a highlighted note reaches **1.5x** -- measured 7.41 -> 11.12 -- and both factors ramp together over `TWEEN_MS`, deliberately the same duration as the push in `animateTo`, because becoming highlighted is one event that moves a note and grows it. At different durations the dot arrives and then keeps swelling. |
| 2026-08-22 | **The highlight ramp is PER NOTE, not one global scalar.** Highlights are additive -- two groups can be lit at once, a subfolder can be lit inside an already-lit group, and a heatmap hover swaps one whole set for another -- so a single ramp would pop the second set in at whatever value the first had left it, and could not fade one set out while another fades in. `hl[id]` follows the pattern `alpha` already establishes. Verified: ramp up `0.003 ... 0.968 1` and back down, and with 05 settled at 1 a fresh 01 correctly starts from 0.16. Everything about the treatment now keys on `hl[id]` rather than on `isHighlighted(id)` -- on the way out the predicate is already false while the ramp is still above zero, so keying the halo on it dropped the ring on the fade's first frame and left a shrinking bare dot. The ring is MIXED from the note's own colour instead of alpha-faded, so it emerges from the dot rather than ghosting over it and stays a solid hex, which is the only thing Sigma's colour parser is reliable about. Started from `afterRender` behind a signature of what is highlighted, so none of the six places that write the highlight set has to remember to kick it. |
| 2026-08-22 | **Hovering a note is animated instead of snapping.** It used to switch on one frame: everything else to `--dim`, the label with it. That is the same discrete step this project exists to remove from the layout, and it read the same way — as the disc flinching rather than responding. `hoverT` ramps over **150ms** (a third of the click tween, because this fires on every note the pointer crosses) and everything visible is a function of it: the dim, the lift (`HOVER_GROW` 0.45), the edge highlight, and the label, which waits until `hoverT > 0.5` so the box does not land on notes that are still at full colour. Wall-clock and frame-clamped like every other animation here, so a stalled frame stretches it rather than leaping. Mixing is sRGB, not OKLab: this runs per node per frame and both endpoints are known, so a perceptual path buys nothing visible and costs two cbrt per channel. Measured — ramp in `0.027 0.172 0.349 0.528 0.706 0.884 1`, out `0.98 0.776 0.687 0.509 0.331 0.153 0`, a distant node reduced to exactly `#2a2a28`, and the hovered note grown 4.45 -> 6.52. The hovered id is released by the tween **at zero**, not in `leaveNode` — clearing it there empties `focusSet()` on that frame and the fade-out is never drawn. |
| 2026-08-22 | **The demo hovers two notes, and the aim is now provably unambiguous.** Two bugs, both found by pointing at a note and asking what got hit. `graphToViewport` is **container**-relative, not page-relative: it returned (888, 607) for node 158 and pointing there hovered a different note, because the disc's container starts at (288, 155) — the sidebar plus the heatmap band. And the isolation margin was too small: requiring twice the note's own size let the daily note `2026-06-20` resolve to a spot covered by the bigger dot of `2026-W27` next door. The bound is now this note's radius plus the largest radius on screen, targets carry an `expect` the driver verifies after the dwell, and an unsafe target skips the beat rather than risking it. This is not fussiness: hovering **names the note on camera**, so a miss puts a title the storyboard never chose into the video — which is also why the beats aim only at daily notes and weekly reviews, both titled by date. |
| 2026-08-22 | **The demo's storyboard now covers hide, highlight, heatmap hover and `only` — and the recording has a real cursor.** Under `?demo` the page comes up **at rest** and the storyboard replays the intro itself by pressing Refresh, because a recorder cannot start before the page does: ffmpeg needs Chrome's window to exist and its rect to be readable first, ~2.8s, so an intro that began on load was most of the way through by the first frame — measured, the disc was already at 250 of 449 notes one second in. Two new target kinds: `["sub", "03/People"]`, resolved through `subOrder` because rows carry tint-slot indices rather than names, and `["busiest", n]`, ranked by each day's summed **alpha** so it can never land on a cell emptied by an earlier hide. Heatmap cells are painted rather than DOM, so they resolve to a synthetic rect. 14 beats, 28.4s. |
| 2026-08-22 | **A visible pointer, by moving the real one.** CDP input never moves the OS cursor, so the first recordings showed every effect and no arrow. `--cursor` starts `scripts/cursor.ps1` — one long-lived process fed `x y` on stdin, because spawning PowerShell per position costs ~80ms against a 16ms step and the glide would crawl — and gdigrab draws it with `-draw_mouse 1`. The two pointers cannot drift: both come from the same coordinates in the same loop, and the clicks stay on CDP so input still hit-tests and still works unfocused. Page origin on screen is asked of the PAGE (`screenX` + the halved horizontal inset), since only the page knows how much of its window is chrome. Off by default: it genuinely takes the mouse for the duration. |
| 2026-08-22 | **`?demo` runs a scripted walkthrough, and `record-demo.ps1` captures it to video unattended.** New. The storyboard is data returned by `demoMode()` in the template; the input is performed from outside by `scripts/demo.mjs` over CDP, so the page answers only *where is that control* and *are you still moving*. `el.click()` was rejected because it skips hit-testing — an in-page demo keeps passing after the button it aims at has become covered or 0x0 — and a drawn SVG cursor was built and removed (measured: it finished a run at (0, 847) instead of on the button it had just clicked at (42, 650), because rebuilding the legend makes Chrome re-dispatch a mousemove at the *real* cursor). Waits ask `__vg.demo.busy()` until quiet for 250ms, never a duration — same rule as `decisions/0003`. `scripts/cdp.mjs` is a hand-rolled RFC 6455 client, because Node 18 has no WebSocket and this repo installs nothing. See `decisions/0007`. |
| 2026-08-22 | **A 6.9s demo was producing a 39.2s video.** Each CDP command armed a 30s timeout that was never cleared, and an outstanding `setTimeout` keeps Node's event loop alive — so the driver process lingered 30s past its last command, and the recorder stops ffmpeg when the driver *returns*. 32s of every take was a still frame. Invisible from the logs, which reported 6.94s throughout: the process was finished, it just would not exit. Two more found the same way, by measuring the output instead of reading the code: `gdigrab -i title=` is an exact match and Chrome's window is `<page> - Google Chrome` (captures a `GetWindowRect` region now, which also survives the page renaming itself as its completion signal), and `ffmpeg -i <file>` reports to stderr and exits non-zero, which PowerShell 5.1 turns into a `NativeCommandError` on a recording that had in fact worked. Now 8.77s of video for 8.9s of wall clock, 263 frames. |
| 2026-08-22 | **Today is marked by an arrow in the right margin, on its ROW.** A ring alone could not do it: every ring on the band is `--today` and they differ only in weight, so today's was the weakest of three *and* disappeared entirely whenever the same cell was hovered or picked, since the three are one if/else chain. It had been sitting at 0.4 alpha and was invisible. The marker is now drawn unconditionally, outside that chain — verified drawn plain, while hovered, while picked, and with `mark today` on — and today's own ring goes to full strength at 1px against a selection's 1.5px. It points at the row rather than the column because the **column is a foregone conclusion**: `start` is `(cols - 1)` weeks before this Monday, so the trailing column is always the current week, and pointing at it says nothing. The row is what moves, so the arrow names the weekday too. A first version put a caret in the month strip above the column, which both said less and had to displace a colliding month label; all 12 month-opening weeks now label themselves. `HEAT_ARROW_W` is reserved out of the width before `heatGeom` sizes the grid, so the marker costs no column: canvas 805 = 796 grid + 9, still inside its box. |
| 2026-08-22 | **Nav counts line up, and every row can be soloed.** The `only` button existed on top-level folders only and sat *after* the count; it is now on every folder and subfolder row, between the name and the count, and the count is last with `min-width: 3ch` + tabular numerals. The fixed width is the mechanism, not the grid: grid columns align only *within* one grid and every row is its own grid, so a shared template would not have aligned anything. Measured after: **24 rows, 24 `only` buttons, 24 counts, one distinct right edge (266px)** — and the button is laid out at every depth with only its opacity changing on hover, so revealing it never shifts a count sideways. `only` on a nested row hides every sibling branch at every level, derived from the notes' own `dirs` chains rather than from the tree the legend drew (the legend only renders what is unfolded). |
| 2026-08-22 | **Palette slots 5 and 6 transposed: green into 5, magenta into 6.** On a PARA vault slot 5 lands on the daily-notes folder, which is one of the largest groups and owns one of the widest wedges — a wide magenta wedge beside the yellow of slot 4 read as a pink cast over a third of the disc. The token *values* swap rather than the slot order, so `slot N == --gN` stays true. Subfolder tints re-derive for free: `hueBudget` computes each family's rotation from the live palette's nearest neighbour, so nothing else needed touching. And to answer the question that prompted it — colours are **not** picked from neighbours. A group takes `THEME.slots[i]` by its index in *name* order, so a folder keeps its colour as the vault grows; only the subfolder tint budget looks at neighbours. |
| 2026-08-22 | **Heatmap band above the disc: notes added per day, each square pieced together from its own notes' exact colours.** New. `#stage` is now a two-row grid — band, then `#canvas` — rather than an overlay, so the disc is centred in what is left and the two cannot collide; `#canvas` becomes the positioning context for the logo, tooltip and detail card, because `graphToViewport` is relative to sigma's container and `#stage` no longer shares its origin. Weight is `alpha[id]`, the same source of truth the renderer reads, so the band densifies frame for frame with the intro and the timeline and needs no filter clause of its own; it repaints from `afterRender` beside `placeLogo`, guarded on a signature of the per-day counts quantised to a quarter of a note. One canvas, not 364 divs. Hover haloes that day's notes, click pins it — haloed and recoloured but **not** pushed radially, for the reason `isPushed` already documents for pooled subfolders: a day's notes are scattered across every folder, so pushing them slides a subset out *through* its cell-mates. Verified: **0 nodes move, 0 pushed, 14 haloed**, while `mark today` still pushes its 6. `checkPlanParity` and `checkZeroWeightInvariance` both still pass, with a folder hidden and at full vault. |
| 2026-08-22 | **Three encodings for the heatmap, two of them measured wrong before the third stuck.** *Averaging* each day's colours failed in both directions: mixing many hues in OKLab collapses toward grey, so the busiest day — being also the most mixed — rendered **duller** than a quiet single-colour day, 180 notes at OKLab L=0.713 against **L=0.781 for a 13-note day**, and with five quantile levels the count channel was flat at the top (every day from 8 notes up shared the last bucket) and overlapping at the bottom (1-note days reached L=0.47 while 2-note days started at 0.45). Worse, the mean is a colour no note has. *Sizing the square by the count* did work — strictly monotonic, **5.2px at one note to the full 13px at 180** — but a grid of squares that mostly do not touch stops reading as a calendar. Final: full squares, tiled one block per note in that note's exact colour. Verified **0 of 48 day-cells partially filled**. The cost is stated rather than hidden — two days with the same folder mix now differ only in grain, which below ~4 notes is nearly invisible; the exact number is in the tooltip. |
| 2026-08-22 | **Blocks inside a square are ordered by HUE, and the square is tiled exactly.** Folder order was the first key and it is the wrong one: the group palette is assigned in *name* order precisely so a folder keeps its colour, and name order is not hue order — measured on this palette consecutive folders run blue 264°, orange 42°, aqua 168°, so grouping by folder puts the three most distant hues on the wheel side by side. Hue angle, tie-broken by lightness (the axis the subfolder tints move along), makes each square its own small gradient. Verified on the busiest day: **180 blocks, hue-sorted 1° → 255°**. The tiling is `round(sqrt(n))` vertical strips each split into horizontal bands, which covers the square exactly at any `n` — a row-major grid leaves `ceil(sqrt(n))`'s last row part-empty, and a ragged corner reads as a different count. |
| 2026-08-22 | **The band crops to the weeks that fit instead of scrolling, and its size comes from a ResizeObserver.** Two separate reports, both real. Scrolling was wrong in the worst possible way: the grid starts at `scrollLeft` 0, which is the *oldest* end, so at 150% browser zoom a narrow viewport opened on eleven empty months with every note off the right edge — it was reported as a missing stylesheet, which is exactly how it looked. `heatGeom` now drops weeks before it drops pixels: columns are how many fit at the 7px floor (min 8), then the cell grows into what is there, capped at 13. Separately, a window `resize` listener was not enough — the band came up at the 7px floor **in a 1124px slot**, because boot ran before the embedded pane had settled and no resize event ever followed. A ResizeObserver on `#heatwrap` answers the question actually being asked and fires on its first observation, so the boot race and a later resize are one code path. `__vg.heatCell` is live, for the same reason `radialEase` is. |
| 2026-08-22 | **`created` is validated, not just sliced — 16 notes were ranked as the newest in the vault.** `fm.created.slice(0, 10)` on an unrendered Templater placeholder (`created: {{date:YYYY-MM-DD}}`) yields the string `{{date:YYY`, which is not a date but **sorts as one, after every digit**. So those notes sat at the newest end of the timeline's rank axis and would have grown the heatmap a column for a day that does not exist. `build-graph.mjs` now tests `^\d{4}-\d{2}-\d{2}$` and treats anything else as undated. Measured before: **894 valid, 16 placeholders, 6 genuinely undated** of 916. Also recorded why `created` is the right field for "added" at all: NTFS `birthtime` says **472 of 934 files were born today** (OneDrive re-creates on sync), and `mtime` shows **240 files on 2026-08-19**, which was the folder renumbering. |
| 2026-08-22 | **Inner-band stepping closed, incidentally.** The last open outlier -- a ~38 unit inner-band step when `05 - Weekly Reviews` arrives, noted below and never chased -- is gone, confirmed by eye rather than measured. No separate fix: the inner ring was never a special case. `share()` and `allocateBand` are each called once per band (`share(inner, "i")` / `share(outer, "o")`, and `[true, false].forEach`), so the integer group count and the `band[0]`-anchored leading offset were stepping there too -- and 05 arriving is precisely a group whose presence crosses in that band. Same defect, same fix. Worth recording because it is the fifth instance of one rule: **nothing in the chain from weight to position may step.** The others were a switched plan basis, a floored row coordinate, an integer group count, and an offset anchored on set membership. |
| 2026-08-22 | **One allocator. Three copies of the gap arithmetic reduced to one, and the last rotation jump with it.** There were three: `share()` in `buildWedgePlan`, the allocation in `ringsLayout`, and a `gapPlan`/`availPlan` pair that was **computed and never read** -- dead, deleted. `allocateBand(list, weightOf, seatsOf, opts)` now serves both live callers, with the genuine differences as explicit options (`subGaps`: the reference width spends none; `clamp`: only the rendered arc can overflow the circle; `totFloor`: the two floored the weight total differently, preserved exactly) so they read as decisions rather than as one copy having forgotten something. **Also fixes a second rotation jump**, reported as "toggling 03 makes 08 move slightly left on the last frame": the leading offset was `gap * groupPres[band[0].g] / 2`, anchored on whichever group is first in the sweep, so when that group's presence hit zero and it left `band` the offset jumped 0 -> gap/2 and the whole ring rotated. There is now one gap **before every group**, the leading one serving as the wrap boundary, making the placed total exactly `gap * nG` with nothing left over and every term continuous. Verified against a pre-refactor build of the same vault across five filter states: note counts identical, **radii bit-identical to 6 decimals**, and the only change a uniform **-1.0000 degree** rotation -- the constant half-gap, by construction. |
| 2026-08-22 | **The wedge-disable jump, actually fixed: the gaps that POSITION the wedges were the integer ones.** Two earlier attempts aimed at `share()` in `buildWedgePlan`, which sets the reference width the row counts are solved against -- not the rendered geometry. The spans come from `ringsLayout`, which had its own gap arithmetic: `nG` an integer count of the groups in `band`, `nSub = band.length - nG`, and `band` drops a cell the instant its `geom` falls below `1e-4`. So a fading group kept its gaps reserved all the way down and then, on the single frame its last note reached zero opacity, `nG` went 9 -> 8 and `nSub` 9 -> 6: **2deg + 3 x 0.3deg = 2.9deg redistributed across every wedge in one frame**, with no animation. Each group now spends its gap in proportion to its opacity (`geom / seats`), each non-first cell its sub-gap likewise, and every *placed* increment carries the same weight as the reservation so the wedges still fill the circle exactly. Measured with a static opacity sweep (immune to the rAF throttling that made frame sampling useless): the seated plan at alpha 0 and the settled plan now agree to **0 degrees and 0 units** -- and the fade itself moves ~1.5deg per step, 132.9 -> 134.5 -> 136.1 -> 137.9 -> 139.0 -> 139.4. |
| 2026-08-22 | **The small jump when disabling a wedge: the gap count stepped on membership.** `share()` took the total gap as `gapFor(n) * n` with `n` an integer count of the groups present. A departing group is present while it fades and absent at rest, so `n` dropped by one at handover, the disc gained a whole 2-degree gap to redistribute, and every wedge's share moved with it. Found by measuring **screen pixels** on the six outermost notes outside the toggled folder, after position, size, label, colour and camera ratio had all measured clean: `1965ms graph=0.2` -> `1982ms graph=33.1 screen=6.0` -> `1999ms graph=0.1`, one isolated frame *after* the animation had converged. `n` is now continuous -- each group contributes `min(1, its weight)` -- so a leaving group's gap closes over its final note. Tail now `6.9 -> 0.6 -> 0.4 -> 0.3 -> 0.2 -> 0.1 -> 0`, no spike. **This is not two planners**: there is one planner and one basis, and the two membership sets are irreducibly different; what was wrong is that a zero-weight member cost something. `__vg.checkZeroWeightInvariance()` now asserts it directly and would have caught this on day one. |
| 2026-08-22 | **`OBSIDIAN_VAULT` is honoured.** Vault resolution is now `--vault`, `VAULT_GRAPH_VAULT`, `OBSIDIAN_VAULT`, `--vault-name`, Obsidian's registry, then the walk-up -- tool-specific override first, then the machine-wide answer other tooling can share. Errors name which source supplied the bad path. Proved itself the same hour: the vault moved from `<vault>\<vault>` to `<vault>` and the build followed it with no edit, where the hardcoded default it replaced would have kept writing to a folder that is now empty. |
| 2026-08-22 | **The vault is discovered, not hardcoded — asks Obsidian's own registry.** The same vault sits on a different drive, path and user profile on each of the two machines, so the `<vault>\SecondBrain` default that briefly stood in for the old walk-up would have failed outright on the other one. `build-graph.mjs` now reads `%APPDATA%\obsidian\obsidian.json` (and the macOS/Linux equivalents), so it resolves the vault with **no arguments on any machine**. Order: `--vault`, `VAULT_GRAPH_VAULT`, `--vault-name`, the registry (sole entry or the open one), then the walk-up. Several registered and none unambiguously open lists them and stops rather than guessing. `refresh-graph.ps1` holds no default and no longer computes the output path either — it reads the builder's own `wrote <path>` line — so 'which vault' and 'where does it land' each have exactly one implementation. Verified all five paths, including a deliberately bad `--vault` failing loudly. |
| 2026-08-22 | **Converge before settling — the last frame is no longer a correction.** Easing closes only `RADIAL_EASE` of each note's gap per frame, so at progress 1 a remainder was still outstanding and `settle()` assigned exact targets, closing it in one frame: a small jump at the end, like a skipped frame. Reported as happening **only when toggling off**, and the measurement explains why — the hide tail's first frame carried **27 units** of remainder against the show's **7**, because hiding shrinks the ring so notes travel much further inward. The cascade now runs past progress 1 while any note is more than half a unit short, with the ease ramping to 1 across that tail; targets are static by then so it converges in a handful of frames. Measured per-frame max displacement of visible notes, hide: 27 → 22.3 → 12.8 → 4.6 → 0.8 → 0, show: 7.1 → 8.5 → 7 → 4 → 1.5 → 0.3 → 0 — smooth decay to zero in both, settle() a no-op. The ramp is deliberately confined to the tail: ramping across the whole animation would restore the row tick the easing exists to smooth, which for `04` lands around 90% of the way through. |
| 2026-08-22 | **One planner. `REPACK_BELOW` retired — planning no longer depends on how many notes were toggled.** The threshold switched the plan basis at 55% visible, and that was the root of the whole class: it made the same gesture behave differently by folder size (hiding `08` crossed the line and re-densified, hiding `04` did not, so one shrank the ring and the other held it), a toggle that *crossed* it animated from a whole-vault planA to a visible-only planB, and any call site hardcoding one side silently disagreed with the other. The plan is now always built from what is visible, at all five call sites. The failure that motivated the whole-vault side — notes appearing to swap seats on a light filter — does not apply, because a cell's notes are sorted by hubRank, so hiding some **compacts the rest inward in order** and nothing crosses anything. Behaviour is now uniform, measured on hide: 03 ring 2138→1818, 04 2138→1978, 08 2138→1658, 05 (inner) 2138→2138; **settle jump 0 and plan parity OK on all four**, worst single-frame step 40 — one RADIAL_EASE quarter-step of a row, rather than 04's old 160-unit snap after the animation had finished. Also adds `__vg.checkPlanParity()`, which asserts the static and live plans agree per cell, so this cannot drift again unnoticed. |
| 2026-08-22 | **The ring changed size AFTER the animation: the cascade animated a different PLAN than settle() assigns.** `ringsLayout` chooses between the whole-vault plan and a visible-only rebuild on `shown < order * REPACK_BELOW`, but the cascade hardcoded `onlyVisible = true` for both planB and every frame. So any filter light enough to stay above the threshold animated the visible-only packing and then settle() re-planned onto the whole-vault one — violating this file's own contract, "at 1, exactly the one settle() assigns". Measured with `04 - Daily Notes` hidden (393 of 450 visible, threshold 248): visible-only **16 cells / 64 rows / maxR 13**, whole-vault **19 cells / 75 rows / maxR 14**, every major cell differing by exactly one row (6 vs 7). One row is the jump. `08 - Meeting Notes` was never affected because hiding it leaves 229 visible, *below* the threshold, so both sides agreed by luck — which is why 03 and 08 looked fine while 04 did not. The cascade now computes the same flag settle() will (`visible()` is already at its post-toggle value on frame one) and uses it for planB and each frame. Verified, settle-time ring jump: **0 units on 04, 03 and 08**, with 03 still shrinking 320 and 08 480. 04 now holds its radius rather than shrinking and snapping back, which is correct: with seats kept its row count does not actually change. |
| 2026-08-22 | **`RADIAL_EASE` back to 0.25 — there were ticks to smooth after all.** Setting it to 1 assumed the continuous row coordinate left nothing to smooth, but `placeCell` floors that coordinate for the radius, so a cell's fractional row count crossing an integer teleports its outermost note a whole pitch. Swept against the in-page probe on the `04 - Daily Notes` hide, worst single-frame step of the outer band: **ease 1 → 160 units (one row), 0.5 → 80, 0.35 → 56, 0.25 → 40**, and on the show 30. The reason 1 was chosen — leftover lag closing in one go at the end — did not reproduce at any value: final-frame delta 0 and end-snap 0 measured on both directions. That failure was measured over 48 frames; a toggle now runs 62–123, ample to converge. Unlike taking the radius from the continuous coordinate (below), easing only moves notes whose target changed, so the disc stays on its lattice and only the crossing note glides. Exposed live as `__vg.radialEase` for re-sweeping. |
| 2026-08-22 | ~~The end-of-toggle jump was a row teleport: the radius is now continuous while animating.~~ **Reverted the same day — the cure was worse.** Taking the radius from the continuous coordinate does kill the teleport (04's worst step 160 → 2) but puts *every* note off-lattice on *every* intermediate frame, so the whole animation reads as a smeared disc — the same failure the row-count note describes at rest, traded from one bad frame to ~120 mushy ones. Superseded by `RADIAL_EASE` above, which smooths only the notes that actually crossed. The diagnosis stands and is worth keeping: measured on the 04 hide, **one −160 unit step in a single frame**, still exactly −160 in one frame at `timeScale = 4` (384 frames, every other step under 3 units), proving a discontinuity rather than pacing; 160 is one row of the outer band. Also retires the "toggling an inner group moves the outer ring" report — an 05 toggle measures the outer band at a constant 0 units of movement, so the bands were never coupled. |
| 2026-08-22 | ~~**The end-of-toggle jump was a row teleport: the radius is now continuous while animating.** A cascade walks each cell's row count as a REAL number, but `placeCell` took the radius from `Math.floor(pp)` — so as a fractional count drifted past an integer, the outermost note's floored row flipped and it teleported a full row pitch. The angular side of a crossing had been made continuous (the serpentine reverses, so `u` is smooth across it); the radial side never was, which is why the code claimed continuity it did not have. Caught by measuring in-page rather than reading: hiding `04 - Daily Notes` showed **one −160 unit step in a single frame at 1809ms**, and still exactly −160 in one frame at `timeScale = 4` (384 frames, every other step under 3 units) — proving it was a discontinuity, not pacing. 160 is one row: the outer band spans 1178–2293 and that identified it. `recs` now carries `row` (integer bucket, for the serpentine and row weights) and `rc` (continuous, for the radius), with `rc` collapsing to the integer at rest so the resting disc stays on the lattice — verified, resting radii still 2134 hidden / 2293 full. After: biggest single-frame outer step is 2 (04 hide), 6 (04 show), 6/25 (08), 4/13 (03), and **0 for 05 in the inner ring** — which also retires the "toggling an inner group moves the outer ring" report: the outer band measures constant at 2293 across an 05 toggle, so the bands were never coupled. Remaining outlier: 38 units in the inner band when 05 arrives, worth a look but ~1 inner pitch. |
| 2026-08-21 | **Pooled tail subfolders are no longer pushed — reverting the entry below.** `03 - Resources/Locations` is the case that decided it: 3 notes, seventh in the order, sharing the tail slot with six other folders, so highlighting it slid those 3 out through their cell-mates. The nav-consistency argument for including them does not outweigh the overlaps, and the ring already marks a pooled selection unambiguously. `ownsWedge()` is back to asking whether the sub owns its own tint slot — `k < SUB_NAMED`, plus the real exception that a tail slot with exactly one occupant is that folder's own wedge and still moves. Depth 2 and below was never pushed and is unchanged; the gate for that is `isPushed` reading `pathKey(a, 1)`, which was verified intact while chasing this. |
| 2026-08-21 | **Pooled tail subfolders are pushed radially now too.** `ownsWedge()` excluded them, which made the nav inconsistent — a folder in "N smaller subfolders" is still a level-1 subfolder, so selecting one did nothing while its named siblings stepped out. It costs a few transient overlaps, because pooled folders share a cell and their notes are interleaved with their cell-mates: measured 2-4 cross-collisions, worst 67 graph units (~half a dot, on 4 notes), only while that folder is selected; named subs and groups stay at 0. Grouping a pooled cell by subfolder to make the pushed run contiguous was tried and rejected — cross-collisions across the tail only fell 9 → 7, `04 Weekly Summaries` went 0 → 3, and it perturbs every pooled cell's resting position while costing the hubs-near-the-centre ordering. Depth 2 and below is still never pushed. |
| 2026-08-21 | **The folder tree is now fully dynamic — any depth, nothing hardcoded.** `build-graph.mjs` emits each note's whole chain below its PARA folder as `dirs`, and the legend renders it by recursion, so a folder five deep works like one a single level down on any vault. Filtering and highlighting are keyed by path *prefix*, so one code path serves every depth: `visible()` walks a note's ancestors, and hiding a folder hides its subtree without knowing how deep it is. Deeper levels take no tint and cut no wedge (they inherit the parent swatch — the pie does not distinguish them, so the legend must not pretend it does) but get their own eye and highlight; only depth 1 is ever pushed radially, since a deeper folder is interleaved with its siblings at the same angles. Verified: `People` 81 opens to Professional 72 / Personal 9, `00 1 on 1` 62 opens to its seven people, hiding `People/Professional` leaves 376 of 448 with the parent row still reading 81, and highlighting it halos 72/72 with 0 radial movement. |
| 2026-08-21 | ~~Subfolders now go two levels deep.~~ **Reverted the same day — it was the wrong shape.** Walking two levels into `sub` itself *replaced* `00 1 on 1` with seven flat siblings, so the folder vanished as a unit and its people sat beside `2026-06` as if they were its peers rather than inside it. A deeper level is a child, not a substitute — and the depth should not have been a fixed number either. Superseded by the dynamic tree above. |
| 2026-08-21 | **Subfolders now go two levels deep.** `paraSub` read only the immediate child, so `08 - Meeting Notes/00 1 on 1/<person>` collapsed 62 notes into one wedge — hiding who the 1-on-1s are with, which is the only interesting division that folder has — and `03 - Resources/People/{Professional,Personal}` collapsed 81 the same way. Measured, **136 notes** sat in meaningfully-named second-level folders being flattened. A date bucket stops the walk, on the reasoning `inferType` already uses: as the first level it is the only division a folder has (Daily Notes), deeper down it is noise under a real name, so `03 Sprint Reviews/2026-06` stays `03 Sprint Reviews`. Backward compatible — every existing sub resolves unchanged. Cell count stays 19 and collisions stay 0, because cells are keyed by tint slot and extra subfolders flow into the shared tail; the seven people are reached by unfolding "16 smaller subfolders", verified end to end (hiding hiding one person's subfolder hides exactly that person's 16 notes). Only three subs are ever *named*, deliberately: the tint ladder has four steps because nine tints in one hue family land ~3 dE apart, so naming more than the pie distinguishes would misrepresent it. |
| 2026-08-21 | **Killed the hard seam down the centre of the logo: a conic gradient's centre is a singularity.** Every angular stop converges on one point, so a transition ~29px wide at the rim is 0px wide in the middle — the mark kept a hard cyan/amber edge down the fissure no matter how wide the angular blur got, because blending is measured in degrees and degrees are worth nothing at r=0. The centre now stops using angle: a radial core in the palette's own **mean** is laid over the conic, solid to 9% and gone by 34% (small on purpose — past ~30% the arcs blend on their own, and a bigger core washes the hue variation out of the middle). Save PNG paints the same core so the export matches. The faint line left is the artwork's own hemisphere divider, not a gradient edge. |
| 2026-08-21 | **The inner ring no longer teleports outward when every outer folder is hidden.** A tiny-vault guard (`if (!outer.length)` → move all cells to the outer band) sits immediately after `bandLock` is applied and silently overrode it: `!outer.length` is also true when *filtering* has emptied the outer band, so on the one frame the last outer note stopped being `present()` the whole inner ring jumped to the outer base radius. Measured, `01 - Projects` went r=479 → r=1175, a **696-unit jump at frame 206 of 210**, with the outer band already still — which is why it read as the inner ring jumping at the end rather than as part of the fade. Now gated on `!bandLock`, so it only fires before the lock exists; an empty outer band is a legitimate state (the disc is just the inner ring, small). Re-verified by stepping whole cascades frame by frame: hiding all outer groups (210 frames) and hiding one (126 frames) both move the inner band by 0 at every frame, settle jump 0. The resting layout was never wrong, which is why before/after measurement kept clearing it — and stepping a cascade needs `requestAnimationFrame` *intercepted and pumped*, not waited on, which also works in a tab that is not compositing. |
| 2026-08-21 | **The palette handover is now a share of the band, and eased.** The first ramp was 12 notes' worth of alpha, which on a 418-note outer band covered the last **2.9%** of the fade — continuous on paper, still a snap on screen. `BAND_HANDOVER` is now a *share* (0.5, so the ramp spans the last half of the fade; 0.25 still piled 92 and 93 into the final two tenths against a ~35 median) and the ramp is **smoothstepped**, because a linear one kinks where it meets the knee: the rate of change goes nothing-to-full in a frame, which is a visible event even when the value is continuous. Measured, `t` runs 1 · 0.97 · 0.90 · 0.79 · 0.65 · 0.50 · 0.35 · 0.22 · 0.10 · 0.03 · 0, peak per-step colour change is 1.65× the median (was ~2.6), the last five steps decelerate 33 · 31 · 26 · 17 · 7, and it lands exactly on the pure-inner reference. |
| 2026-08-21 | **The outer→inner palette handover is a ramp, not a switch.** `outer \|\| inner` repainted the whole mark in one step on the frame the last outer note went invisible, and in two-ring mode dropped the core layer at the same instant — two jumps at once. `outerPresence()` is now the outer band's summed *opacity* over `BAND_HANDOVER` (12 notes), clamped 0..1, and the palettes are mixed per bucket by it, so the handover rides along with the cascade that causes it. Measured stepping the outer band down: base average goes amber → 205,194,94 → 162,179,98 → 116,158,97 → 70,137,96, landing exactly on the pure-inner reference; pinned at 1 above 12 notes, so ordinary filtering never tints the mark. The core layer now shows whenever the inner band has notes — both layers converge on the same colours, so duplication is free and the second jump goes. Added `placeLogo` / `ringColors` / `bandColors` / `outerPresence` to `__vg`, because `refresh()` only schedules a render and a non-composited tab never runs one, which had made an earlier measurement of this silently read a stale DOM. |
| 2026-08-21 | **Logo colouring: outer band only, blended harder, plus a two-ring option.** The inner band is no longer sampled — it crowded the mark with hues from an annulus barely wider than the logo, and both bands competed for the same angles — except when the outer band is empty, where the inner ring *is* the disc and supplies the paint. `LOGO_BLEND_BUCKETS` 2 -> 5, so each boundary spreads ~27.5° and nearly every bucket ends its own colour (103 stops, 97 distinct); at 2 it still read as wedges with soft joins rather than a wash. And a **Two-ring colouring** switch under *Logo*: the inner band's palette paints a 40% core and dissolves outward into the outer band's, as a second layer masked by `url(mask), radial-gradient(...)` with `mask-composite: intersect` — no clipping wrapper, and both layers stay in registration by sharing a `mask-size`. Skipped when the inner band is empty or already painting the base. Save PNG composites both layers, re-applying the radial fade as a second `destination-in`. |
| 2026-08-21 | **The centre logo is now a MASK painted by the ring's own colours.** New white-on-transparent art is used as an alpha mask, and what shows through it is a conic gradient built from the disc: `ringColors()` samples the outermost note at each of 144 angular buckets. So the mark carries the same hues in the same directions as the wedges behind it and needs no copy of the palette — verified angularly (45° = `People`'s orange, 135° = `2026-06`'s pink, 180° = the `1 on 1` teal, 315° = Sprint Reviews cyan), and reactively: flipping the theme repaints every stop and returns exactly, while hiding `08 - Meeting Notes` removes its teal from the mark entirely as its neighbours grow into the vacated angle. Boundaries are softened with a circular box blur over the buckets (hard edges read as a pie chart pasted inside the mark) — 222.5° of the circle stays flat, 137.5° ramps, no ramp wider than 15°; an early attempt at merging repeated stops smeared one change over 47.5° by ramping across whole wedges. Two sources now: `logo-mask-source.png` (white, for the centre) and `logo-source.png` (colour, for the favicon, which has no disc to borrow from). Save PNG rebuilds the mark by hand — a CSS-masked element cannot be `drawImage`d — painting the same buckets as canvas wedges and punching them to the mask with `destination-in`, sharing `ringColors()` so the two cannot drift. Page 644 KB -> 610 KB, since monochrome art compresses better (40 KB vs 73 KB). |
| 2026-08-21 | **The toggling wedge now keeps the ring's depth while its arc closes.** A cascade walks each cell's row count from its source packing to its destination, but the cell being toggled exists in only one of those. Mirroring the other end (`s = d`, `d = s`) pinned its count, so the disc re-densified around a wedge that held its radii and faded at full size. Walking it to 0 fixed that and overshot — it contracted faster than the ring (1494 against the survivors' 1814 at 75%), reading as sinking out of the disc. A missing end now takes the band's own depth, i.e. the deepest cell in it. Measured leaving `08 - Meeting Notes`, leaving/staying depth is identical at every frame — 7, 7, 6, 5 rows (2134, 2134, 1974, 1814) — while its arc closes 182° -> 158° -> 125° -> 76° -> 0 and the disc contracts to 1654, exactly the post-toggle resting radius. Arriving mirrors it frame for frame. Animation-only: `rowsAt` runs solely in the cascade loop, so the resting layout and its verified invariants are untouched. |
| 2026-08-21 | **Rows now continue across sub-wedge boundaries: notes are centred WITHIN their row.** The wide gap between sub-wedges was never a gap problem — it was the row *phase*. `u` was the fractional part of the continuous row coordinate, i.e. wherever the cumulative sum crossed an integer, so a note could land hard against a wedge edge with only the angular gap beyond it (measured: pairs 17–67 graph units apart where two dots need ~126). Neither easy fix worked: 2 degrees of gap only reaches 46 units, and re-measuring with the dot cap at 11/10/9/8 left the same pairs colliding, so it was never a size problem. `placeCell` now runs two passes — rows assigned as before, then each note positioned by its own half-share of its row's weight — which yields half-step margins at both ends for free. Measured on the innermost outer row: steps within a sub-wedge median 172, **across a sub-wedge boundary median 162**, across a group boundary 208 — so sub-wedges read as continuous while groups stay separate. `SUB_GAP` 0.6 -> 0.3 degrees, now purely a legibility choice since 0 degrees also measures clean. The edge inset is retired (both constants 0), returning the disc from 2294 to 2134. Verified: restore-after-hide drift 0 units on three folders, 0 of 444 notes off-lattice. |
| 2026-08-21 | **Pooled sub-subfolders are no longer pushed radially — they only get the ring.** Cells are keyed by tint slot, so the "N smaller subfolders" share ONE wedge with their notes interleaved at the same angles; pushing one of them slid a subset out through its own cell-mates and the highlight itself created the overlaps. `isPushed` now asks `ownsWedge()`, so groups and *named* subfolders still move as blocks while pooled ones are identified by the halo alone. Measured: `Partners` (named) moves 144 and rings 16/16; `Rezepte` (pooled) moves 0 and rings 14/14; 0 collisions in every state. A tail slot with a single occupant does own it, and still moves. |
| 2026-08-21 | **Innermost-row crowding fixed, and the inset retuned down.** The crowding was self-inflicted: capacity is "one note per row-pitch of arc", so insetting the placement without telling the row count handed the innermost row more notes than it had room for. `usableRef` now solves the count for the inset span. Then the pad itself was swept and cut from 70/0.22 to **35/0.10** — the first guess cleared collisions but pushed narrow cells 8 rows -> 11 (the sparse-spoke failure mode) and added 480 units of raggedness, where 35/0.10 clears them at the *baseline* 800-unit spread, i.e. for free. `SUB_GAP` 1 -> 0.6 degrees; with the inset doing the separating, even 0.3 measured clean, so 0.6 is legibility not geometry. Disc 2134 -> 2294 (+7.5%). Added live `subGap`/`edgePadArc`/`edgePadMax` setters plus `relayout()` on `__vg`, which is what made the sweep possible. |
| 2026-08-21 | **Sub-wedges no longer overlap: notes are held off their wedge edges.** The 1 degree gap could never have fixed this alone — `u` is the fraction across a row, so a row's first and last notes sit *on* the wedge edges, and the gap's arc at r=1334 is 23 graph units against the ~156 two max-size dots need (2 degrees would reach 46). Notes are now inset by an absolute arc expressed as a fraction of the cell's fixed reference width, capped at 0.22; the remap is affine, so the serpentine stays continuous across a row tick. Measured across four window sizes: **0 cross-wedge collisions at every one**, against `Partners`/`People` sitting 6.9px apart before. Got the units wrong first — `base` is in layout units and `EDGE_PAD_ARC` in graph units, so without `* UNIT` every one of the 19 cells pinned to the cap and squeezing each wedge into 56% of its span traded a few edge collisions for 29 interior ones. Right units: pads spread 0.05–0.22, only 5 of 19 capped. |
| 2026-08-21 | **Subfolders are highlightable too, and selection is marked in the nav.** Both levels now carry their own eye and their own highlight, stored as `highlight` (by group) and `highlightSub` (by `folder/sub`) behind one `isHighlighted` predicate. Verified: highlighting `Partners` pushes its 16 notes out 144 units and rings all 16 while its sibling `People` moves 0. Selected rows are marked in three channels — fill, border, and an inset leading-edge bar — because a fill alone is easy to skim past on the smaller indented rows. The "N smaller subfolders" row reads as selected only when all of its subfolders are, so a partial selection below does not light it up, and clicking it toggles the block. |
| 2026-08-21 | **One level of subfolders now shows by default** (state flipped from `expanded` to `collapsed`). That level is what the pie actually draws as sub-wedges; the earlier folded-by-default choice buried it. The tail row's contents stay folded, since they all share one tint step. |
| 2026-08-21 | **Groups navigation rebuilt: eye icons, folding subfolders, and highlight instead of hide.** Each row is now three controls — an eye for visibility, a twisty for unfolding, and the label for highlighting — because one click could only ever mean one of the three. Clicking the label used to hide the group, which is the wrong verb for "where is this?": seeing one folder meant hiding the other eight, reflowing the disc into a different picture. Highlight pushes a group out by `HL_PUSH` (0.9 rows) and rings its notes with `createNodeBorderProgram`; verified as a pure display offset, with every other group moving 0 / 0 units and the pushed group moving exactly 144 (0.9 × UNIT). 0.9 rows is sized against the ~6% of slack that already exists between the pinned box (1.02) and `fit()` (1.08), so the resting disc did not have to shrink to pay for it. Subfolder rows fold by default, and the "N smaller subfolders" row has its own twisty so the tail can be listed — verified unfolding `03 - Resources` gives 4 rows and its tail 7 more, summing to 28. |
| 2026-08-21 | **"Mark today" was a no-op most days; it now reads file mtime too.** It matched frontmatter `created`, but this vault pre-creates daily notes from the calendar, so today's note carries `created: 2026-08-17` and `created` outranks `date`. Measured on 2026-08-21: **0 notes created today against 3 files touched.** The extractor now also emits `touched` (mtime), and "today" means created *or* edited today — which found the 2 notes actually worked on. It also gets the same treatment as a highlighted group now (pushed out and ringed) rather than only a colour swap, which was easy to lose among ten group hues. mtime is honest but not pure: bulk operations rewrite it wholesale (2026-08-19 shows 111 files), which is why both fields are kept. Also wired `touched` into the graph's node attributes — the field reached the data and stopped there, so the first build silently still matched nothing. |
| 2026-08-21 | **Dot sizes now scale with the row pitch on screen, fixing overlap on smaller displays.** Node sizes are screen pixels fixed at build, but the disc's pixel radius follows the stage, so the radius-11 cap — tuned against ~28px of pitch on a 1068x1270 stage — was calibrated against something that moves. On a 1080p screen (720px of stage) the pitch is 22.6px and 22px dots touch: measured 3 pairs, worst overlap 9.8px. `sizeScale = pitch / REF_PITCH`, capped at 1 and floored at 0.45, restores the tuned ratio exactly (max radius 8.87 at 22.6px pitch = 0.393, against the original 11/28 = 0.393); overlap fell to 2 pairs and 6.6px. Hooked to `afterRender`, not just the camera: `fit()` animates for 380ms, so measuring right after calling it read the pre-animation ratio and pinned the scale to its floor, drawing every dot at 4.95px. The 2 remaining collisions are sub-wedge boundary artifacts at equal radius, not size — `SUB_GAP` is angular and 1 degree is only ~3.3px of arc at r=1334. |
| 2026-08-21 | **A logo in the hub hole, and the same art as the favicon.** A neon brain — itself a node-link drawing — inlined as base64 at build time so the page stays self-contained. Three files because the build cannot make its own derivatives: `logo-source.png` (the 1254² original, kept here so it syncs), the committed `logo.png` / `favicon.png`, and `make-logo.ps1` to regenerate them, since `build-graph.mjs` is node-builtins-only and node has no image decoder. The source is cropped to its alpha bounding box first — the art fills only 73% × 67% of its frame, and base64 puts every byte in the HTML at 4/3 size, so cropping buys real quality per byte (224² would have cost 132 KB against 192²'s 99 KB). Sized from a measurement, not by eye: the hole is 180px across on a 1016px stage, the logo draws at 0.5 of that. Placed by projecting (0,0) and (r0·UNIT, 0) through the camera rather than by centring in the stage, because the hole's pixel radius scales with zoom. The `<img>` precedes `#graph` so the canvases paint over it, leaving room for hub notes to draw on top. Page 494 KB → 609 KB. |
| 2026-08-21 | **1 degree gaps between sub-wedges** (`SUB_GAP`), against 2 degrees between groups — the group boundary stays the more prominent one on purpose, or the disc reads as a flat ring of subfolders rather than folders containing them. The gaps come out of the total before the spans are shared, never added after, or the ring overlaps itself; they are counted only from cells with weight, so a hidden subfolder spends nothing. Verified both bands close at exactly 360.0000° with 0 overshoot, spending 14° each (inner 6×2° + 2×1°, outer 3×2° + 8×1° = 3.9% of the circle). A clamp shrinks both gaps together if a cell count ever makes them exceed 45% of the circle. |
| 2026-08-21 | **Fixed curved edges going straight once the intro finished.** Curvature is a function of node POSITIONS, and `timelineFrame()` refreshed with `skipIndexation: true` on every frame including the last — so the GPU kept whatever curvature was in the edge buffers when they were last built. On the `until = 0` frame that is almost nothing: measured, exactly **1** of 1419 edges has display data there, the rest being hidden at zero opacity. Nothing ever re-uploaded it, so the finished disc drew straight lines while the edge reducer still reported `type: "curve"` — which is why it was invisible to measurement. `timelineFrame(full)` now re-indexes on frames that LAND (end of playback, jump to "all", slider release) and stays cheap while scrubbing, mirroring what the cascade already did in `settle()`. After playback: 1419 edges, mean \|curvature\| 0.186, max 0.547. |
| 2026-08-21 | **Curved edges are now the default**, with the switch turning them off rather than on. 91% of links cross the disc, so straight lines are the case that needs the excuse. The switch stays out of Refresh's reset: it changes how the canvas is drawn rather than what it shows, so it persists as a preference. |
| 2026-08-21 | **Removed the *Min. links* and *Labels (hubs)* sliders.** Neither was being used: labels are carried by hover, click and search, and folder toggles do the filtering that Min. links was for. Dropping them took `state.minDeg` and `state.labelCount` with them, one clause out of `visible()` and one out of `nodeStyle()` (which now clears `label` outright, so nothing ever reaches Sigma's label grid to be thinned arbitrarily), and `buildSliders()` entirely. `hubRank` stays — it still orders notes best-connected-first inside a cell and orders the unlinked notes in the hub hole. Sidebar is now Search / Timeline / Groups / Edges / View. |
| 2026-08-21 | **Added a "curve edges away from hub" toggle.** Only 9% of links stay inside one folder, so most of the 1419 of them cross the disc -- and as straight chords they wash the whole interior grey. Curved, the dead centre clears completely and the haze concentrates into a swirl just outside the hub. It is a genuine trade rather than a strict win (the inner ring still sits in that swirl), which is why it is a switch and not a replacement. Bow magnitude is scaled by how close the chord would have passed to the centre, squared, so short intra-wedge links stay near-straight and only the long crossings sweep round; the sign is chosen per edge so the bow always points outward. Uses the `EdgeCurveProgram` already vendored in the Sigma bundle. |
| 2026-08-21 | **Fixed the timeline animation running in the wrong fill mode at random.** `fullRing` -- trade mode vs draw mode -- was written in exactly one place (inside `cascade()`) and read in another (inside the packer), and `timelineFrame()` never set it. So Play inherited whatever the last cascade had left behind: `false` after a fresh load, giving the pie drawn clockwise in wedges, and `true` after any legend toggle, giving the ring. Same button, two animations, and which one you got depended on whether you had touched a filter first. The timeline is a density animation by construction, so the mode is now a constant there. Detecting it from `present()` would have been actively wrong, not merely absent: the timeline empties the screen itself on its first frame (`until = 0`), so an honest check picks draw mode every time. |
| 2026-08-21 | **Refresh no longer navigates, and the intro is now the timeline.** Refresh used to `location.replace()` its own URL with a cache-busting `?t=`, which is unreliable on `file://` -- the one protocol this page runs on -- so the click could do nothing at all, which is how it was reported. It never bought anything either: the data is baked in, so a reload can only redisplay the same bytes unless `refresh-graph.ps1` has rewritten the file, and picking up new notes is that script's job anyway. Refresh now resets every filter in state *and* in the controls that show it, then replays the intro. First paint plays the same timeline growth instead of `clearAlpha()` + `cascade()` (which on an empty screen meant draw mode), so load, Refresh and Play are one animation rather than two that were easy to mistake for a bug. |
| 2026-08-21 | Documented the `--flat-months` flag, which the Flags table had never listed, and remeasured the counts through the note (441/1409 -> 443/1419). Fixed `RENDERING`: the Sigma programs hang off the UMD namespace object, not off the class `SigmaCls` resolves to, so `SigmaCls.rendering` is `undefined` and reaching through it throws inside the init `setTimeout` -- which presents as a page stuck on "Laying out graph..." with an empty console. The doc's own warning about `Sigma.rendering` meant the namespace; this is the same trap one level down. |
| 2026-08-20 | **Added the timeline scrubber and a "mark today" toggle.** Play grows the vault from its first note to now, reusing the cascade's opacity model -- wedges widen and the disc densifies as it goes (outer radius 1174 -> 2134 across the sweep). The slider is linear in note count rather than in time, because 409 of 442 notes fall in the last three months against dates reaching back to 2015; a time axis would waste 97% of its travel. It immediately showed that slider positions 120-260 span three days, i.e. the 140-note initial import. Mark today colours every note created today (11) in a non-categorical colour, with the date read at load so it stays correct tomorrow. |
| 2026-08-20 | `INNER_SCALE` 0.9 -> 0.8. Measured on the 26 inner-band notes: minimum gap 27.1px against a largest node radius of 11 (22px across), 0 touching pairs. That is the edge of the 23-27px band the radius cap assumes, so further shrinking would mean revisiting the cap. |
| 2026-08-20 | Inner ring drawn at 90% of its packed radius (`INNER_SCALE`), a pure size trim with proportions and spacing intact (173-unit nearest-neighbour against a 160-unit row pitch). Confirmed the pinned-box centring is already dynamic: after a resize from a 1068x1270 stage to 612x620, the disc centre still lands on the stage centre exactly. |
| 2026-08-20 | **The ring centre no longer moves: Sigma's `autoRescale` is pinned with `setCustomBBox`.** Sigma renormalises against the graph bounding box on every refresh, so a shrinking disc moved the graph origin 13px and zoomed 8.2% with the camera untouched -- every frame of a cascade. It scales about the centre, so the artifact grows with radius, which is why the inner ring looked silky and the outer ring jumpy. Origin drift is now 0px and scale drift exactly 1. Also: panning and rotation disabled, camera x/y pulled back on zoom so the disc is always centred on the stage (verified offset 0,0), Fit is a zoom reset, and `SPREAD_MIN` raised to 24 so a mid-sized folder's wedge does not close at ~6 degrees per frame. |
| 2026-08-20 | **The whole-disc snap at the end of a cascade is fixed: the resting plan is now weighted by visibility.** A cascade weights the packer by opacity, so a hidden folder shapes nothing; `settle()` then rebuilt the plan with every note at full weight, so the resting packing was the full-vault one and every survivor snapped back. It only showed on toggles that stay above `REPACK_BELOW` -- which is why `08` (221 of 442, crosses it) looked perfect while `04`, `05`, `03` and `02` jumped. Cascade-final vs settled layout: 0 units on every toggle. Penultimate-frame spikes 881 -> 213 and 1881 -> 323. Added `TIME_SCALE` / `__vg.timeScale` for slow-motion inspection, which is what isolated it. |
| 2026-08-20 | **Fixed two end-of-animation jumps unrelated to the packing.** The frame guard was a fixed timeout assuming 60fps, so on a page that renders slower it fired mid-animation and snapped the disc to its final layout; it is now a watchdog that only fires after 400ms with no frame, and the cascade skips re-indexing 442 nodes and 1409 edges every frame. And `RADIAL_EASE` (0.3) was pure lag now that the row coordinate is continuous -- the disc contracted 2133 -> 1731 across 48 frames and then 1731 -> 1653 in the last one; at 1 it follows exactly. The big toggle's last five frames now move 166, 165, 32, 58, 0. Still spiking on the penultimate frame: `05 - Weekly Reviews` at 881 and `03 - Resources` at 1881, not yet isolated. |
| 2026-08-20 | **Row count back on the live span, with placement as one continuous formula.** Locking it per folder was stable but degenerate -- isolating a 55-note folder drew a scattered cloud across 7 sparse rings instead of a ring. The row coordinate now comes from inverting the cumulative capacity (`base*x + SP*x^2/2`), so a fractional count slides instead of binning, and the serpentine keeps u continuous across a row tick. Replaces the two-grid blend that swung notes 1996 units in a frame. Resting disc: 0 off-lattice rows of 91; settle jump 0; big-group worst frame 359-380 (median 221-234). Still jumpier: an inner-ring group at 912 and `03 - Resources` (cells in both bands) at 1881. |
| 2026-08-20 | **Row capacity is now proportional to radius with no floor**, fixing the wonky columns that appeared once the reference width was locked: the floor bound on narrow cells' inner rows, so they held 1, 1, 1 then many. Notes per row now rise smoothly (the 81-note People cell: 8, 10, 10, 12, 12, 14, 15) and the irregularity measure for the affected cells fell from 2.42 to 1.13-1.66. Also unfloored in the row-count function, where the same clamp made a 19-note cell ask for 8 rows. Stability is unaffected -- a constant factor on capacity cancels in `cap/capSum`, so the note-to-row assignment never depended on the span. |
| 2026-08-20 | **A folder is packed from its own weight only.** The reference wedge width that row counts and capacities derive from was a live share of the band's total weight, coupling every cell to every other: hiding one folder re-packed all the rest, which was the source of the remaining jumps. It is now fixed at load per cell, so a folder's rows depend only on its own weight, while live angular spans still keep the circle full. Other folders' notes now shift radially by 0 on any toggle; worst single-frame movement 858 -> 119 (inner ring) and 1494 -> 237 (`03 - Resources`, the both-bands case). |
| 2026-08-20 | **Row count is walked by animation progress, not derived from the live weights.** Deriving it produced a fractional count at rest, so the resting disc was a smear of two grids one row apart, and the animation ended on a blended grid and then snapped to a crisp one. It now runs from the source packing's count to the destination's, hitting both exactly. Resting disc: 0 off-lattice rows of 98. Jump at settle: 0 on every toggle. |
| 2026-08-20 | **Removed every remaining step between weight and position**, which is what the jumps were. Row assignment is a continuous function of cumulative weight rather than a per-row quota; `u` is shared by weight rather than by count; rows serpentine so crossing a row boundary is a short move rather than the width of the wedge; and the row COUNT is fractional, with the placement interpolated between the two adjacent grids. Worst single-frame movement: 3191 -> 325 units closing the big group, 2970 -> 372 opening it, 856 -> 103 for an inner group. |
| 2026-08-20 | **The rings no longer affect each other's packing.** The hub radius was solved from the global weighted total and the outer band's base radius from the inner band's row count, so enabling or disabling anything in one ring re-packed the other. Both are now locked at load alongside the band split. Measured: hiding an inner group moves the outer band by 0, hiding an outer group moves the inner band by 0, and restoring is exact. |
| 2026-08-20 | **Density is now interpolated by weighting the packer with opacity**, replacing the blend of two finished packings (which pulled the rows apart) and the earlier attempt to switch the repack off (which left a filtered disc far too loose). Notes appear radially, a row at a time at the ring's current density, while their wedge widens circumferentially -- one calculation. **The two bands are also independent now:** which ring a group lives in is locked at load, so filtering can never make a group jump between rings; letting it follow the weights had flung a fading group into the inner ring across ~300 degrees. `REPACK_BELOW` back to 0.55. |
| 2026-08-20 | **`REPACK_BELOW` lowered 0.55 -> 0.3.** At 0.55, hiding the biggest group (~50% left) triggered a re-densification from 8 rows per cell to 5, moving every surviving note radially; since `r` and `u` are lerped independently across the two packings, the rows smeared rather than re-densifying. 221 notes over 8 rows is ~28 per row, so there was nothing to fix -- the spidery view the repack exists for was measured at 55 notes (12%). Toggling a big group is now purely angular: survivors' radial shift frame-steps at 0 throughout. |
| 2026-08-20 | **Vault-agnostic discovery and typing.** Removed the hardcoded folder prefixes: the crawl now walks the whole tree, reads the templates folder from `.obsidian/templates.json` (and Templater's config) and the daily-notes folder from `.obsidian/daily-notes.json`, and derives a note's type from the deepest meaningfully-named folder, skipping date buckets (`2026-08`, `2026-Q3`, `2026-W34`). The old map broke when the vault was renumbered -- `06` had become Monthly Reviews, so monthly reviews were typed `daily` and the `05 - Templates` skip matched nothing -- and would never have worked on another vault. <!-- path-check: ok --> |
| 2026-08-20 | **Overlap between the toggled wedge and its neighbour is fixed properly: cascades now interpolate the PLAN, not positions.** Blending two separately-valid full-circle allocations never yields a valid one in between (they disagree by mean 23.7 / max 138 degrees), so the survivors sat mid-arrangement and the wedge overlapped its neighbour by ~45 degrees. One blended plan per frame means angles are allocated once, in a single sweep, so overlap is structurally impossible. `buildWedgePlan` stores a cell key so cells can be matched across packings. Verified 0 intruding notes at every frame in both directions. |
| 2026-08-20 | **A closing wedge no longer lags the ring.** `ringsLayout` falls back to a note's current position when a plan gives it no seat, which hid "no seat" from the cascade -- departing notes were interpolating toward where they already were, so they never migrated radially while the ring re-densified around them (frame-stepped: ring 0.04 -> 0.90, wedge 0.000 throughout). It now takes a `strict` flag, and one-sided notes get a counterpart synthesised from the median radius ratio between the two packings (0.768 here). Ring and wedge now agree to 0.000 every frame. |
| 2026-08-20 | **The repack now rides along with the fade** instead of bracketing it. Crossing `REPACK_BELOW` moved 220 of 221 survivors radially (mean 371 units, max 655; the disc goes 2292 -> 1637 and back), and animating that separately gave three movements in a row -- reflow, fade, repack. The cascade now interpolates between the pre-toggle and post-toggle packings by overall progress, in polar space, so the other wedges resize across the whole time the big wedge arrives or leaves. Verified jump-free at both ends. |
| 2026-08-20 | **Big groups no longer vanish instead of closing.** Below `REPACK_BELOW` the plan is rebuilt from visible notes only, so hiding a group that crossed the threshold left its departing notes with no slot: nothing held their space and they faded at stale coordinates. `08 - Meeting Notes` (221 of 442 notes) hit this every time. A cascade now plans over the union of staying-and-leaving notes, tweens into that plan first if a toggle crosses the threshold, and tweens the re-densifying repack afterwards instead of snapping it. |
| 2026-08-20 | **Enabling is now the mirror of disabling.** An arriving note claimed a whole slot of geometry up front while rendering at zero width, so the ring carried a hole the size of the incoming group (~150 degrees for `08 - Meeting Notes`) that the dots popped into. Arrivals now ramp their geometry with their opacity, so the other wedges shrink at exactly the rate the new one grows: the circle stays full and only changes density. First paint still draws the pie clockwise from nothing, since there is no ring to trade against. |
| 2026-08-20 | **Wedges close instead of vanishing.** Hiding a group turns `visible()` false on the first frame, so allocating geometry from visible notes alone struck the cell out of the allocation instantly -- its neighbours snapped wider while its own notes were still fading in place on top of them. Cells now hold room for anything still on screen, so a hidden wedge's share decays with its notes: measured 152.5 -> 132.9 -> 42 -> 0 degrees over ~950ms, with no gap wider than 8.9 degrees opening in the ring at any point. |
| 2026-08-20 | **Refresh busts the cache** (`?t=` on the URL) -- a plain reload was being answered from cache, so a rebuilt snapshot still showed the old data. Added `refresh-graph.ps1`, which rebuilds and opens in one step, since the page itself cannot regenerate its data. |
| 2026-08-20 | **Reveal cascade.** Notes fade in one at a time, sweeping clockwise from 12 o'clock along the circumference, each wedge fanning open between its two neighbours to make room as they arrive; departures sweep the same way. Opacity is now the source of truth for rendering, replacing the instant `hidden` flip. |
| 2026-08-20 | Doc reconciled with the code: dropped the shape-differentiation and Rings-vs-Force-mode sections for features that no longer exist, corrected the `lib/` contents, and remeasured the diagnostics (441 notes, 1409 links, 9% intra-folder, 127 dead ends, 0 orphans, 141 unresolved, 8 shells / 34-note 8-core). |
| 2026-08-20 | Moved out of `.vault-graph/` to `03 - Resources/Vault Graph/`. Synology Drive filters entries starting with `.`, so the dot-folder had never reached the other devices. `build-graph.mjs` now finds the vault by walking up for `.obsidian` instead of assuming its parent. |
